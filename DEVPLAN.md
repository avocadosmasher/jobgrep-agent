# DEVPLAN — 취업준비 Helper Agent 개발 허브

> **이 문서의 역할** — 전체를 꿰는 **허브**. 무엇을 어떤 순서로 만드는가 + **어디까지 됐는가**.
> 개별 태스크의 상세 지시는 `tasks/T##.md`에 있다. 이 문서에는 없다.
> 매 세션 첨부되며, 세션 종료 시 **§3 원장**과 **§5 요약**이 갱신된다.

---

## §0 · 문서 지도

| 문서 | 역할 | 대상 독자 | 언제 첨부하나 |
| --- | --- | --- | --- |
| `README.md` | 프로젝트 개요·실행법 | **사람** (저장소를 처음 여는 사람) | 세션에 첨부 불필요 |
| `AGENTS.md` | 에이전트 자동 로드용 운영 계약 (명령·경계·고유 규약) | AI | 저장소 기반 도구가 **자동** (수동 첨부 불필요) |
| **`DEVPLAN.md`** (이 문서) | 허브 — 프로토콜·원장·구조 | AI | **매 세션** |
| **`tasks/T##.md`** | 태스크 1건의 상세 지시 | AI | **해당 1장만** |
| **`contracts/**`** | 인터페이스 정본 (코드) | AI | **매 세션** (T01 이후) |
| `DEVLOG.md` | 일탈·결정 기록 (append-only) | AI | 문제 발생 시 / 선택 결정 시 |
| `docs/INDEX.md` | `docs/` 폴더 안 파일 색인 | AI | 필요 시 |
| `docs/계약_초안.md` | 계약의 산문 초안 | AI | **T01에서만** (완료 후 폐기) |
| `docs/설계_v2.md` | 설계 근거 — 왜 이렇게 만드는가 | AI | 카드가 §번호로 가리킬 때만 |
| `docs/UC1_레벨측정_질문세트.md` | UC-1 질문 명세 | AI | T23에서만 |

> **`README.md` vs `DEVPLAN.md`** — README는 사람이 저장소를 열었을 때 "이게 뭐고 어떻게 돌리는가"를 5분 안에 파악하게 하는 문서다. DEVPLAN은 AI가 태스크를 수행할 때 필요한 실행 프로토콜이라 세션 첨부 대상이지만, README는 AI 세션에 첨부할 필요가 없다. 이름이 비슷한 `docs/INDEX.md`(구 `docs/README.md`)는 `docs/` 폴더 안 색인일 뿐이라 혼동하지 않도록 이름을 분리했다.

**세션당 기본 첨부는 3개**: `DEVPLAN.md` + `tasks/T##.md` + `contracts/`.

> **왜 계약 정의가 이 문서에 없나** — T01 이후 `contracts/`가 정본이다. 같은 정의를 문서에도 두면 **진실 원천이 둘**이 되어, 한쪽만 고쳤을 때 조용히 어긋난다. `docs/계약_초안.md`는 T01의 **입력**일 뿐이며 T01 완료와 동시에 폐기 표시한다.

---

## §1 · AI 작업 프로토콜

### 1-1. 핵심 원리 — 왜 남의 소스를 안 봐도 되는가

인간 팀이 API 문서만 주고받으며 병렬 개발하는 이유는 **인터페이스가 구현보다 먼저 고정되기 때문**이다. 여기서도 같다. 결정적 차이는:

> **계약은 산문이 아니라 실행 가능한 코드다.**
> `contracts/`의 Pydantic 모델과 함수 시그니처가 계약이며, 구현 모듈은 이를 **import** 한다.
> 산문 명세는 해석 여지가 있어 표류하지만, import한 타입은 표류할 수 없다.

태스크를 맡은 AI가 읽는 것은 넷뿐이다: ① `contracts/**` ② 자기 카드 1장 ③ 카드가 지정한 픽스처 ④ (필요 시) 카드가 가리킨 설계도 §번호.

**다른 모듈의 구현 코드는 읽지 않는다.** 읽어야만 만들 수 있다면 계약이 부실하다는 신호이므로, 구현 대신 `DEVLOG.md`에 기록하고 계약 보강을 요청한다.

### 1-2. 세션 시작 프롬프트 (그대로 복사)

```
프로젝트: 취업준비 Helper Agent
첨부: DEVPLAN.md, tasks/T07.md, contracts/ (+ 카드가 지정한 픽스처)

tasks/T07.md 의 지시를 수행해줘.

규칙:
- 카드의 "소유 파일"에 명시된 파일만 생성/수정할 것
- contracts/ 는 수정 금지. 문제가 있으면 멈추고 DEVLOG.md 에 기록
- 카드의 "검증" 명령이 통과해야 완료
- 마지막에 DEVPLAN.md §3 원장의 해당 행과 §5 요약을 갱신
```

선행 태스크의 **구현 코드는 첨부하지 않는다.** 그 태스크가 남긴 **계약과 픽스처**만 주면 된다.

### 1-3. 불변 규칙

| # | 규칙 | 이유 |
| --- | --- | --- |
| R1 | **`contracts/`는 T01 외 수정 금지.** 변경 필요 시 구현 중단 + `DEVLOG.md` 기록 | 계약이 흔들리면 이 방식 전체가 무너짐 |
| R2 | **카드의 "소유 파일" 밖을 건드리지 않는다** | 세션 간 충돌 방지 |
| R3 | **완료 표기는 §3 원장 한 곳에서만** | 상태가 흩어지면 진척을 못 믿음 |
| R4 | **검증 명령 통과가 완료 조건.** 실패 시 `⚠️` + 사유 기록 | "됐다"는 자기보고 금지 |
| R5 | **테스트는 픽스처를 쓴다.** 자기 구현을 흉내낸 mock으로 통과시키지 않는다 | 스텁이 초록불 받는 것 방지 |
| R6 | **LLM 호출은 `llm/` 어댑터를 통해서만** | 모델 교체·테스트 대체 가능하게 |
| R7 | 카드 범위 밖 개선 아이디어는 **구현하지 말고 `DEVLOG.md`에** | 스코프 크립 방지 |

### 1-4. 상태 표기

⬜ 미착수 · 🔄 진행중(중단됨) · ✅ 완료(검증 통과) · ⚠️ 완료(부채 있음, DEVLOG 필수) · ⛔ 차단됨

---

## §2 · 리포지토리 구조와 소유권

```
jobprep-agent/
├─ README.md               # 사람용 — 프로젝트 개요·실행법
├─ AGENTS.md               # 에이전트 자동 로드 — 명령·경계·고유 규약
├─ pyproject.toml          # uv 프로젝트·의존성 (T00)
├─ uv.lock                 # 잠긴 의존성 (전이 포함)
├─ .python-version         # 3.11+
├─ DEVPLAN.md              # 허브 (이 문서)
├─ DEVLOG.md               # 일탈·결정 로그
├─ tasks/                  # T01.md ~ T29.md
├─ docs/                   # INDEX / 설계_v2 / UC1질문세트 / 계약_초안
├─ contracts/              # ★ 정본 — R1 적용
│  ├─ enums.py  models.py  tools.py  state.py
├─ llm/client.py           # LLM 어댑터 (R6)
├─ tools/                  # 도구 구현
├─ nodes/                  # LangGraph 노드 (state -> partial state)
├─ graphs/                 # analysis_graph / profile_graph / session
├─ render/                 # markdown.py / cards.py
├─ app/                    # Streamlit
├─ presets/                # categories.yaml / level_questions.yaml / node_labels.yaml
├─ fixtures/               # 골든 테스트 데이터
└─ tests/
```

**소유권 원칙** — 파일 하나는 태스크 하나가 만든다. 여러 태스크가 같은 파일을 고쳐야 하면 카드 설계가 잘못된 것이다.

### 2-1. 예외 — 배선 전용 공유 파일 (D40)

아래 셋은 **소유자가 없는 공유 파일**이다. 각 카드가 자기 노드·렌더러·라벨의 **호출부만** 여기에 추가하며, 이는 R2 위반이 아니다.

| 파일 | 각 카드가 추가하는 것 | 건드리면 안 되는 것 |
| --- | --- | --- |
| `graphs/analysis_graph.py` | `NODE_SEQUENCE`에 자기 노드 1줄 | 배선 알고리즘·조건부 간선 규칙 |
| `app/main.py` | 자기 화면 요소의 호출 1줄 | 폼·재개 루프·`run_analysis()` 경계 |
| `presets/node_labels.yaml` | 자기 노드의 `label` 블록 | 스키마·`fallback`·다른 노드 항목 |

**왜 규칙으로 올렸나** — 소유권 원칙이 배선 파일에는 애초에 안 맞는다. 노드를 만드는 카드와 그것을 그래프에 꽂는 카드가 다르면 **만들어졌는데 아무도 안 부르는 모듈**이 남는다. 실제로 T14가 그렇게 됐고(D39), T12(D29)·T13(D33)은 매번 예외 승인을 받아 넘겼다. 세 번 예외면 그건 규칙이다.

**대신 두 가지를 요구한다.** ① 카드의 "소유 파일"에 그 파일을 **명시**할 것 — 안 적혀 있으면 여전히 못 건드린다. ② **추가한 배선을 카드의 검증에 포함**할 것. 배선은 테스트가 없으면 조용히 빠진다.

> **새 카드를 쓸 때의 체크** — "이 카드가 만드는 것을 **누가 부르는가?**" 답이 "아무도"면 카드가 미완성이다. T18은 착수 시 소유 파일에 `graphs/analysis_graph.py`를 추가해 이 체크를 통과했고(배선 테스트 3건 포함), **T25(`nodes/gates.py`)에 같은 구멍이 남아 있다** — 착수 시 같은 추가를 할 것.
>
> **덧 (T18, D59) — 배선 한 줄은 남의 테스트를 깬다.** `NODE_SEQUENCE`에 노드를 넣자 다른 카드 소유의 테스트 5건이 죽었다(하드코딩된 노드 목록 / "다음 줄은 X"라는 전제 / 노드 함수를 한 모듈에서 `getattr`로 찾는 루프). 이 편집도 §2-1 예외에 포함된다 — 다만 고칠 때 **전제를 배선에서 유도하도록** 바꿀 것. 그러면 다음 노드가 끼어도 안 깨진다.

**픽스처가 구조의 핵심인 이유** — `aggregate_states`를 만드는 AI는 웹 수집기가 없어도 작업할 수 있어야 한다. T02가 골든 데이터를 먼저 깔기 때문에 이후 모든 모듈이 상류 모듈 없이 개발·검증된다. 이게 없으면 태스크가 직렬로 묶여 이 방식의 이점이 사라진다.

---

## §3 · 상태 원장 (Single Source of Progress)

> **완료 시 이 표만 갱신한다 (R3).**

### Phase 0 · 척추 — 붙여넣은 JD 1건 → .md 다운로드 관통

| ID | 태스크 | 상태 | 완료일 | 비고 |
| --- | --- | --- | --- | --- |
| T00 | 프로젝트 부트스트랩 (uv init/venv/pyproject) | ✅ | 2026-08-03 | `--package` 대신 `--app` 사용, DEVLOG D01 참조 |
| T01 | 계약 정의 (enums/models/tools/state) | ✅ | 2026-08-03 | `ProfileJSON.coverage`는 §10-2 초안(`dict[str, float]`)이 아닌 계약초안 3-2(`dict[Category, float]`) 채택 — 타입체크 원칙(§7-1)과 일치 |
| T02 | 골든 픽스처 세트 | ✅ | 2026-08-03 | ◇ 슬롯은 `""`(D03), pytest `pythonpath` 설정 추가(D04) |
| T03 | `aggregate_states` (규칙, LLM 없음) | ✅ | 2026-08-05 | 경계값은 `MET_THRESHOLD`/`ADJACENT_THRESHOLD` 상수 분리(§16 S6). 판정 불가(분모 0)는 `UNMET` 반환 — DEVLOG D05·D06 |
| T04 | LLM 어댑터 + `extract_competencies` | ✅ | 2026-08-05 | 오프라인 19건 + **온라인 `-m llm` 통과(D09 해소)**. skip의 진짜 원인은 키가 아니라 conftest 부재였음 — `tests/conftest.py` 신설(D21). 모델은 `gpt-4.1-mini` 유지 — gpt-5 계열은 temperature 미지원 + reasoning 토큰이 output 과금(D22). 의존성 openai 2.53.0·python-dotenv(D07), pytest 기본 실행에서 llm 마커 제외(D08) |
| T05 | `decompose_criteria` + `verify_criteria` | ✅ | 2026-08-05 | 검증 통과(26건 + **온라인 `-m llm` 통과**, D21). 근거 없는 MET/PARTIAL·UNKNOWN·누락은 Question 승격, UNMET은 근거 없이도 판정(D10). `is_required`는 importance에서 파생(D11). `question_id = q-{criterion_id}` 규약 — T11/T12는 `question_id_for()` 사용(D12) |
| T06 | `build_strategy_brief` (3트랙 트리아지) | ✅ | 2026-08-05 | 검증 통과(24건). **◇ 슬롯은 빈 채로 반환 — 채우는 태스크가 계획에 없음(D13, 사용자 판단 필요)**. MET은 상회 여부와 무관하게 트랙1(D14), 구간 경계값 상수화(D15), 카드 미생성 조건 재해석(D16) |
| T07 | Markdown 렌더러 | ✅ | 2026-08-05 | 검증 통과(24건). 7개 섹션 순서 고정, 빈 트랙은 "해당 없음". 파일명 날짜는 `target_date`·경로 문자 치환(D17), 빈 ◇ 슬롯은 `_(아직 작성되지 않음)_`(D18) |
| T06b | 브리프 ◇ 슬롯 채우기 (`fill_brief_slots`) | ✅ | 2026-08-05 | 신설 카드(D19). 오프라인 23건 + **온라인 `-m llm` 통과**(D21) — ■ 불변·병합·실패 처리. `LLMError`는 삼키고 그 밖의 예외는 전파(D20) |
| T08 | 최소 analysis_graph 조립 | ✅ | 2026-08-05 | 오프라인 10건 + **온라인 관통 1건 통과**(실제 JD → 3트랙 브리프). 7노드 직선, `fill_slots` 포함(D19). langgraph 1.2.10 추가(D23). P0는 `my_level=None` — 후보쌍(T14) 부재, 강점 표시만 미뤄짐(D24) |
| T09 | 최소 Streamlit (입력→실행→다운로드) | ✅ | 2026-08-08 | `AppTest`로 앱 실구동 검증 통과 — 폼→실행→`.md` 2,968바이트 다운로드. **브라우저 실 API 관통 완료(2026-08-08, D51)** — 실제 채용공고 이미지를 올려 카드까지 도달. D26 부채 해소. 프로필 업로더 추가(§10-3 근거, D25), 실행 로직은 `run_analysis()`로 분리(T12 대비), streamlit 1.61.0 추가 |

**완료 판정** — JD 본문을 붙여넣고 실행하면 3트랙 브리프 `.md`가 다운로드된다.

### Phase 1 · HITL 골격 (기술 최대 리스크 — 일찍 뚫는다)

| ID | 태스크 | 상태 | 완료일 | 비고 |
| --- | --- | --- | --- | --- |
| T10 | Checkpointer + thread_id 세션 관리 | ✅ | 2026-08-05 | 검증 통과(17건). langgraph **1.2.10 실측**으로 API 확인 — 미시작 판별은 `next`가 아니라 `created_at is None`(양쪽 다 `()`라 오인 위험), 중단은 `snapshot.interrupts`(D27). `SqliteSaver` 기본 + `InMemorySaver` 옵션, `langgraph-checkpoint-sqlite` 3.1.1 추가. **완료 스레드 재실행 안 함을 카운터로 증명** |
| T11 | 델타 인터뷰 노드 (`interrupt`) | ✅ | 2026-08-05 | 검증 통과(16건). 실제 그래프에서 중단→답변 주입→재개로 verdict 갱신 증명, **3라운드 진입 불가** 고정. `interrupt()` 앞은 부작용 없는 순수 계산만(재개 시 재실행되므로, D28). 답변 키는 question_id/criterion_id 양쪽 수용, 빈 답변 폐기, 상한 초과는 라운드 미소비 |
| T12 | Streamlit 재개 루프 (`Command(resume=)`) | ✅ | 2026-08-06 | 검증 통과(17건). **완료 조건을 호출 횟수로 증명 — 재개 후에도 `extract`·`decompose` 각 1회 고정**(처음부터 돌았다면 2회). `AppTest`로 입력→중단→답변→다운로드 관통. 그래프 배선은 사용자 판단으로 `build_analysis_graph(checkpointer, *, interactive=)` 확장(R2 예외, D29). 인터뷰 자기 루프는 조건부 간선·라운드 상한은 노드에 위임(D30), 그래프는 세션 보관(D32) |

**완료 판정** — 폼 제출 시 그래프가 처음부터 재실행되지 않고 중단 지점에서 재개된다.

### Phase 2 · 진행 표시 + 매칭 정교화

| ID | 태스크 | 상태 | 완료일 | 비고 |
| --- | --- | --- | --- | --- |
| T13 | 노드 이벤트 스트림 → `st.status` | ✅ | 2026-08-06 | 검증 통과(30건). 카드의 수동 검증을 자동화 + **뮤테이션 3종으로 그물 확인**(D35). 스트리밍은 `resume_or_start(on_event=)`로 경계 안에 뒀다 — R2 예외, 사용자 승인(D33). 문구는 전부 `presets/node_labels.yaml`, 모르는 노드는 `fallback`으로 떨어져 내부명이 안 샌다. 자기 루프 조기 승격은 `repeats_while`로 차단(D34). 패널 자리는 `st.empty()` 하나 — 안 그러면 재개 중 패널이 둘(D35). pyyaml 추가 |
| T14 | 임베딩 + Faiss 후보쌍 검색 | ✅ | 2026-08-07 | 오프라인 20건 + **온라인 3건 통과**. S8 확정 — `text-embedding-3-large` + `dimensions=1536`(D37, 별칭 표기 R@1 100% vs small 89%). R6·R2 충돌은 **신규 파일 `llm/embeddings.py`**로 해소(D39, D21·D31·D35와 같은 판단 — 양쪽 다 안 어김). API 왕복 1회 고정, **유사도 점수 미반환이 §7-2 불변식의 구조적 보장**. 뮤테이션 3종 중 2종이 살아남아 정규화 테스트 보강(D39). `faiss-cpu` 1.15.0·`numpy` 추가 |
| T14b | 후보쌍 배선 + `my_level` 확정 | ✅ | 2026-08-08 | 오프라인 312건 + **온라인 1건 통과**. 신설 카드(D40). `retrieve` 노드를 `extract` 뒤에 배선 — 소비처(`aggregate`) 앞이 아니라 **입력이 갖춰지는 지점**에 뒀다. **`my_level`은 유사도가 아니라 판정이 게이트다** — `UNMET`이면 후보쌍이 있어도 `None`(§7-2 + §7-3 동시 충족, D43). **뮤테이션 3종 전부 잡힘**(D39는 2건 생존). 오프라인이 실 임베딩 API를 타는 걸 발견해 스텁 2곳 보강 — R2 예외, 사용자 승인(D43) |
| T15 | Streamlit 카드 렌더러 | ✅ | 2026-08-08 | 오프라인 343건 통과(그중 parity·배선 31건). parity를 문자열 비교가 아니라 **표시 항목 집합**으로 정의 — 순수 계층 `brief_items()`를 두고 `.md` 본문과 대조(D44). **뮤테이션 7종 전부 잡힘.** 계약에 없는 중요도·기준별 판정은 `GraphState`에서 받되 `brief_items()`가 `brief`만 받는 것으로 항목 불변을 구조 보장(D45). 최소 요건은 `st.metric`이 아니라 배너 — 사용자 판단, R2도 안 어긴다(D46). T09 인라인 렌더링 삭제 + 배선 테스트 4건(D47) |
| T15b | JD 이미지 업로드 → 텍스트 추출 | ✅ | 2026-08-08 | 오프라인 38건 + **온라인 1건 통과**(실제 스크린샷 → JD 전문). 신설 카드(D42). **업로더는 폼 밖** — 카드 스니펫대로 폼 안에 두면 동작하지 않고 `AppTest`는 그걸 못 잡는다(D48). `llm/vision.py` 신설로 R6·R2 동시 충족(D39와 같은 판단). 오프라인이 또 실 API를 탄 원인은 `AppTest`가 스크립트를 새로 실행하는 것 — 스텁은 **원본 모듈**에(D49). **뮤테이션 10종 전부 잡힘**(3종 생존 → 테스트 보강, D50) |

**완료 판정** — 노드 진행이 화면에 보이고, 후보쌍이 `my_level`을 채워 **브리프가 "나에게 맞춰" 정렬된다**(트랙2 우선순위·트랙1 강점 정렬).
→ **여기까지가 최소 데모(MDD).** 이후는 전부 품질 증강이다.

> **완료 판정 문구를 고쳤다 (D40).** 원래는 "후보쌍 필터링으로 LLM 호출이 유의미하게 감소"였는데 **달성될 수 없는 문장이다.** 이 파이프라인의 LLM 호출은 extract 1 + decompose 1 + verify 1(라운드당) + fill_slots 1로 **입력 규모와 무관하게 고정**이다 — "LLM 도구는 배치 호출"이 이미 그렇게 만들어 놨다. 후보쌍을 끼워도 호출 수는 그대로고, 줄어드는 건 verify 프롬프트의 프로필 블록 토큰인데 이 규모(요구 26 × top-3 ∪ ≒ 보유 8 전부)에선 그마저 실질이 없다. **T14의 진짜 값은 비용이 아니라 `my_level`이다.** D38이 Faiss에 쓴 잣대를 완료 판정에도 댄 것이며, 안 고치면 T29 질의응답에서 그대로 역공당한다.

> ✅ **배선 갭 — T14b로 해소됨 (2026-08-08, D43).** 기록으로 남긴다: T14가 `retrieve_candidates`를 만들었지만 **아무도 호출하지 않았다.** T14의 소유 파일은 `tools/retrieve.py`·`tests/test_retrieve.py` 둘뿐이라 노드 배선이 카드 범위 밖이었고, T15는 렌더러라 이걸 붙이지 않았다. 파장은 D39가 적은 것보다 넓었다 — `my_level`이 전원 `None`이라 `level_gap()`의 `mine`이 상수가 되고, 그 결과 트랙2 우선순위가 JD 요구 레벨만 반영하고 **사용자 격차는 반영하지 못했다.** 트랙1 강점 정렬(`exceeds_requirement`)도 죽어 있었다. → **T14b** 신설(사용자 승인, D40)로 `retrieve` 노드를 배선해 해소. **이 갭이 §2-1 규약을 낳았다** — 새 카드를 쓸 때 "이걸 누가 부르는가"를 반드시 확인할 것.

### Phase 3 · 수집 확장

| ID | 태스크 | 상태 | 완료일 | 비고 |
| --- | --- | --- | --- | --- |
| T16 | `fetch_jd_body` 3층 폴백 | ✅ | 2026-08-08 | 오프라인 75건 통과, **뮤테이션 24종 전부 잡힘**(D54). 층은 **URL 호스트로 판별** — 붙여넣기=①상, 회사 페이지=②중, 알려진 플랫폼=③하(D52). **수집 실패는 예외가 아니라 빈 본문 문서**(`is_uncollected()`) — 계약 반환형이 Optional이 아니고 "조용히 스킵"이 불변식이라 그렇다. **실 HTTP 1회가 gzip 결함을 잡았다 — 오프라인 75건이 전부 초록불이었다**(D53). 새 의존성 없음(urllib·html.parser) |
| T17 | `fetch_tech_blog` / `get_company_values` | ✅ | 2026-08-09 | 오프라인 78건 통과, **뮤테이션 35종 전부 잡힘**(D57). 실패는 T16과 달리 **빈 리스트** — 반환형이 `list`라 판별자가 필요 없다. missing은 순수 함수 `missing_soft_sources()`가 수집 결과에서 유도하며 **라벨 어휘는 `SourceType` 값 그대로**(`source_coverage`와 같은 어휘, D55). 표지어 판별로 엉뚱한 페이지(404 안내·홈)를 버리고, 통짜 문서는 청킹 대신 **줄 경계 절단**. **실 URL 확인이 apex/`www.` 문제를 잡았다** — `hyundai-autoever.com`은 TLS에서 죽고 `www.`만 산다(D56). 새 의존성 없음 |
| T18 | 수집 ReAct 서브에이전트 + 예산 | ✅ | 2026-08-10 | 오프라인 39건 + **실 URL 3종 통과**(LLM 비용 0). 뮤테이션 26종 중 25종 잡힘, 생존 1종은 동치라 죽은 가드를 삭제(D60). **T16·T17의 배선 갭을 닫았다** — `collect` 노드가 수집 도구 셋을 예산 안에서 부른다. ReAct의 **구조는 유지하되 정책은 규칙** — 도구 셋에 LLM이 고를 게 없고, 고르게 하면 D56("주소를 추측하지 말 것")을 어긴다(D58). 무한 루프를 막는 것은 예산이 아니라 `tried` 집합. 재시도 각도는 **수집한 JD 주소에서** 얻는다 — 실 URL로 값을 냄(D61). 예산 소진은 `GateStatus`에 칸이 없어 `missing` 라벨 + `budget_exhausted()` 판별자로(R1 유지, D59). JD 문서는 추가가 아니라 **교체**(D59). 배선 1줄이 남의 테스트 5건을 깨 최소 편집 — §2-1 예외, T14b 전례(D59) |
| T19 | `discover_jobs` + H1 공고 선택 | ✅ | 2026-08-10 | 오프라인 67건 + **실 URL 5사 확인**(LLM 비용 0). 뮤테이션 **29종 전부 잡힘**(생존 4종 → 테스트 보강, D65). 카드 검증란이 "수동"이나 `AppTest`로 자동화 — 중단점이 둘이 된 첫 카드라 회귀가 조용히 날 자리다. **중단점 둘**: `INTERACTIVE_INSERTS`로 삽입 지점 일반화, H1은 `collect` 앞(D63). **발견도 대화형 전용** — 물어볼 사람이 없으면 시스템이 골라야 하고 그건 §12-2 금지(D63). multiselect 때문에 `app/hitl.py` 확장(위젯 한 곳 + 제출 문구, R2 예외·사용자 승인, D62). 발견은 **메타만** 가져오고 고른 것만 본문 수집. **실물이 오탐 두 종을 잡았다**(D64) — 표지를 URL 전체에 대면 채용 서브도메인이 아무 페이지나 공고로 만들고, 후보 페이지를 잘못 고르면 당근알바 구인글이 회사 공고로 올라온다 |
| T20 | (선택) 공식 채용 API 연동 | ⛔ | 2026-08-10 | **착수하지 않기로 결정(사용자 판단, D67).** 카드가 스스로 "승인 지연 시 ⛔ 두고 다음 Phase로 진행"이라 적은 선택적 증강이고, 키 발급·승인이 통제 불가 요소다. **카드의 완료 조건("API 없이 실행해도 T19가 정상 동작")만 떼어 회귀 확인 — `tests/test_fetch_jd.py` 75건 통과.** 발견 품질의 실제 한계는 D64에 있으며 이 카드 없이는 안 풀린다(정적 스크레이핑의 벽). 재개하려면 이 행을 ⬜로 되돌릴 것 |

### Phase 4 · UC-1 + 견고화

| ID | 태스크 | 상태 | 완료일 | 비고 |
| --- | --- | --- | --- | --- |
| T21 | `parse_resume` 계층화 (텍스트 우선) | ✅ | 2026-08-10 | 오프라인 38건 통과, **뮤테이션 28종 전부 잡힘**(생존 4종 → 픽스처·테스트 보강, D68). 완료 조건("텍스트 PDF는 OCR 없이")은 결과 텍스트로 증명이 안 돼 **OCR 호출 횟수로 증명**했다 — T12 전례. **T22가 이 파일을 수정할 수 없어서(소유 파일이 `tools/ocr.py`·`app/hitl.py`뿐) 호출부를 미리 뚫어 뒀다** — `tools/ocr.py`에 `ocr_document(path)->str`이 생기면 자동으로 그 경로를 탄다(D68). 신뢰도는 층에서 나온다 — `CONFIDENCE_BY_LAYER` 유일 정의점(T16 규약). **품질 검사의 "한글 포함"은 "한글 또는 라틴"으로 완화** — 가려낼 것은 언어가 아니라 깨진 인코딩이고, 한글 필수면 영문 이력서가 통째로 OCR을 탄다(D68). 실패해도 **건진 텍스트를 그대로 반환**(H4가 고칠 거리가 있어야 한다). `pymupdf` 1.28.2·`python-docx` 1.2.0 추가 |
| T22 | OCR fallback (엔진) | ⚠️ | 2026-08-11 | 오프라인 33건 통과, **뮤테이션 22종 전부 잡힘**(생존 6종 → 죽은 가드 2개 삭제 + 테스트 4건 보강, D69). **⚠️ 사유: 온라인 검증 불가 — OpenAI 크레딧 소진(429 `credit_balance_exhausted`, D70).** 배관은 뚫렸다는 증거가 있다(400이 아니라 429라 페이로드 형식은 수용됨) 그러나 **모델이 한국어 스캔본을 실제로 읽는지는 미확인**이다. 크레딧 충전 후 `uv run pytest -m llm tests/test_ocr.py -q` 2건을 돌릴 것. **엔진은 기존 `llm/vision.py` 재사용**(사용자 판단) — 단 프롬프트가 JD 전용이라 `llm/ocr_vision.py` 신설로 갈랐다(D39·D42와 같은 판단). H4 보정은 **T22b로 분리**(D69) |
| T22b | H4 OCR 보정 (사용자 직접 수정) | ⬜ | | 신설 카드(D69). **의존에 T24가 있다** — 보정 화면이 얹힐 `profile_graph`와 이력서 업로드 화면을 T24가 만든다. 착수 시 §2-1 표에 `graphs/profile_graph.py` 한 줄 추가할 것 |
| T23 | 프리셋 YAML + UC-1 레벨 측정 폼 | ✅ | 2026-08-11 | 오프라인 48건 통과, **뮤테이션 30종 전부 잡힘**(생존 0, D71). 질문세트 §4의 26개 축을 `presets/level_questions.yaml`로 떨궜다 — **마커 문구가 코드에 하나도 없다**(문구를 다시 지으면 "행동 기준만" 원칙이 조용히 무너진다). **`level_coordinates` 키는 역량 id** — 질문세트 §6은 "역량명"이라지만 골든 픽스처와 D06이 이미 `pf-01`을 전제로 결정을 내려 뒀다(D71). 상태 계약에 설문 칸이 없어 `profile`로 반환(R1 유지, D59와 같은 판단). **주입점을 기본 인자로 박았다가 테스트가 실 임베딩을 탔다 — 누수 일곱 번째**(D71). `app/hitl.py`는 `default`(사전 채움)·`section` 두 필드 추가, 렌더 경로 한 곳(R2 예외·T19 `multi` 전례). `C_인재상컬처핏`은 레벨 축이 아니라 제외 |
| T24 | `profile_graph` + ProfileJSON 저장/로드 | ⬜ | | |
| T25 | 품질 게이트 + 실패 처리 | ⬜ | | 임계값 실측 확정 |
| T26 | 프롬프트 인젝션 격리 | ⬜ | | |

### Phase 5 · 평가·발표

| ID | 태스크 | 상태 | 완료일 | 비고 |
| --- | --- | --- | --- | --- |
| T27 | 루브릭 평가 하네스 | ⬜ | | |
| T28 | 과정 지표 로깅 | ⬜ | | |
| T29 | 발표 자료 | ⬜ | | |

---

## §4 · 테스트 정책

| 계층 | 대상 | 방식 |
| --- | --- | --- |
| 계약 | Pydantic 모델 | 픽스처 검증 (`model_validate_json`) |
| 순수 함수 | `aggregate_states`, `build_strategy_brief`, 렌더러 | 픽스처 in → 기대값 out. **LLM 없음** |
| LLM 도구 | extract / decompose / verify | 오프라인: 스키마·대조 로직 / 온라인: `-m llm` 마커 분리 |
| 노드 | 각 노드 | 가짜 `GraphState` dict 주입 → 부분 갱신 dict 검증 |
| 그래프 | 스모크 | 픽스처 입력으로 end-to-end 1회 |
| UI | Streamlit | 수동 + 스크린샷 |

**`uv run pytest -q`는 LLM 없이 통과해야 한다.** 비용 드는 테스트는 `uv run pytest -m llm`으로만 실행.
모든 명령은 `uv run` 접두 — venv 활성화 상태에 의존하지 않는다.

---

## §5 · 진행 요약 (매 세션 종료 시 갱신)

```
현재 Phase  : **P4** (UC-1 + 견고화) — **T21·T23 완료, T22 ⚠️(온라인 검증만 남음).
              남은 것은 T24 · T25 · T26 + T22b.**
              **P3는 닫혔다** — T16·T17·T18·T19 완료, T20은 `⛔`(착수 안 함, D67).
              아래 "P3 상태" 참조.
P3 상태     : **T16·T17·T18·T19 완료. T20은 선택적 증강이라 `⛔`로 두고 넘어갔다(D67).**
              P2는 T15b로 완전히 닫혔고 최소 데모(MDD)에 도달해 있다. JD를 붙여넣거나
              **스크린샷을 올리거나 URL만 넣어도**, 이제는 **회사명만 넣어도** 노드 진행이
              보이고 3트랙 브리프가 카드로 뜨며 `.md`로 내려받는다. **발표 가능 상태이며,
              그것이 실제 공고로 브라우저에서 확인됐다**(D51). P0~P3의 완료 카드에 부채로
              남은 ⚠️가 하나도 없다.
              입력 경로가 넷이 됐다 — 붙여넣기 · 이미지(T15b) · URL(T18) · **공고 발견 후
              선택(T19)**. 앞의 셋은 H1을 건너뛰고 마지막만 중단해서 묻는다.
다음 태스크  : **T24**(profile_graph + ProfileJSON 저장/로드). 의존 T21·T23 모두 완료다.
              **이 카드가 P4의 매듭이다** — T21(`parse_resume`)도 T23(`level_survey`)도
              **아직 아무도 안 부르고 있고**, 둘 다 T24의 그래프가 부른다:
              `START → parse_resume → extract → level_survey(HITL) → build_profile → END`.
              착수 시 **§2-1 "누가 부르는가" 체크를 두 모듈에 다 적용할 것.**
              주의 ① `nodes/level_survey.py`에 **이미 `build_profile` 순수 함수가 있다** —
              T24의 노드는 조립·영속을 맡고 그 함수를 부르면 된다(이름이 겹치니 확인할 것).
              주의 ② 이력서 업로드 위젯은 **폼 밖**에 둘 것(T15b가 같은 자리에서 깨졌다, D48).
              주의 ③ 화면을 바꾸는 카드이므로 **브라우저를 한 번 열어 볼 것**(D41·D51).
              P4 남은 순서는 **T24 → T22b → T25 → T26**.
              T22b가 T24 뒤인 이유는 아래 "T22 상태" 참조.
              **T25 착수 시 소유 파일에 `graphs/analysis_graph.py`를 추가할 것**(§2-1).
              **T27(루브릭)은 사람 라벨링 시간이 계획에 없어 병목**이다 — 발표 일정을 볼 때
              P4를 도는 동안 라벨링을 미리 시작해 두는 편이 낫다(아래 T27 유의사항)
T22 상태     : **엔진은 들어갔고 온라인 검증만 남았다(⚠️, D69·D70).** `tools/ocr.py`가 T21의
              이음매를 채워 **스캔 이력서가 실제로 OCR 경로를 탄다** — 배선을 고치지 않고도
              그렇게 됐고 테스트가 그것을 건다. 엔진은 기존 OpenAI vision 재사용이되
              프롬프트가 JD 전용이라 `llm/ocr_vision.py`로 갈랐다(D39·D42와 같은 판단).
              **남은 것 하나: 크레딧을 채우고 `uv run pytest -m llm tests/test_ocr.py -q`
              2건을 돌린 뒤 원장을 ✅로 고칠 것.** 읽히지 않으면 2순위는 Upstage Document
              Parse이고, 교체 지점은 `llm/ocr_vision.py` 하나다(D69 표).
              **H4 보정은 T22b로 분리됐다** — 보정 화면이 얹힐 `profile_graph`와 이력서
              업로드 화면을 **T24가 만들기 때문**이다. T22 카드의 의존란(T21·T12)에 안 적힌
              숨은 의존이었다. T22b 착수 시 **§2-1 표에 `graphs/profile_graph.py`를 추가**할 것
주입점 규약   : (T23이 남긴 것 — **모든 카드에 해당한다**, D71)
              **`def f(..., dep=some_function)`을 쓰지 말 것.** 파이썬 기본 인자는 정의
              시점에 묶여서, 주입점이 있는데도 `monkeypatch.setattr(module, …)`가 안 먹는다.
              T23이 그렇게 썼다가 **오프라인 테스트가 실 임베딩 API를 왕복**했고, 크레딧이
              소진돼 있어서 겨우 드러났다(잔액이 있었으면 초록불인 채로 과금). 형태는
              `dep=None` + 호출 시점에 `dep = dep or 기본값`이다 — `tools/retrieve.py`의
              `embed=None`, `llm/vision.py`의 `client=None`이 이미 그 형태다
T23이 남긴 것 : (레벨 측정·프로필을 건드리는 카드는 먼저 읽을 것)
              ① **`level_coordinates`의 키는 역량 id다.** 축이 추출 역량과 묶이면 그
                 역량의 `comp_id`, 안 묶이면 축 id(`d4-02`). 질문세트 §6은 "역량명"이라
                 적었지만 골든 픽스처와 D06이 이미 `pf-01`을 전제로 했다(D71)
              ② **마커 문구는 코드에 없다.** `presets/level_questions.yaml`이 정본이고
                 그 정본은 `docs/UC1_레벨측정_질문세트.md` §4다. 문구를 코드에서 지으면
                 "행동 기준만"이라는 첫 원칙이 조용히 무너진다
              ③ **"해당 없음"은 레벨 0이 아니다** — 좌표에 안 들어가고 커버리지 분자에서
                 빠진다. 무응답도 같게 다룬다
              ④ **`app/hitl.py`의 `default`는 레벨 측정 전용 예외다.** D28이 `index=None`을
                 못 박은 이유(없는 근거로 미리 고르기 금지)와 충돌하지 않는다 — 사전 채움은
                 이력서에서 추출된 근거로 고른 것이고, 레벨을 모르면 여전히 안 고른다
              ⑤ 설문 산출을 담을 칸이 `GraphState`에 없어 **`profile`로 반환**한다(R1 유지)
T21이 남긴 것 : (이력서 파싱을 건드리는 카드는 먼저 읽을 것)
              ① **신뢰도는 층에서 나온다** — 텍스트레이어 `상` / OCR `중` / 실패 `하`.
                 `CONFIDENCE_BY_LAYER`가 유일한 정의 지점이다(T16 D52와 같은 규약)
              ② **실패해도 건진 텍스트를 그대로 돌려준다.** T16처럼 빈 것을 주지 않는다 —
                 H4가 "보여주고 고치게" 하는 화면이라 고칠 거리가 있어야 한다.
                 화면은 `needs_manual_correction(text, confidence)`만 보면 된다
              ③ **품질 검사는 "한글 포함"이 아니라 "한글 또는 라틴"이다.** 가려낼 것은
                 언어가 아니라 깨진 인코딩이고, 한글을 필수로 걸면 영문 이력서가 통째로
                 OCR을 탄다(D68). 카드 문구에서 의도적으로 벗어난 지점
              ④ **픽스처를 가장 단순한 형태로 만들면 그 단순함이 사각지대가 된다.**
                 한 쪽짜리 이력서 픽스처는 "첫 쪽만 읽는" 회귀를 영원히 못 잡는데,
                 그건 분량이 임계를 넘어 품질 검사가 초록불을 주는 **조용한 손실**이다.
                 두 쪽으로 다시 구웠다(D68 M23)
              ⑤ **`fixtures/README.md`에 새 PDF 두 건이 안 적혀 있다** — T02 소유라
                 못 건드렸다(R2). `fixtures/`를 건드리는 다음 카드가 두 줄 추가할 것
발견 한계    : (T19가 남긴 것 — 발견에 기대는 카드는 먼저 읽을 것)
              **한국 대기업에서 `discover_jobs`는 대체로 0건이고 그게 정상이다.**
              카카오·토스·네이버·쿠팡 전부 0건 — 채용 사이트가 JS 렌더링이라 정적 본문에
              링크가 없다(D56의 현대오토에버와 같은 벽). 당근만 6건인데 그마저 개별
              공고가 아니라 분류 목록 링크다. **제품은 그래도 돈다** — 사용자가 JD를
              붙여넣으면 되고 폼이 그렇게 안내한다. 발견 품질을 올리려면 정적
              스크레이핑이 아니라 공식 API(T20)나 렌더링 엔진이 필요하다(D64)
T18 배선 결과 : ✅ **닫혔다(2026-08-10, D58·D59).** `nodes/collect.py`의 `collect` 노드가
              `ingest_pasted_jd`와 `extract` 사이에 서서 `fetch_jd_body`·`fetch_tech_blog`·
              `get_company_values`를 한꺼번에 부른다. T14가 만들고 아무도 안 부른 채
              남았던 구멍(D39·D43)의 세 번째 반복을 여기서 끝냈다. **화면(`app/main.py`)은
              여전히 URL 입력창이 없지만** JD 텍스트 영역에 URL을 붙여넣으면 그대로 통한다 —
              전용 입력창을 붙이려면 별도 카드로 신설할 것(T14b·T15b 전례), 그 카드는
              **브라우저를 한 번 열어 볼 것**(D41·D51)
정리 대상 하나 : **`ingest_pasted_jd`는 이제 산출이 전부 버려진다** — `collect`가 JD 문서와
              `selected_job_ids`를 다시 만든다(D59). 해롭지는 않지만(네트워크 안 탐,
              마이크로초) 부르는데 아무도 안 쓰는 노드다. **`nodes/analysis_nodes.py`
              소유자가 걷어낼 것** — T18의 경계가 아니라 손대지 않았다. 걷어내면 진행
              표시 줄 하나("JD 본문 수집")가 사라진다
P1 판정 근거 : 폼 제출 시 그래프가 처음부터 재실행되지 않는다 — 재개 후에도 `extract`·`decompose` 호출이 각 1회로 고정(재실행이면 2회). D31 표 참조
차단 요소   : ⛔ **OpenAI 크레딧 소진 (2026-08-11, D70).** `429 insufficient_quota /
              credit_balance_exhausted`. **온라인(`-m llm`) 테스트가 전부 막혔고**,
              T22의 ⚠️가 이것 때문이다. 오프라인 713건은 영향 없다.
              다만 **429는 400이 아니라 페이로드 형식은 수용됐다는 뜻** — 배관은 뚫렸고
              미확인은 모델의 인식 품질 하나다. 충전 후 `-m llm` 1건으로 먼저 확인할 것.
              (이력 — 아래는 그 전의 기록이다)
              **2026-08-07 크레딧 충전 확인으로 해제(D36).** 채팅·임베딩 양쪽 실호출 성공, `-m llm` 4건 통과(174.65초). T14의 온라인 검증도 열렸다
              2026-08-08 — 크레딧이 소진됐다는 전제로 T15b를 시작했으나 **실호출이 그대로 성공했다**(D49의 누수로 우연히 확인 → 온라인 1건 정식 통과). vision은 회당 1콜이라 값이 싸다. 크레딧 상태는 추정하지 말고 `-m llm` 1건으로 확인할 것
남은 부채   : **하나 — T22의 온라인 검증(⚠️, D70).** 크레딧이 채워지면 `-m llm` 2건으로 닫힌다.
              그 밖에는 없다. (P0~P3는 깨끗하다 — 아래 이력 참조)
              **2026-08-08 브라우저 실 API 관통으로 D26·D31·D35·D41이 한꺼번에 닫혔다(D51).**
              실제 채용공고(현대오토에버 2026 하반기 신입공채) JD **이미지**를 올려
              추출 → 분석 → 결과 카드까지 도달하는 것을 브라우저에서 확인했다.
              T15b의 이미지 경로가 실사용 첫 시도에서 그대로 통했다는 뜻이기도 하다.
              (이력 — 이 부채는 "API 키 문제"가 아니라 **HTTP 계층 전체가 미검증**이라는
              뜻이었다. D41에서 streamlit 1.61.0 + starlette 1.4.0이 gzip 미들웨어에서
              죽어 브라우저 접속이 100% 실패했는데 오프라인 296건은 전부 초록불이었다 —
              `AppTest`는 스크립트를 인프로세스로 돌려 ASGI·HTTP 경로를 안 탄다.
              **이 교훈은 부채가 닫혀도 유효하다**: 화면을 바꾸는 카드는 오프라인이
              초록불이어도 브라우저를 한 번 열어 볼 것)
T14 유의사항 : ① 진행 표시에 줄을 하나 늘리려면 **`presets/node_labels.yaml`에 라벨을 추가**하면 된다 — `app/progress.py`는 손대지 않는다(D34). 라벨이 없으면 그 줄은 "처리 중"으로 뜬다(동작은 함). ② 노드 안에서 `get_stream_writer()({"tool": …, "ok": …})`를 부르면 서브 라인으로 흐른다 — T18용으로 뚫어 뒀고 실제 스트림 관통까지 확인됐다(D33). ③ `resume_or_start()` 밖에서 `graph.invoke()`/`graph.stream()`을 직접 부르지 말 것 — 스트리밍도 `on_event=`로 이 경계를 지난다(D27·D33). ④ 그래프 인스턴스는 `get_graph()`가 세션에 들고 있다. **새로 컴파일하지 말 것**(D32)
수집 규약    : (T16이 남긴 것 — 수집 도구를 만드는 카드는 먼저 읽을 것)
              ① **수집 실패는 예외가 아니라 빈 본문 문서다.** `raw_text=""` + 신뢰도 하로
                 돌려주고 `tools.fetch_jd.is_uncollected(doc)`로 판별한다. 계약 반환형이
                 Optional이 아니고 "②③ 실패는 조용히 스킵"이 불변식이라 이 표현뿐이다(D52)
              ② **T25 하드 게이트는 JD 건수가 아니라 `is_uncollected()`가 거짓인 문서 수를
                 세야 한다.** 빈 문서를 1건으로 세면 게이트가 헛통과한다
              ③ **신뢰도는 층에서 나온다** — 붙여넣기 상 / 회사 페이지 중 / 플랫폼 하.
                 `CONFIDENCE_BY_LAYER`가 유일한 정의 지점이다(D52)
              ④ **네트워크 경계를 새로 만들면 실 URL로 한 번 지나가 볼 것.** 주입점 뒤에
                 갇힌 오프라인 테스트는 바이트 계층을 아예 안 탄다 — 75건 초록불이 gzip
                 미해제를 못 잡았다(D53). D41·D48·D49와 같은 맹점의 네 번째 반복이다
              ⑤ 실패 주입 테스트는 **"어디서 실패하는가"까지 지정**할 것. 방어 계층이 두
                 겹이면 앞 계층이 먹어버려 뒤 계층이 검증되지 않는다(D54)
              ⑥ (T17이 더한 것) **소프트 요건 실패는 빈 리스트다** — JD처럼 빈 문서를
                 돌려주지 않는다. 반환형이 `list`라 빈 리스트가 곧 미수집이고, 상류는
                 `missing_soft_sources(tech, values)`가 준 라벨을 `gate_status["missing"]`에
                 넣으면 된다. **라벨 어휘를 새로 짓지 말 것** — `SourceType` 값이 정본이고
                 `source_coverage()`가 이미 같은 어휘를 쓴다(D55)
              ⑦ **주워 온 게 엉뚱한 페이지면 안 가져온 것만 못하다.** 회사 사이트는 404
                 안내·홈을 200으로 돌려주는 일이 흔해서, 유형별 표지어가 하나도 없는 본문은
                 버린다(`_MARKERS`). 소프트 요건을 억지로 채우면 없는 근거로 판단하게 된다
              ⑧ **회사 주소를 추측해서 적지 말 것.** `KNOWN_SITES`에 박힌 절대 URL은 전부
                 실제로 한 번 가져와 본문이 나온 것만 남긴 것이다(D56). 빗나간 주소는 조용한
                 실패라 티가 안 나므로 사전이 조용히 썩는다. 관례로 안 찾아지는 회사는
                 호출자가 URL·도메인을 그대로 넘기거나 `sites=`로 주입한다
              ⑨ **apex(`example.com`)가 죽고 `www.`만 사는 사이트가 있다** — 한국 대기업에
                 흔하다. `live_host()`가 robots.txt 응답으로 고르며, 상태 코드가 오면(404
                 포함) 살아 있는 호스트, 상태 없는 실패(DNS·TLS)면 없는 호스트다(D56)
              ⑩ (T18이 더한 것) **수집 도구를 새로 만들면 `nodes/collect.py`의 루프에
                 붙인다.** 노드를 새로 만들지 말 것 — 예산·`tried`·진행 표시가 거기
                 한 자리에 모여 있다. 붙일 곳은 `SOFT_TARGETS`와 `_next_action()` 둘이며,
                 도구는 `Toolbox`에 `(target, company) -> 결과` 모양으로 넣는다
              ⑪ **무한 루프를 막는 것은 예산이 아니라 `tried` 집합이다.** 도구가 실패를
                 빈 결과로 표현하므로(①⑥) 실패를 근거로 재시도하면 영원히 돈다. 예산은
                 12회에서 끊어 줄 뿐 "끝났다"고 말할 수 없다(D58)
              ⑫ **예산 소진은 `gate_status["missing"]`의 라벨로 남는다** — `GateStatus`에
                 칸이 없고 계약은 수정 금지라 이 표현뿐이다. 문자열을 직접 비교하지 말고
                 `nodes.collect.budget_exhausted()`로 읽을 것(D59)
              ⑬ **소프트 요건을 못 찾으면 회사명 말고 두 번째 각도를 본다** — 이미 수집한
                 JD 주소에서 뽑은 도메인이다. 각도를 늘릴 때도 **근거에서 유도할 것**,
                 지어내지 말 것(⑧과 같은 규칙, D58·D61)
              ⑭ (T19가 더한 것) **링크 표지는 URL 전체가 아니라 경로에만 댈 것.**
                 `https://careers.x.com/about`의 `//careers.`가 경로 표지 `/career`를
                 만족시킨다. 채용 서브도메인은 후보 관례라 이 오탐은 예외가 아니라
                 **기본값**이다(D64)
              ⑮ **여러 후보 페이지의 결과를 합치지 말 것.** 후보 순서가 곧 신뢰 순서이므로
                 합치면 덜 믿을 만한 출처가 같은 급으로 올라온다. 당근은 `careers.`가
                 회사 채용이고 `jobs.`가 동네 구인 서비스라, 합치면 "김밥집 주방 직원"이
                 회사 공고 목록에 올라온다(D64)
              ⑯ **주워 온 링크가 그 페이지가 거느리는 것인지 볼 것**(`in_scope`) — 같은
                 호스트이거나 알려진 ATS·채용 플랫폼. **허용 목록으로 갈 것**: 차단
                 목록은 다음 회사에서 또 뚫리고, 빗나가도 덜 가져올 뿐이라 그 방향이
                 안전하다(§6-2). 플랫폼 어휘는 T16 `PLATFORM_HOSTS`가 정본이다(D64)
T28 참고    : 과정 지표는 **계측을 새로 심지 말고** `ProgressTracker.add_listener()`에 붙인다. 응답 시간·도구 호출 횟수는 `ProgressEvent`에 이미 실려 있다(D34)
화면 규약    : (T15가 남긴 것 — 화면을 건드리는 카드는 먼저 읽을 것)
              ① **화면에 뭔가 더 그리려면 `render/cards.py`의 `brief_items()`에도 추가해야 한다.** 안 그러면 parity 테스트가 잡는다(D44). 이게 "화면 = .md = 루브릭 단위" 일치를 지키는 장치다
              ② **`app/main.py`에 표시 로직을 다시 심지 말 것.** T09의 인라인 렌더링은 T15가 걷어냈고(D47), 다시 심으면 그 그물 밖에서 화면만 따로 자란다
              ③ **유사도 점수를 만들어 붙이지 말 것** — 검색기가 반환하지 않는다(§7-2, D39). "매칭 87%"류는 `.md`에 없으므로 parity가 깨진다. 표시할 근거는 기준별 판정과 원문 인용뿐이다
              ④ **상단 `st.metric` 네 칸(남은 기간·소스 충족률·신뢰등급·카드)은 고정이다** — `tests/test_hitl.py`가 라벨 집합을 못 박고 있고 그 파일은 T15 소유가 아니다(D46). 지표를 늘리지 말고 배너·캡션을 쓸 것
              ⑤ `BriefCard`에 없는 것(중요도·기준별 판정)은 `render_brief(brief, matches=, required=)`로 `GraphState`에서 넘긴다 — 계약을 고치지 않는다(D45). `is_strength`는 여전히 화면까지 못 온다(R1 필요), 강점은 **트랙1 정렬 순서**로만 드러난다
              ⑥ `my_level`이 `None`인 카드는 정상이다 — **UNMET이면 후보쌍이 있어도 비운다**(D43). "없음"은 버그가 아니라 판정 결과다
              ⑦ (T15b) **위젯을 새로 붙일 때 "폼 안인가 밖인가"를 먼저 판단할 것.** 기준은 하나 — 제출 전에 다른 위젯을 바꿔야 하면 **폼 밖**이다. `st.form` 안에서는 제출 전까지 rerun이 없어 값이 서버에 오지 않는다. **`AppTest`는 이 차이를 못 잡는다**(폼 배칭을 모사하지 않아 안/밖 둘 다 통과시킨다, D48)
              ⑧ (T15b) 이미지 추출 결과는 **초안이지 입력이 아니다.** 자동 제출하지 않고 `text_area`에 실어 사용자가 고칠 수 있게 둔다 — "근거 없이 지어내지 않는다"를 입력 단계에도 적용한 것
HITL 규약    : (T19가 남긴 것 — 중단점을 새로 만드는 카드는 먼저 읽을 것)
              ① **중단점은 `INTERACTIVE_INSERTS`에 한 줄로 붙는다**(`graphs/analysis_graph.py`).
                 `{앞 노드: [끼울 노드들]}` 모양이며 비대화형 배선은 건드리지 않는다
              ② **`interrupt()`를 부르는 노드는 비대화형 그래프에 넣지 말 것.** checkpointer
                 문제만이 아니라, 물어볼 사람이 없으면 시스템이 대신 결정해야 하는데 그게
                 대개 금지 사항이다(§12-2 임의 선택 금지, D63)
              ③ **폼은 `app/hitl.py`가 페이로드를 보고 그린다 — `app/main.py`에 다시 심지
                 말 것.** 다만 위젯 **종류**가 새로우면 `_render_widget`은 고쳐야 한다.
                 H1이 multiselect 때문에 그랬다(D62). 고치는 범위가 그 한 곳으로 좁게
                 유지되는 것이 이 모듈 설계의 값이다
              ④ **안내 문구는 `resume_notice()`(app/main.py)에 `kind`별로 추가**한다.
                 T12의 델타 인터뷰 문구가 다른 중단점에 뜨면 사용자가 못 알아본다(D63)
              ⑤ **다중 선택 답변은 `MULTI_SEPARATOR`로 이어 붙인 문자열 하나**다. 반환
                 타입이 `dict[str,str]`로 유지되며, 그 전제는 **라벨에 줄바꿈이 없다**는
                 것이고 만드는 쪽이 보장한다(D62)
테스트 규약  : (T15b가 남긴 것 — `AppTest`를 쓰는 카드는 먼저 읽을 것)
              **(T19 추가) 관통 테스트에 "끝까지 갔다"만 걸지 말 것.** 2건을 골랐는데
              1건만 분석해도 다운로드 버튼은 똑같이 뜬다 — **개수나 내용**을 하나 걸어야
              양이 틀린 회귀가 잡힌다(D65 M21).
              **(T19 추가) 그래프를 통째로 돌리는 테스트는 `retrieve_candidates`까지
              갈아끼울 것.** 도구 4종만 스텁하면 임베딩이 실제 API를 왕복하고, **성공하는
              동안은 계속 초록불이다** — 실제로 T14b 이후로 새고 있었고 429가 나서야
              드러났다(D66). `install_stubs`를 쓰면 자동으로 맞는다
              **스텁은 원본 모듈에 건다.** `AppTest.from_file`은 `app/main.py`를 별도 스크립트로 새로 실행하므로, 이미 import된 `app.main` 객체에 건 `monkeypatch`는 **안 걸린다.** 스크립트가 매 실행마다 다시 import하는 원본 모듈(`llm.vision`·`graphs.session`·`nodes.analysis_nodes`)을 갈아끼워야 한다(D49). `install_stubs`가 처음부터 그렇게 돼 있는 게 우연이 아니다
              **판별법 — 오프라인 테스트가 갑자기 느려지면 스텁이 새는 것이다.** 성능 문제가 아니다. 실 API를 타면 테스트 1건이 20초를 넘는다(D43·D49가 같은 결함의 재발)
S8 결과     : **`text-embedding-3-large` + `dimensions=1536`**(D37, 확정). 픽스처 정답쌍은 두 모델 다 100%라 변별이 안 됐고, 별칭 표기(한글↔영문·약어)에서 갈렸다 — large 9/9 vs small 8/9. Matryoshka 절단이라 1536차원에서도 품질이 안 떨어져 인덱스 메모리는 small과 같다. 회당 약 $0.0001. **재실험 금지 — 수치는 D37에 있다**
스택 판단   : 스택 목록은 **가용 목록이지 약속이 아니다**(D38, 사용자 확인). LangGraph는 쓰고 LangChain 모델 계층은 안 쓴다 — 둘은 다른 층이다. **재판단 지점이던 T18은 닫혔다** — 권장대로 `create_react_agent`를 안 쓰고 직접 짰다(D58). 근거는 예고된 둘(`BaseChatModel`이 `langchain-openai`를 끌고 온다 / "예산 소진 = 부분 수집"을 `recursion_limit` 예외로밖에 표현 못 한다)에 실제로 만들어 보고 알게 된 셋째가 붙었다: **도구 셋에 LLM이 고를 것이 없다.** Faiss는 이 규모에선 numpy로 대체 가능하나 사용자 판단으로 유지
계획 점검    : 2026-08-07 세션에서 남은 계획 전체를 훑었다(D40). 배선 주체가 없는 카드가 T14 말고도 **T15·T18·T25**에 있었다 — 원인은 소유권 원칙이 배선 파일에 안 맞는다는 것이고, §2-1 규약으로 승격해 해소했다. T18·T25는 착수 시 소유 파일에 `graphs/analysis_graph.py`를 추가할 것
T27 유의사항 : 완료 조건이 "사람-LLM 일치율"인데 **사람이 20~30건을 매기는 시간이 계획 어디에도 없다.** 발표 일정이 있다면 여기가 실제 병목이다 — P4 진입 시점에 라벨링을 병렬로 시작할 것
T24 참고     : 완료 조건("업로드 전후 질문 수 비교")이 수동으로 적혀 있으나, T12가 이미 호출 횟수를 자동으로 증명한 전례가 있다(D31). 수동으로 남길 이유가 없다
마지막 갱신  : 2026-08-07 — 크레딧 충전으로 차단 해제(D36) → T14 완료(D37·D39, `llm/embeddings.py`·`tools/retrieve.py`·`tests/test_retrieve.py` 신설). 스택 판단 정리(D38). 계획 점검 + T14b 신설 + §2-1 규약(D40). streamlit 실행 불가 해소 — 1.61.1 / starlette 1.3.1(D41). T15b 신설(D42).
              2026-08-08 — **T14b 완료**(D43). `retrieve` 노드 배선 + `my_level` 확정, 뮤테이션 3종 전부 잡힘. **오프라인 312 passed(6.7초) / 온라인 8 passed**
              2026-08-08 — **T15 완료 → P2 종료, MDD 도달**(D44·D45·D46·D47). `render/cards.py`·`tests/test_render_parity.py` 신설, `app/main.py` 인라인 렌더링 교체. **오프라인 343 passed(7.3초)**, 뮤테이션 7종 전부 잡힘. 새 의존성 없음
              2026-08-08 — **T09 부채 해소 → P0~P2에 ⚠️ 없음**(D51). 브라우저에서 실제 공고 이미지로 관통 확인. 코드 변경 없음
              2026-08-08 — **T16 완료 → P3 착수**(D52·D53·D54). `tools/fetch_jd.py`·
              `tests/test_fetch_jd.py` 신설. **오프라인 456 passed(9.9초)**, 뮤테이션 24종
              전부 잡힘. 새 의존성 없음(urllib·html.parser). 실 URL 2건으로 HTTP 계층
              확인 — gzip 미해제 결함을 거기서 잡아 고쳤다(D53)
              2026-08-09 — **T17 완료**(D55·D56·D57). `tools/fetch_soft.py`·
              `tests/test_fetch_soft.py` 신설. **오프라인 534 passed(9.6초)**, 그중 T17이
              78건. **뮤테이션 35종 전부 잡힘.** 새 의존성 없음(HTTP·robots·HTML은 T16 것을
              import). 실 URL 3사 확인 — 카카오·토스·우아한형제들 수집 성공(신뢰도 중),
              **현대오토에버는 0건이 정상 동작**이다(홈이 JS 렌더링이라 정적 본문이 없다 →
              `missing`에 표기되고 제품은 그대로 돈다)
              2026-08-10 — **T19 완료 → P3 실질 종료**(D62·D63·D64·D65·D66).
              `tools/discover.py`·`nodes/select_job.py`·`tests/test_discover.py`·
              `tests/test_select_job.py` 신설, `graphs/analysis_graph.py`에 H1 배선(§2-1),
              `app/hitl.py` multiselect 확장(R2 예외·사용자 승인), `app/main.py`에 발견
              경로·중단점별 안내. **오프라인 644 passed(9.6초)**, 그중 T19가 67건.
              **뮤테이션 29종 전부 잡힘.** 새 의존성 없음. 실 URL 5사 확인 — 오탐 두 종을
              거기서 잡았고(D64), 한국 대기업 발견이 대체로 0건이라는 것도 거기서 확인했다.
              **덤으로 T14b 이후 계속 새던 오프라인 스텁 누수를 잡았다**(D66)
              2026-08-10 — **T18 완료 → T16·T17 배선 갭 해소**(D58·D59·D60·D61).
              `nodes/collect.py`·`tests/test_collect.py` 신설, `graphs/analysis_graph.py`에
              `collect` 배선(§2-1), `presets/node_labels.yaml`의 라벨을 배선 구역으로 이동.
              **오프라인 573 passed(9.2초)**, 그중 T18이 39건. 뮤테이션 25/25 잡힘(동치 1종은
              죽은 코드라 삭제). **실 URL 3종 통과 — LLM 비용 0.** 새 의존성 없음.
              배선 여파로 `tests/test_graph_smoke.py` 3건·`tests/test_progress.py` 2건을
              최소 편집(T14b 전례) — 두 곳은 전제 자체를 배선에서 유도하도록 바꿔 다음에
              노드가 끼어도 안 깨진다
              2026-08-08 — **T15b 완료 → P2 완전 종료**(D48·D49·D50). `llm/vision.py`·`tests/test_vision.py`·`fixtures/jd_screenshot_sample.png` 신설, `app/main.py`에 폼 밖 업로더 배선. **오프라인 381 passed(11.7초) / 온라인 1 passed(7.2초)**, 뮤테이션 10종 전부 잡힘. 새 의존성 없음 — vision은 기존 `default_model()`을 그대로 쓴다
```
