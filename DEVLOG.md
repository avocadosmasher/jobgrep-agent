# DEVLOG — 일탈 및 결정 기록

> **append-only.** 기존 항목을 수정하거나 삭제하지 않는다.
> 계약과 다르게 만들었거나, 계약 자체에 문제가 있으면 여기에 적는다.
> **조용히 우회하는 것이 이 개발 방식을 무너뜨리는 가장 흔한 경로다.** 발견 즉시 기록하고 사용자에게 알린다.

## 기록해야 하는 경우

| 유형 | 예시 |
| --- | --- |
| **계약 문제** | `contracts/`를 고쳐야만 진행 가능 → 구현 중단하고 기록 (R1) |
| **소유권 침범** | 카드의 "소유 파일" 밖을 건드려야 했음 (R2) |
| **부채 완료** | 검증 명령을 통과 못 하고 `⚠️`로 완료 — **사유 필수** (R4) |
| **선택 결정** | 임베딩 모델(T14) / OCR 엔진(T22) / 임계값(T25) 등 + **선정 근거** |
| **범위 밖 아이디어** | 개선안이 떠올랐으나 카드 범위 밖 → 구현하지 말고 기록 (R7) |
| **문서 불일치** | 카드 서술이 라이브러리 실제 API와 다름 (특히 T10 LangGraph) |

---

## 기록

### 형식
```
### [D##] T## · 제목
- 일자 :
- 유형 : 계약문제 | 소유권침범 | 부채완료 | 선택결정 | 범위밖 | 문서불일치
- 상황 :
- 조치 :
- 후속 : (필요 시 어느 태스크에서 처리할지)
```

---

### [D01] T00 · `uv init --package` 대신 `--app` 사용
- 일자 : 2026-08-03
- 유형 : 문서불일치
- 상황 : T00 카드는 `uv init --package .`를 지시하지만, 이는 `src/jobprep_agent/__init__.py` src-layout과 `[project.scripts] jobprep-agent = "jobprep_agent:main"` 진입점을 생성한다. 반면 DEVPLAN.md §2는 `contracts/`, `tools/`, `nodes/`, `app/` 등 저장소 루트 바로 아래의 **평평한(flat)** 구조를 정본으로 명시하고, T01의 검증 명령도 `from contracts import models, enums, tools, state`로 **루트 기준 import**를 전제한다. `--package`로 만든 src-layout을 쓰면 이후 모든 태스크가 `contracts/`를 `src/jobprep_agent/contracts/` 아래로 옮기거나 import 경로를 바꿔야 해서 T01 이하 전 카드와 충돌한다.
- 조치 : `uv init --app --python 3.11 --no-readme .`로 재실행. src-layout·빌드 백엔드·scripts 진입점 없이 루트에 `pyproject.toml`만 남기고, uv가 함께 생성한 보일러플레이트 `main.py`(T00 소유 파일 목록에 없음)는 삭제. `app/main.py`는 예정대로 T09가 만든다. 이후 모든 모듈은 저장소 루트를 기준으로 한 네임스페이스 패키지(`contracts/`, `tools/` 등, `__init__.py` 불필요)로 import된다.
- 후속 : 없음 (T01부터는 이 구조를 전제로 진행)

### [D02] T01 · `ProfileJSON.coverage` 타입 — 계약초안 채택
- 일자 : 2026-08-03
- 유형 : 선택결정
- 상황 : `docs/설계_v2.md` §10-2 GraphState 초안은 `coverage: dict[str, float]`로 적혀 있는 반면, T01의 직접 입력인 `docs/계약_초안.md` 3-2는 `coverage: dict[Category, float]`로 적혀 있다. 둘이 다르다.
- 조치 : 계약_초안 3-2(`dict[Category, float]`)를 채택했다. T01 카드가 "옮길 대상"으로 명시한 문서이고, §7-1이 강조하는 "코드가 타입 체크할 수 있게" 원칙에도 `str` 자유 키보다 `Category` enum 키가 더 부합한다. §10-2의 `dict[str, float]`는 산문 초안의 단순화 표기로 판단.
- 후속 : 없음

### [D03] T02 · `brief_expected.json`의 ◇ 슬롯 — `null` 대신 `""`
- 일자 : 2026-08-03
- 유형 : 문서불일치
- 상황 : T02 카드는 `brief_expected.json`을 "■ 필드만, ◇ 슬롯은 null"로 만들라고 지시한다. 그러나 `contracts/models.py`의 `BriefCard.body: str`, `StrategyBrief.summary_line: str`은 기본값 없는 **비-옵셔널 `str`**이라 JSON `null`을 넣으면 `model_validate_json`이 타입 에러로 실패해 T02 완료 조건("픽스처가 Model.model_validate_json 통과")과 충돌한다. (`culture_fit: str | None = None`은 실제로 옵셔널이라 여기엔 `None`을 그대로 사용했다.)
- 조치 : `body`, `summary_line`은 빈 문자열(`""`)을 "아직 채우지 않음" 자리표시자로 사용했다. contracts는 R1로 수정 금지 대상이라 건드리지 않았다.
- 후속 : T06(`build_strategy_brief`)·T07(렌더러) 담당 세션은 ◇ 슬롯 미채움을 `""`로 판별할 것 (`None` 체크가 아님).

### [D04] T02 · pytest가 `contracts`를 못 찾는 문제 — `pythonpath` 설정 추가
- 일자 : 2026-08-03
- 유형 : 문서불일치
- 상황 : T02 카드의 검증 명령 `pytest tests/test_fixtures.py -q`를 그대로 실행하면 `ModuleNotFoundError: No module named 'contracts'`가 난다. `tests/`에 `__init__.py`가 없는 flat 레이아웃에서, pytest가 특정 테스트 파일 경로로 호출되면 `rootdir`가 아니라 `tests/` 디렉터리 자체를 `sys.path`에 넣기 때문. T00에서 결정한 flat(no-src) 레이아웃(DEVLOG D01)의 직접적 후속 결과.
- 조치 : `pyproject.toml`에 `[tool.pytest.ini_options] pythonpath = ["."]` 추가 (pytest 7+ 코어 기능). 이후 어떤 방식으로 pytest를 호출해도 저장소 루트가 `sys.path`에 들어가 `contracts`, `tools` 등 최상위 패키지를 import할 수 있다. `pyproject.toml`은 R2 소유 예외 대상이라 T02 세션에서 직접 수정.
- 후속 : 없음 (이후 태스크의 테스트도 동일하게 `contracts`/`tools`/`nodes` 등을 문제없이 import 가능)

### [D05] T03 · 판정 불가(분모 0) → `UNMET` 반환으로 결정
- 일자 : 2026-08-05
- 유형 : 선택결정
- 상황 : T03 카드 불변식이 "모든 UNKNOWN이면 UNMET이 아니라 판정 불가로 취급할지 결정하고 docstring에 명시"를 요구한다. 그러나 `MatchState`에는 MET/ADJACENT/UNMET 셋뿐이라 "판정 불가"를 표현할 값이 없고, `aggregate_states`의 반환 타입은 `MatchState`(옵셔널 아님)라 `None`도 못 쓴다. 필수 기준이 전부 UNKNOWN인 경우 외에, **필수 기준이 하나도 없는 경우**(전부 우대)와 **대응 판정이 전혀 없는 경우**도 같은 분모 0 상황이다.
- 조치 : 세 경우 모두 `MatchState.UNMET`을 반환하되, 의미를 "미보유를 확인했다"가 아니라 **"충족을 확인하지 못했다"**로 docstring에 명시했다. 근거 부재를 등급 라벨로 굳히지 않는 것은 설계도 §11-2 ②("근거 부재 → 라벨링 아님, 카드 미생성")의 몫이므로, 호출부가 반환값만으로 카드를 만들지 말고 판정된 기준의 존재 여부를 별도로 확인해야 한다는 단서를 docstring에 함께 적었다. R1에 따라 `MatchState`에 값을 추가하지 않았다(계약 변경 없이 해결 가능한 사안으로 판단).
- 후속 : T06(`build_strategy_brief`) 세션이 §11-2 ② 카드 미생성 판단을 담당한다. → D06

### [D06] T03 · 판정 불가 구분 헬퍼는 만들지 않음
- 일자 : 2026-08-05
- 유형 : 범위밖
- 상황 : D05의 결과로 "필수 기준 중 판정된 것이 하나라도 있는가"를 호출부가 매번 다시 계산해야 한다. `tools/aggregate.py`에 `is_decidable(criteria, verdicts) -> bool` 같은 헬퍼를 두면 T06이 §11-2 ② 카드 미생성 판정을 일관되게 할 수 있다.
- 조치 : T03 카드의 완료 조건·검증 범위 밖이고 계약(`contracts/tools.py`)에도 없는 함수라 **구현하지 않았다**(R7). 집계 규칙 자체는 docstring에 전부 노출되어 있어 T06이 직접 판단해도 정보 손실은 없다.
- 후속 : T06 착수 시 필요하다고 판단되면 그때 추가할 것. 추가 시 `tools/aggregate.py`(T03 소유) 수정이 되므로 소유권 예외를 DEVLOG에 남길 것.

### [D07] T04 · 의존성 `openai`·`python-dotenv` 추가 + 기본 모델 선정
- 일자 : 2026-08-05
- 유형 : 선택결정
- 상황 : 첫 LLM 사용 지점이라 SDK가 필요하고, `.env.example`에 `OPENAI_API_KEY`가 있는데 이를 실제로 읽어들이는 코드가 없었다. 또 structured output API는 SDK 버전마다 위치가 달라(`beta.chat.completions.parse` → `chat.completions.parse` → `responses.parse`) 무엇을 쓸지 정해야 했다.
- 조치 : `uv add openai python-dotenv` → **openai 2.53.0**, **python-dotenv 1.2.2** (전이 의존성 10종 포함, `uv.lock` 갱신). 설치본을 직접 조회해 `client.responses.parse(text_format=...)`가 있음을 확인하고 이를 채택했다(추측 대신 확인 — AGENTS.md 버전 주의 항목과 같은 취지). 기본 모델은 **`gpt-4.1-mini`** — structured output을 지원하면서 P0 반복 실행에 부담이 적다. `llm/client.py`의 `DEFAULT_MODEL` 상수이며 `JOBPREP_LLM_MODEL` 환경 변수로 덮어쓸 수 있다. `.env`는 `load_dotenv(override=False)`로 best-effort 로딩하되, `OpenAI()` 생성은 호출 시점까지 미뤄 **키 없이도 모듈 import·오프라인 테스트가 가능**하게 했다.
- 후속 : `.env.example`은 T00 소유라 건드리지 않았다. `JOBPREP_LLM_MODEL` 항목 추가가 필요하면 별도 판단. 모델 품질이 부족하면 상수만 교체.

### [D08] T04 · `pytest` 기본 실행에서 `llm` 마커 제외
- 일자 : 2026-08-05
- 유형 : 선택결정
- 상황 : T04가 저장소 최초의 `@pytest.mark.llm` 테스트를 만들었다. pytest는 마커가 붙었다고 기본 실행에서 빼주지 않으므로, `.env`에 키가 있는 환경에서 `uv run pytest -q`를 돌리면 **의도치 않게 유료 호출이 발생**한다. DEVPLAN §4는 "`uv run pytest -q`는 LLM 없이 통과해야 한다 / 비용 드는 테스트는 `-m llm`으로만"을 정책으로 명시하고 있으나 이를 강제하는 설정이 없었다.
- 조치 : `pyproject.toml`에 `addopts = "-m 'not llm'"` 추가 (D04와 같은 예외 조항 적용 — pyproject는 소유 경계 밖). CLI에서 `-m llm`을 주면 뒤에 오는 값이 이겨 온라인 테스트만 돈다. 세 조합(`pytest -q` / `pytest tests/test_extract.py -q` / `-m llm`) 모두 실제 실행해 확인했다.
- 후속 : 없음 (T05 이후 LLM 도구 테스트도 마커만 붙이면 동일하게 격리된다)

### [D09] T04 · 온라인 검증(`-m llm`) 미실행 — API 키 부재
- 일자 : 2026-08-05
- 유형 : 부채완료
- 상황 : T04 검증 명령은 오프라인/온라인 2개이며, 완료 조건 "픽스처 JD로 실행 시 요구역량 10개 이상 추출"은 실제 호출이 있어야 확인된다. 그러나 저장소에 `.env`가 없고 환경 변수 `OPENAI_API_KEY`도 비어 있어 `uv run pytest -q -m llm`이 **skip**으로 끝난다(1 skipped). 통과가 아니므로 R4에 따라 ⚠️로 표기한다.
- 조치 : 오프라인 검증(`uv run pytest tests/test_extract.py -q` → 19 passed)은 전부 통과. 온라인 테스트는 키가 없으면 조용히 통과하지 않고 **skip**되도록 `skipif`를 걸어, 검증되지 않은 것이 검증된 것처럼 보이지 않게 했다. 대조 로직·스키마 변환·배치 1콜·재시도는 골든 픽스처로 오프라인 검증이 끝난 상태라, 남은 미검증 영역은 **실제 모델이 이 프롬프트로 10개 이상을 원문 보존해 뽑아내는가** 하나다.
- 후속 : 저장소 루트에 `.env`(`OPENAI_API_KEY=...`)를 두고 `uv run pytest -q -m llm` 1회 실행 → 통과 시 원장 T04를 ⚠️ → ✅로 바꾸고 이 항목에 확인 일자를 남길 것. 실패 시(추출 10개 미만·원문 대조 실패) 프롬프트 규칙 문구 또는 `DEFAULT_MODEL`을 조정한다. **T08(그래프 조립) 착수 전에 해소하는 것이 좋다** — 그 시점엔 어차피 키가 필요하다.

### [D10] T05 · 근거 없는 판정의 처리 — MET/PARTIAL은 질문 승격, UNMET은 판정 유지
- 일자 : 2026-08-05
- 유형 : 선택결정
- 상황 : T05 카드는 "근거 없으면 UNKNOWN", 계약(`contracts/tools.py`)은 "근거 없이는 MET/PARTIAL/UNMET 판정을 내리지 않고 UNKNOWN으로 두거나 Question으로 승격"이라고 적혀 있다. 둘 다 UNMET을 근거 필수로 읽을 여지가 있으나, 골든 픽스처 `verdicts_half.json`·`verdicts_mostly_unmet.json`은 **UNMET 항목의 `evidence`를 빈 배열로 두고 있다**. "프로필에서 근거를 찾지 못했다"는 서술이 곧 근거이므로 UNMET에 인용을 요구하면 픽스처와 모순되고, 실제로는 어떤 미충족도 판정할 수 없게 된다.
- 조치 : 상태별로 갈랐다. **MET·PARTIAL**은 인용이 원문 대조를 통과해야만 판정으로 인정하고, 실패 시 Question으로 승격한다(근거 없는 충족 주장 금지). **UNMET**은 근거가 없어도 판정으로 남긴다(부재가 근거). **UNKNOWN**은 "판단보류 = 물어봐야 한다"는 뜻이므로 항상 Question으로 승격했다 — 즉 `verify_criteria`는 UNKNOWN 판정을 반환하지 않는다. `rationale`이 빈 문자열인 판정도 Question으로 승격한다(카드: rationale 필수). 근거 인정 상태는 `EVIDENCE_OPTIONAL_STATES` 상수로 분리해 나중에 바꾸기 쉽게 뒀다.
- 후속 : `aggregate_states`(T03)는 UNKNOWN을 분모에서 제외하도록 이미 구현돼 있어, verify가 UNKNOWN을 내지 않아도 규칙은 그대로 성립한다. 인터뷰 2라운드(§8-4) 후에도 남은 Question의 기준은 판정 자체가 없으므로 T03 문서대로 분모에서 빠진다.

### [D11] T05 · `decompose_criteria`에서 코드가 채우는 필드 확정
- 일자 : 2026-08-05
- 유형 : 선택결정
- 상황 : `Criterion`은 `criterion_id`·`comp_id`·`text`·`is_required` 넷인데, 이 중 무엇을 모델에게 맡길지 카드에 명시가 없다. 특히 `is_required`는 이미 `CompetencyRecord.importance`에 있는 정보다.
- 조치 : 설계도 §7-1("코드가 채울 수 있는 것은 코드가 채운다")에 따라 모델은 **기준 문장(text)과 comp_id만** 산출하게 하고, `criterion_id`는 `cr-{comp_id}-{순번:02d}`로 결정론 생성, `is_required`는 `importance is 필수`에서 파생했다. 부수 규칙 : 같은 역량 안의 중복 문장 제거, `MAX_CRITERIA=5` 초과분은 잘라냄, 입력에 없는 comp_id는 폐기. **입력 역량은 모델이 빠뜨려도 전부 키로 남기고 빈 리스트를 넣는다** — 조용히 사라지는 대신 "분해 실패"가 호출부에 드러나야 하기 때문(§11-2 ②의 카드 미생성 판단 재료).
- 후속 : 없음

### [D12] T05 · `question_id` ↔ `answers` 키 규약을 코드가 정함
- 일자 : 2026-08-05
- 유형 : 계약문제
- 상황 : `verify_criteria(criteria, profile, answers)`의 `answers`는 계약상 `dict[str, str]`(question_id → 답변)이고 `Question.question_id`는 자유 문자열이다. 그런데 **어떤 답변이 어떤 기준의 것인지** 되짚는 규칙이 계약에 없다. 이게 없으면 T11(델타 인터뷰)·T12(재개 루프)가 임의 규약을 각자 만들게 되어 조용히 어긋난다.
- 조치 : 계약 수정 없이(R1) `tools/verify.py`가 `question_id = f"q-{criterion_id}"` 규약을 정하고, 역변환을 `question_id_for()` / `criterion_id_from_question()` 두 함수로 **공개**했다. T11·T12는 문자열을 직접 조립하지 말고 이 함수를 쓰면 된다. 한계 : 같은 기준이 2라운드에 다시 질문되면 id가 동일하다 — 라운드 구분이 필요해지면 그때 접미사를 붙여야 하며, 그 변경은 이 두 함수 안에서 끝난다.
- 후속 : T11 착수 세션에 이 항목을 알릴 것. 라운드 구분이 실제로 필요하면 `question_id_for(criterion_id, round=)` 형태로 확장.

### [D13] T06 · ◇ 슬롯(`summary_line`·`body`·`culture_fit`)을 채우는 태스크가 계획에 없다
- 일자 : 2026-08-05
- 유형 : 문서불일치
- 상황 : `contracts/tools.py`의 `build_strategy_brief` docstring은 "◇ 필드만 LLM이 채운다"고 적고, T06 카드도 "LLM은 슬롯(◇: body, summary_line)만 채운다"고 서술한다. 그런데 **T06의 의존은 T01·T02·T03뿐이고 T04(LLM 어댑터)가 없다.** 의존에 없는 모듈을 부르는 것은 이 개발 방식의 전제를 깨는 것이고, 카드의 완료 조건(공백 케이스 3종 + 결정론)과 검증 명령에도 LLM 요소가 전혀 없다. `tasks/`를 전부 훑어봐도 `summary_line`·`body`를 **채우는 카드는 존재하지 않는다**(T07은 렌더러, T08은 그래프 조립, T09는 UI).
- 조치 : T06은 ■만 만들고 ◇는 빈 값(`body=""`, `summary_line=""`, `culture_fit=None`)으로 남겼다 — 빈 문자열 = 아직 채우지 않음(D03 규약). 이 상태로도 P0 완료 판정("붙여넣기 한 번으로 .md가 떨어진다")은 성립하지만, **떨어지는 .md에는 서술형 문장이 비어 있다.** 카드 범위 밖이라 임의로 LLM 호출을 넣지 않았다(R7).
- 후속 : 사용자 판단 필요 — ① 별도 태스크(예: T06b `fill_brief_slots`)를 신설하거나 ② T08 그래프에 슬롯 채우기 노드를 추가하거나 ③ P0에서는 빈 슬롯을 그대로 두고 P2 이후로 미룬다. **P0 데모 품질에 직결되므로 T09 전에는 결정할 것.**

### [D14] T06 · 요구 레벨을 상회하지 않는 MET의 행선지 — 트랙1 포함
- 일자 : 2026-08-05
- 유형 : 선택결정
- 상황 : T06 카드의 트리아지 규칙은 "트랙1 = MET **이면서** my_level > required_level"이다. 규칙을 문자 그대로 적용하면 **요구 수준을 정확히 충족한 MET 항목(my == required)이 어느 트랙에도 속하지 못한다.** 트랙2("채울 것")는 이미 충족한 항목에 부적절하고, 트랙3("포기할 것")은 더더욱 아니다. 카드에서 누락하면 `summary_counts`(카드에서 집계)에서도 사라져 충족 항목 수가 실제보다 적게 나온다.
- 조치 : **트랙1 = MET 전부**로 두고, 상회 항목이 앞에 오도록 정렬했다(`exceeds_requirement` → 정렬 키). 설계도 §11-3의 트랙1 정의가 "지금 내세울 것 — 강점을 이력서·자소서 문구로 번역"이므로, 요구를 정확히 충족한 항목도 이력서에 쓸 재료라는 판단이다. 카드 규칙의 `my_level > required_level`은 **트랙1 소속 조건이 아니라 강점 여부(정렬·강조)** 로 해석했다.
- 후속 : §11-6의 "트랙1 공백" 규칙은 상회 항목이 아니라 **MET이 하나도 없을 때** 발동하게 된다. 상회는 없지만 동일 레벨 충족은 있는 경우 고정 문구가 뜨지 않는데, 이 편이 사용자에게 정직하다고 판단했다(내세울 것이 실제로 있으므로).

### [D15] T06 · 소요 구간 경계값과 인접 보정 — 잠정값 상수화
- 일자 : 2026-08-05
- 유형 : 선택결정
- 상황 : 트랙3 규칙("소요 구간 > 남은 기간인 UNMET")을 판정하려면 구간(단기/중기/장기)을 **일수와 비교**해야 하는데, 그 수치가 어디에도 정의돼 있지 않다. 설계도 §16 S5(실패 임계값)·S6(집계 경계값)는 P2~P4 실측 후 확정 상태다. 구간 자체도 무엇에서 파생하는지 명시가 없다.
- 조치 : 구간은 **레벨 격차**(요구 - 보유, 보유 없음은 -1)에서 파생하고, 인접(ADJACENT)은 격차를 1 깎았다 — "Docker 경험자에게 오케스트레이션만 얹으면 된다"(§7-2)는 인접의 정의가 곧 낮은 학습비용이기 때문이다. 격차 ≤1 단기 / =2 중기 / ≥3 장기. 비교용 최소 일수는 `BAND_MIN_DAYS = {단기 14, 중기 30, 장기 90}`으로 두고 **모듈 상수로 분리**했다(T03 `ADJACENT_THRESHOLD`와 같은 취급). 요구 레벨 미표기는 `실무운영`으로 가정(`DEFAULT_REQUIRED_INDEX`). 설계도 §11-6의 "절대 시간이 아니라 상대 순위 + 구간" 원칙을 지키기 위해, 이 일수는 **트랙3 배정 판단에만 쓰고 출력에는 넣지 않는다** — 브리프에는 순위(priority)만 나간다. `days_remaining` 계산은 `days_remaining_from()` / `build_brief_meta()`로 이 모듈에 뒀다(카드 불변식: target_date 기준 코드 계산). 지난 날짜는 음수 대신 0.
- 후속 : P2~P4 실측 시 `BAND_MIN_DAYS`와 격차 경계를 튜닝. 출처 있는 숫자(표준 커리큘럼 시수 등)를 인용하라는 §11-6 요구는 ◇ 슬롯을 채우는 단계(D13)에서 지켜야 한다.

### [D16] T06 · "evidence 없는 항목 카드 미생성"의 적용 범위
- 일자 : 2026-08-05
- 유형 : 문서불일치
- 상황 : T06 카드 결측 처리 표는 "evidence 없는 항목 → 카드 미생성"이라고 적혀 있다. 그런데 골든 픽스처 `brief_expected.json`의 **트랙3 카드(req-be-07)는 `evidence`가 빈 배열인데도 카드로 존재한다.** 문자 그대로 적용하면 픽스처와 모순되고, D10에서 확정한 "UNMET은 부재 자체가 근거"와도 충돌해 트랙3이 항상 비게 된다.
- 조치 : 설계도 §11-2 ②("개별 역량의 **근거** 부재 → 라벨링 아님, 카드 미생성")의 취지를 살려 두 조건으로 좁혔다. ① **판정된 기준이 하나도 없는 항목**(= 판단 불가. T03이 분모 0에서 UNMET을 돌려주는 경우 — D05가 경고한 바로 그 지점) ② **MET·ADJACENT인데 인용이 하나도 없는 항목**(근거 없는 충족 주장). UNMET은 인용이 없어도 카드로 만든다. 드롭한 항목은 사라지지 않고 사유와 함께 `gaps`(공백 고지)에 기재된다.
- 후속 : 없음 — D05가 남긴 "호출부가 판정 유무를 따로 확인해야 한다"는 숙제를 `is_card_worthy()`가 여기서 처리한다. D06에서 미룬 헬퍼는 결국 필요 없었다.

### [D17] T07 · 파일명의 날짜는 `target_date` (생성일 아님)
- 일자 : 2026-08-05
- 유형 : 선택결정
- 상황 : 카드와 설계도 §11-5가 지정한 파일명은 `{회사}_{직무}_전략브리프_{YYYYMMDD}.md`인데, **어느 날짜인지** 명시가 없다. 흔한 해석은 생성일이지만 `filename_for(brief)`의 입력은 브리프 하나뿐이고 `StrategyBrief`·`BriefMeta`에는 생성 시각 필드가 없다. `date.today()`를 쓰면 같은 브리프를 내일 내려받을 때 파일명이 달라져 "같은 입력 → 같은 출력"이 깨진다.
- 조치 : **`meta.target_date`(목표 시점)** 를 썼다. 브리프에서 결정론적으로 도출되는 유일한 날짜이고, 사용자에게도 "이 브리프가 겨냥하는 시점"이 파일명에 남는 편이 유용하다. 아울러 회사·직무에 경로로 쓸 수 없는 문자(`\ / : * ? " < > |`)와 공백이 섞일 수 있어 `_`로 치환했다 — **Windows 환경이라 이 처리가 없으면 다운로드가 실패**한다. 빈 문자열은 `미상`으로 대체.
- 후속 : 생성일이 파일명에 필요하다는 판단이 서면 `BriefMeta`에 `built_at` 추가가 필요하고, 이는 계약 변경(R1)이므로 T01 재개 판단 사항이다.

### [D18] T07 · 빈 ◇ 슬롯의 렌더 표기
- 일자 : 2026-08-05
- 유형 : 선택결정
- 상황 : D13으로 `summary_line`·`body`가 빈 문자열인 채 렌더러에 들어온다. 빈 문자열을 그대로 출력하면 카드마다 이유 없는 빈 줄이 생겨 사용자가 "렌더링이 깨졌나"로 읽는다. 반대로 렌더러가 문장을 지어내면 §12의 "그럴듯하게 지어내지 않는다" 원칙을 정면으로 위반한다.
- 조치 : 고정 문구 `_(아직 작성되지 않음)_`(상수 `NOT_FILLED`)로 표기했다. 컬처핏 부재는 카드 지시대로 `미수집`, 근거 없는 카드는 `근거: 프로필에서 관련 서술을 찾지 못함`으로 각각 **다른 문구**를 쓴다 — "아직 안 채움"과 "수집 못 함"과 "찾지 못함"은 서로 다른 상태이며 같은 말로 뭉뚱그리면 사용자가 원인을 구분할 수 없다.
- 후속 : D13이 해소되면 이 문구는 자연히 사라진다. 지금은 픽스처 브리프 렌더 시 4곳에 나타나며, **P0 산출물의 미완성 지점이 눈에 보이게 남는다**(감추는 것보다 낫다는 판단).

### [D19] D13 해소 · 태스크 `T06b` 신설 + `tasks/T08.md` 수정
- 일자 : 2026-08-05
- 유형 : 선택결정
- 상황 : D13(◇ 슬롯을 채우는 태스크가 계획에 없음)에 대해 사용자가 **별도 태스크 신설**을 선택했다. 남은 문제는 ① 번호를 어떻게 붙이는가 ② 이미 작성된 `tasks/T08.md`가 새 노드를 모른다는 점이었다.
- 조치 : ① **`tasks/T06b.md`** 로 신설했다. T09~T29를 밀어 재번호하면 이미 쓰인 카드 22장의 상호 참조와 원장 전체가 흔들리므로, 파생 번호가 비용이 훨씬 싸다. 위치는 T06(골격 생성) 바로 뒤가 의미상 맞다. ② **`tasks/T08.md`를 최소 수정**했다 — 의존에 `T06b` 추가, 그래프 다이어그램을 `build_brief → fill_slots → END`로 갱신. 카드 본문 수정은 원래 그 태스크 세션의 몫이지만, **T08 세션은 자기 카드만 읽으므로 여기서 반영하지 않으면 새 노드가 조용히 누락된다.** 이 정도 누락이 R2 준수보다 위험하다고 판단했고, 수정 범위는 두 줄로 한정했다.
- 카드 설계의 요점 : T06b의 완료 조건을 "슬롯이 채워짐"이 아니라 **"■ 필드가 한 글자도 바뀌지 않음을 코드로 대조"** 로 잡았다. §11-1이 주장하는 멱등성은 "LLM이 구조를 못 건드린다"는 것이므로, 그 주장을 실제로 검사하는 테스트가 이 카드의 진짜 산출물이다. 또 `LLMError` 시 예외를 던지지 않고 **입력 브리프를 그대로 반환**하도록 못박았다(§12 "부분 실패를 전체 실패로 만들지 않는다") — 슬롯이 비면 렌더러가 D18 문구로 표기하므로 사용자는 브리프를 잃지 않는다.
- 후속 : T06b 완료 후 T08 진행. T06b는 실제 LLM 호출이 있으므로 D09와 마찬가지로 온라인 검증에 `OPENAI_API_KEY`가 필요하다.

### [D20] T06b · 실패 삼키는 범위 — `LLMError`만, 그 밖의 예외는 전파
- 일자 : 2026-08-05
- 유형 : 선택결정
- 상황 : 카드는 "`LLMError` 발생 시 입력 브리프를 그대로 반환"을 요구한다(§12). 그런데 `LLMConfigError`(API 키 부재)도 `LLMError` 하위라, 규칙을 그대로 적용하면 **키가 없을 때 에러 없이 빈 슬롯 브리프가 조용히 나간다.** 이것이 의도인지, 아니면 설정 오류는 시끄럽게 터져야 하는지 판단이 필요했다.
- 조치 : `LLMError` 전체를 삼키기로 했다. 근거는 두 가지다. ① §12의 원칙은 "부분 실패를 전체 실패로 만들지 않는다"이고, 슬롯이 비어도 ■ 브리프는 완전한 산출물이다. ② 침묵하지 않는다 — 렌더러가 빈 슬롯을 `_(아직 작성되지 않음)_`으로 **명시 출력**하므로(D18) 사용자는 무엇이 비었는지 즉시 본다. 감춰지는 실패가 아니다. 반면 `LLMError` **밖의** 예외(구현 버그 등)는 삼키지 않고 그대로 올린다 — 그건 데이터 문제가 아니라 코드 문제이고, 조용히 넘기면 원인을 잃는다. 테스트로 양쪽을 모두 고정했다.
- 부수 결정 : 카드가 한 장도 없는 브리프는 LLM을 부르지 않고 즉시 반환한다(설명할 대상이 없는데 총평만 지어낼 이유가 없다).
- 후속 : **T09(Streamlit)는 슬롯이 비어 있으면 그 사실을 화면에도 드러낼 것.** .md에는 D18 문구로 남지만, 화면에서 빈 카드가 그냥 허전해 보이면 사용자가 원인을 모른다.

### [D21] 인프라 · `tests/conftest.py` 신설 — `.env`를 pytest 수집 이전에 로드
- 일자 : 2026-08-05
- 유형 : 소유권침범
- 상황 : `.env`에 `OPENAI_API_KEY`를 넣었는데도 `uv run pytest -q -m llm`이 계속 3건 모두 skip됐다. 원인은 키가 아니라 **평가 시점**이었다. 각 테스트의 `@pytest.mark.skipif(not os.environ.get("OPENAI_API_KEY"), ...)`는 pytest **수집(collection) 시점**에 `os.environ`을 직접 본다. 반면 `.env` 로딩은 `llm/client.py`의 `_ensure_env()`가 **LLM 호출 시점**에야 지연 수행한다. 저장소에 `conftest.py`가 없어 수집 이전에 `.env`를 환경 변수로 올리는 주체가 아무도 없었고, 그래서 마커 평가 순간엔 언제나 키가 없는 것으로 보였다. D09가 "키 부재로 skip"이라 기록한 상태의 실체가 이것이다 — **키를 넣어도 저절로 풀리지 않는다.**
- 조치 : `tests/conftest.py`를 새로 만들어 최상단에서 `load_dotenv()`를 호출했다. pytest는 conftest.py를 테스트 수집보다 먼저 import하므로 skipif 평가 시점보다 확실히 앞선다. **대안이었던 `pytest-dotenv` 플러그인은 채택하지 않았다** — 월 250만 다운로드로 가장 대중적이지만 최근 12개월간 신규 릴리스가 없어 사실상 유지보수가 끊겼고(그 대체로 `pytest-dotenv-modern` 포크가 나온 상황), `python-dotenv`는 이미 `llm/client.py`가 쓰는 기존 의존성이라 3줄 자체 코드로 끝나는 일에 끊긴 플러그인을 새로 물릴 이유가 없었다. `load_dotenv()`는 기본이 `override=False`라 셸에 이미 있는 환경 변수를 덮어쓰지 않는다 — CI에서 시크릿을 주입하는 방식과 충돌하지 않는다.
- 소유권 : `tests/conftest.py`는 어느 카드의 소유 파일도 아니다(R2). 특정 태스크의 산출물이 아니라 **테스트 하네스 전체에 걸리는 인프라**이고, 이게 없으면 T04·T05·T06b 세 카드의 온라인 검증이 영구히 skip으로 남아 R4("검증 명령 통과가 완료 조건")를 어느 카드도 만족시킬 수 없다. 파일 내용을 `load_dotenv()` 한 줄로 한정해 다른 카드의 테스트 거동에 영향이 없도록 했다.
- 후속 : 없음. 이 조치로 D09가 해소됐다.

### [D22] T04 · 모델 후보 재검토 — `gpt-5-mini` 시도 후 `gpt-4.1-mini` 유지
- 일자 : 2026-08-05
- 유형 : 선택결정
- 상황 : API 사용료 부담을 줄이려고 더 싼 모델을 찾다가 `DEFAULT_MODEL`을 `gpt-4.1-mini`($0.40/$1.60) → `gpt-5-mini`($0.25/$2.00)로 바꿔 온라인 테스트를 돌렸더니 3건 모두 `400 Bad Request: Unsupported parameter: 'temperature' is not supported with this model`으로 실패했다. `complete_structured`가 항상 `temperature=0.0`을 넘기는데, gpt-5 계열은 reasoning 모델이라 이 파라미터 자체를 거부한다.
- 판단 1 (결정론) : 먼저 "temperature=0을 포기해도 되는가"를 따졌고, **포기해도 된다**는 결론이 나왔다. 설계도 §11-1이 "멱등성의 정확한 범위 — v2 정정"에서 v1의 "같은 입력이면 같은 결과"를 과한 주장이라고 스스로 정정하며 **LLM 슬롯 자연어(◇)의 멱등성을 ❌로 명시**하고, 그 이유를 정확히 "temperature=0·seed로도 완전 결정론 아님"이라고 적어뒀다. 저장소의 결정론 테스트 5건(`test_brief.py:164`·`test_fill_slots.py:173`·`test_markdown.py:233`·`test_verify.py:380`·`test_extract.py:148`)도 전부 LLM을 타지 않는 코드 경로만 검사한다. 즉 `temperature=0`은 멱등성 주장의 기둥이 아니라 분산 감소 장치일 뿐이므로, **결정론은 모델 선택의 판단 기준이 될 수 없다.**
- 판단 2 (비용, 결정적) : 그런데 애초의 판단 기준인 **비용에서 gpt-5-mini가 오히려 불리하다**. reasoning 모델은 API에 노출되지 않는 reasoning 토큰을 생성하고 **이것이 output 토큰으로 과금**된다. 출력 단가가 이미 $1.60 → $2.00으로 25% 비싼 데다 보이지 않는 토큰이 그 위에 얹힌다. 이 프로젝트의 LLM 워크로드는 고정 스키마 추출·분류(extract/decompose/verify/fill_slots)라 reasoning 이득이 크지 않고, 근거 인용의 원문 대조는 이미 코드가 검증한다.
- 조치 : `gpt-4.1-mini`로 되돌렸다. 코드·테스트 변경 0건이고 `test_extract.py:235`의 `assert kw["temperature"] == 0.0`도 그대로 성립하므로 T04 계약을 건드리지 않는다. 되돌린 뒤 `uv run pytest -q -m llm` 3건 전부 통과(37.59s).
- 후속 : reasoning 모델을 쓸 이유가 생기면 어댑터에 모델별 파라미터 분기가 필요하고, 그건 T04 재개 사안이다. 지금은 필요 없다.

### [D23] T08 · `langgraph` 의존성 추가 — `pyproject.toml` 수정
- 일자 : 2026-08-05
- 유형 : 소유권침범
- 상황 : T08은 LangGraph로 그래프를 조립하는 카드인데 `langgraph`가 의존성에 없었다(`ModuleNotFoundError`). `pyproject.toml`은 T00 소유 파일이라 R2에 걸린다.
- 조치 : `uv add langgraph`로 추가했다(langgraph 1.2.10, langchain-core 1.5.3 등 전이 의존 20건 동반). D07에서 T04가 `openai`·`python-dotenv`를 같은 방식으로 추가한 선례를 따랐다 — **의존성은 그것을 처음 필요로 하는 카드가 추가한다**가 이 저장소의 실질 규약이고, T00 시점에 이후 카드가 쓸 패키지를 전부 예견해 넣어두는 편이 오히려 부정확하다. 변경은 `[project].dependencies` 한 줄 + `uv.lock`으로 한정했다.
- 후속 : T10(checkpointer)이 `langgraph-checkpoint-sqlite`를 별도로 요구할 수 있다. 그때도 같은 방식으로 그 카드가 추가한다.

### [D24] T08 · P0의 `my_level`은 항상 None — 그리고 판정 공백이 실측으로 드러남
- 일자 : 2026-08-05
- 유형 : 선택결정
- 상황 : `aggregate` 노드가 `MatchResult`를 조립하려면 `my_level`(보유 레벨)이 필요한데, 요구 역량과 보유 역량을 잇는 후보쌍 검색(`retrieve_candidates`, T14)이 P0에 없다. 프로필의 `level_coordinates`는 **보유 역량 id**(`pf-01`…)로 키를 잡으므로 요구 역량 id에서 곧바로 끌어올 수 없다.
- 조치 : `my_level=None`으로 두었다. 추측해서 채우면 §7-3의 "지어내지 않기"와 §7-2의 "유사도를 최종 판정에 쓰지 않는다"를 동시에 어긴다. 따라서 `is_strength`도 P0에서는 서지 않는데, **트랙 배정은 상회 여부와 무관하므로(D14) 브리프 산출에는 영향이 없다** — 강점 표시만 T14 이후로 미뤄진다. 노드 docstring과 테스트(`test_aggregate_uses_rules_only`)에 이 불변식을 고정했다.
- 실측 관찰 : 실제 API로 백엔드 JD를 관통시켜 보니 요구 역량 **14건 중 카드가 선 것은 7건**이었다. 나머지 7건은 `verify`가 근거를 못 찾아 판정 대신 질문으로 승격했고(D10), 판정이 없으니 카드가 서지 않아(§11-2 ②·D16) `gaps`로 빠졌다. **이건 결함이 아니라 P0에 HITL이 없다는 사실의 정직한 표현이다** — 델타 인터뷰(T11)가 붙으면 그 질문들이 사용자에게 가고 판정이 채워진다. 다만 P0 데모에서 브리프 절반이 공백 고지로 나간다는 뜻이므로, T09(Streamlit)는 이 공백을 "미완성"이 아니라 "추가 질문이 필요한 항목"으로 보여줘야 한다.
- 후속 : `my_level` 채우기는 T14, 공백 해소는 T11. 둘 다 이 결정을 되돌리는 게 아니라 전제를 바꾸는 작업이다.

### [D25] T09 · 카드에 없는 프로필 입력 추가 + `streamlit` 의존성
- 일자 : 2026-08-05
- 유형 : 선택결정
- 상황 : T09 카드의 화면 명세는 회사·직무·목표 시점·JD 본문 넷뿐인데, **`verify` 노드는 `state["profile"]`이 없으면 판정을 하나도 못 낸다.** 프로필 없이 실행하면 전 역량이 근거 부재로 카드 미생성(§11-2 ②)이 되어 빈 브리프가 나온다. 즉 카드 명세를 글자 그대로 구현하면 완료 조건("`.md` 다운로드 성공")은 만족하지만 산출물이 비어 데모가 성립하지 않는다.
- 조치 : `st.file_uploader`로 ProfileJSON 업로드를 넣었다. 이건 카드 밖 기능 추가가 아니라 **설계도 §10-3이 이미 정한 방식**이다 — "UC-1 산출 `ProfileJSON`을 디스크에 떨어뜨리고, UC-2가 업로드로 받는다". 카드가 P0 화면을 줄여 적으면서 빠진 것으로 판단했다. 아울러 업로드가 없을 때를 위해 `fixtures/profile_sample.json`을 쓰는 "샘플 프로필로 시연" 체크박스를 뒀고, 프로필이 아예 없으면 **결과가 대부분 공백이 된다고 화면에 먼저 경고**한다(조용히 빈 브리프를 내놓지 않는다).
- 부수 : `streamlit` 1.61.0을 의존성에 추가했다(D23과 같은 사유·같은 방식). 실행 로직은 카드 불변식대로 `run_analysis()`로 분리했고 UI를 import하지 않는다 — T12가 이 경계를 유지한 채 `Command(resume=)` 루프로 교체한다. 다운로드 버튼 클릭이 rerun을 일으켜 결과가 날아가므로 최종 상태를 `st.session_state`에 보관한다.
- 후속 : T24(`profile_graph`)가 실제 ProfileJSON 저장/로드를 붙이면 샘플 체크박스는 데모 편의 기능으로만 남는다.

### [D26] T09 · 완료 검증이 실 API로 끝나지 못함 — OpenAI 크레딧 소진
- 일자 : 2026-08-05
- 유형 : 부채완료
- 상황 : T09 완료 조건은 "`streamlit run` → JD 붙여넣고 실행 → `.md` 다운로드 성공"이고 검증 방식은 수동이다. 서버 기동(HTTP 200)과 폼 조작까지는 됐으나, [분석 실행] 시점에 `429 insufficient_quota — You have no credits remaining`으로 실패했다. **코드 결함이 아니라 계정 잔액 문제다.** 같은 파이프라인의 실 API 관통은 불과 몇 분 전 T08 온라인 스모크에서 통과했다(104초).
- 조치 : LLM 도구 4종만 픽스처 스텁으로 대체하고 **Streamlit 공식 드라이버 `AppTest`로 앱을 실제 구동**해 나머지 전 구간을 검증했다. `app/main.py`를 스크립트째 실행하고 위젯을 실제로 조작하는 방식이라 내부 함수 호출이 아니다. 결과: 초기 화면 위젯 전부 렌더 → 폼 입력 → [분석 실행] 클릭 → 요약 지표(남은 기간 90일·소스 충족률 33%·신뢰등급 정상·카드 3장) → 다운로드 버튼 렌더 → `.md` 2,968바이트 생성(7개 섹션·근거 12건 포함). 예외 0건.
- 남은 부채 : **UI에서 실 API로 한 번 관통시키는 것.** 크레딧을 충전하면 `streamlit run app/main.py` 한 번으로 끝난다. 새로 짤 코드는 없다.
- 후속 : 충전 후 실행해 `.md` 또는 스크린샷을 확보하고 원장의 T09를 ✅로 올린다.

### [D27] T10 · langgraph 1.2.10 HITL API 실측 — 국면 판별 근거는 `created_at`
- 일자 : 2026-08-05
- 유형 : 문서불일치
- 상황 : 카드가 착수 전 버전 확인을 필수로 요구했다(HITL API가 버전마다 바뀌어 왔으므로). 공식 문서 페이지는 리다이렉트만 반환해 본문을 못 읽었고, 검색으로 얻은 서술은 "`stream.interrupted` 플래그" 등 **버전이 뒤섞여 있어** 그대로 믿을 수 없었다. 그래서 설치된 1.2.10에 최소 그래프를 태워 직접 확인했다.
- 실측 결과 :
  | 국면 | `created_at` | `next` | `interrupts` |
  | --- | --- | --- | --- |
  | 미시작 | `None` | `()` | `()` |
  | 중단됨 | 설정됨 | `('ask',)` | `(Interrupt(value=…, id=…),)` |
  | 완료 | 설정됨 | `()` | `()` |
  **`next == ()`가 미시작과 완료 양쪽에서 나온다** — 이것만으로 판별하면 완료된 스레드를 미시작으로 오인해 처음부터 재실행한다. 그래서 `created_at is None`을 미시작 판별에 썼다. `invoke`는 중단 시 반환 dict에 `__interrupt__` 키를 얹지만, 재개 경로에서는 반환값이 아니라 `get_state()`를 봐야 하므로 `snapshot.interrupts`를 정본으로 삼았다.
- 재실행 위험 실증 : 완료된 스레드에 `invoke`를 다시 걸면 노드가 **처음부터 전부 다시 돌았다**(`trail`이 `[before, ask, after, before]`가 됨). 카드가 막으려는 문제가 실재함을 확인했고, 이걸 `test_completed_thread_is_never_rerun`으로 고정했다.
- 카드와의 차이 : 카드의 시그니처는 `resume_or_start(thread_id, initial_input)`이지만 실제로는 `graph`와 `resume` 값이 더 필요하다. `resume_or_start(graph, thread_id, initial_input, *, resume=None)`으로 **추가**했다(제거·변경 없음) — 그래프를 인자로 받아야 테스트에서 LLM 없는 그래프로 검증할 수 있고, 모듈 전역에 그래프를 두면 import 시점에 체크포인터가 생겨 부작용이 된다.
- 의존성 : `langgraph-checkpoint-sqlite` 3.1.1 추가(D23과 같은 사유). `SqliteSaver(conn)` 직접 생성이 가능해 컨텍스트 매니저(`from_conn_string`) 없이 긴 수명 세션에 쓸 수 있다. 커넥션은 `check_same_thread=False`로 연다 — Streamlit이 워커 스레드에서 스크립트를 재실행하므로 이게 없으면 두 번째 rerun에서 sqlite3가 예외를 던진다.
- 소유권 : `.gitignore`에 `.jobprep/` 한 줄 추가(R2). 체크포인트 DB의 기본 경로이며, 빠뜨리면 런타임 산출물이 `git status`에 계속 뜬다.
- 후속 : T11이 `interrupt()`를 실제 노드에 심고, T12가 이 계층을 Streamlit 재개 루프에 연결한다. **T12는 `resume_or_start()` 밖에서 `graph.invoke()`를 직접 부르지 말 것** — 이 계층의 유일한 규칙이다.

### [D28] T11 · `interrupt()` 앞에 부작용을 두지 않는다 + 답변 키 정규화
- 일자 : 2026-08-05
- 유형 : 선택결정
- 상황 : D27 실측에서 확인했듯 재개 시 **노드가 처음부터 다시 실행되고 `interrupt()`만 답을 반환**한다. 즉 `interrupt()` 위쪽 코드는 라운드마다 두 번씩 돈다. 여기에 카운터 증가나 상태 변경을 두면 한 번 재개할 때마다 값이 어긋난다.
- 조치 : `delta_interview()`에서 `interrupt()` 앞은 전부 **순수 계산**으로만 뒀다 — 질문 유무 확인, 라운드 상한 검사, 페이로드 생성. 라운드 증가·답변 병합·재판정은 전부 `interrupt()` **뒤**에 있고, 노드는 부분 갱신 dict를 반환할 뿐 상태를 직접 쓰지 않으므로 두 번 실행돼도 결과가 같다. 이 제약을 함수 docstring에 명시해 두었다.
- 부수 결정 1 (답변 키) : UI가 돌려주는 dict의 키가 `question_id`일지 `criterion_id`일지 T12를 아직 안 만든 시점에 확정할 수 없다. **둘 다 받아 `question_id`로 정규화**한다(`question_id_for()`로 변환, D12 규약). 리스트로 오면 질문 순서와 짝짓는다. T12가 어느 쪽으로 짜든 노드를 고칠 필요가 없다.
- 부수 결정 2 (빈 답변) : 공백만 있는 답변은 **버린다**. 빈 문자열을 재판정에 근거로 넘기면 `verify_criteria`가 근거 없는 판정을 내릴 빌미가 된다(§7-3 "지어내지 않기"). 답하지 않은 것은 답하지 않은 채로 다음 라운드나 판단보류로 간다.
- 부수 결정 3 (상한 초과) : 상한을 넘겼을 때 `interview_round`를 **더 올리지 않는다**. 라운드를 쓴 게 아니라 거부한 것이므로 카운터를 올리면 "몇 라운드를 실제로 소비했나"가 흐려진다. 대신 잔여 질문을 `UNKNOWN` 판정으로 확정해 브리프 공백 고지로 흘려보낸다 — 질문을 그냥 버리면 그 기준은 판정도 질문도 없이 사라진다.
- 후속 : T12는 폼 제출값을 `{question_id: 답변}` 또는 `{criterion_id: 답변}` 어느 쪽으로 넘겨도 된다. 페이로드의 `round`/`max_rounds`를 화면에 노출하면 사용자가 "몇 번 더 물어보나"를 안다.

### [D21] 인프라 · `tests/conftest.py` 신설 — `.env`를 pytest 수집 이전에 로드
- 일자 : 2026-08-05
- 유형 : 소유권침범
- 상황 : `.env`에 `OPENAI_API_KEY`를 넣었는데도 `uv run pytest -q -m llm`이 계속 3건 모두 skip됐다. 원인은 키가 아니라 **평가 시점**이었다. 각 테스트의 `@pytest.mark.skipif(not os.environ.get("OPENAI_API_KEY"), ...)`는 pytest **수집(collection) 시점**에 `os.environ`을 직접 본다. 반면 `.env` 로딩은 `llm/client.py`의 `_ensure_env()`가 **LLM 호출 시점**에야 지연 수행한다. 저장소에 `conftest.py`가 없어 수집 이전에 `.env`를 환경 변수로 올리는 주체가 아무도 없었고, 그래서 마커 평가 순간엔 언제나 키가 없는 것으로 보였다. D09가 "키 부재로 skip"이라 기록한 상태의 실체가 이것이다 — 키를 넣어도 저절로 풀리지 않는다.
- 조치 : `tests/conftest.py`를 새로 만들어 최상단에서 `load_dotenv()`를 호출했다. pytest는 conftest.py를 테스트 수집보다 먼저 import하므로 skipif 평가 시점보다 확실히 앞선다. **대안이었던 `pytest-dotenv` 플러그인은 채택하지 않았다** — 월 250만 다운로드로 가장 대중적이지만 최근 12개월간 신규 릴리스가 없어 사실상 유지보수가 끊겼고(그 대체로 `pytest-dotenv-modern` 포크가 나온 상황), `python-dotenv`는 이미 `llm/client.py`가 쓰는 기존 의존성이라 3줄 자체 코드로 끝나는 일에 끊긴 플러그인을 새로 물릴 이유가 없었다. `load_dotenv()`는 기본이 `override=False`라 셸에 이미 있는 환경 변수를 덮어쓰지 않는다 — CI에서 시크릿을 주입하는 방식과 충돌하지 않는다.
- 소유권 : `tests/conftest.py`는 어느 카드의 소유 파일도 아니다(R2). 특정 태스크의 산출물이 아니라 **테스트 하네스 전체에 걸리는 인프라**이고, 이게 없으면 T04·T05·T06b 세 카드의 온라인 검증이 영구히 skip으로 남아 R4("검증 명령 통과가 완료 조건")를 어느 카드도 만족시킬 수 없다. 파일 내용을 `load_dotenv()` 한 줄로 한정해 다른 카드의 테스트 거동에 영향이 없도록 했다.
- 후속 : 없음. 이 조치로 D09가 해소됐다.

### [D22] T04 · 모델 후보 재검토 — `gpt-5-mini` 시도 후 `gpt-4.1-mini` 유지
- 일자 : 2026-08-05
- 유형 : 선택결정
- 상황 : API 사용료 부담을 줄이려고 더 싼 모델을 찾다가 `DEFAULT_MODEL`을 `gpt-4.1-mini`($0.40/$1.60) → `gpt-5-mini`($0.25/$2.00)로 바꿔 온라인 테스트를 돌렸더니 3건 모두 `400 Bad Request: Unsupported parameter: 'temperature' is not supported with this model`으로 실패했다. `complete_structured`가 항상 `temperature=0.0`을 넘기는데, gpt-5 계열은 reasoning 모델이라 이 파라미터 자체를 거부한다.
- 판단 1 (결정론) : 먼저 "temperature=0을 포기해도 되는가"를 따졌고, **포기해도 된다**는 결론이 나왔다. 설계도 §11-1이 "멱등성의 정확한 범위 — v2 정정"에서 v1의 "같은 입력이면 같은 결과"를 과한 주장이라고 스스로 정정하며 **LLM 슬롯 자연어(◇)의 멱등성을 ❌로 명시**하고, 그 이유를 정확히 "temperature=0·seed로도 완전 결정론 아님"이라고 적어뒀다. 저장소의 결정론 테스트 5건(`test_brief.py:164`·`test_fill_slots.py:173`·`test_markdown.py:233`·`test_verify.py:380`·`test_extract.py:148`)도 전부 LLM을 타지 않는 코드 경로만 검사한다. 즉 `temperature=0`은 멱등성 주장의 기둥이 아니라 분산 감소 장치일 뿐이므로, 결정론은 모델 선택의 판단 기준이 될 수 없다.
- 판단 2 (비용, 결정적) : 그런데 애초의 판단 기준인 **비용에서 gpt-5-mini가 오히려 불리하다**. reasoning 모델은 API에 노출되지 않는 reasoning 토큰을 생성하고 **이것이 output 토큰으로 과금**된다. 출력 단가가 이미 $1.60 → $2.00으로 25
### [D29] T12 · 그래프 배선을 T08 소유 파일에서 확장 (R2 예외, 사용자 승인)
- 일자 : 2026-08-06
- 유형 : 소유권침범
- 상황 : T12 카드의 소유 파일은 `app/main.py`·`app/hitl.py` 둘뿐인데, **`delta_interview`(T11)가 아직 어느 그래프에도 배선돼 있지 않았다.** 재개 루프를 만들어도 멈출 지점이 없으면 완료 조건("중단 지점에서 재개")을 증명할 방법이 없다. DEVPLAN §5가 이 충돌을 "편집 전 판단 필요"로 미리 표시해 뒀다.
- 선택지 : ① `graphs/analysis_graph.py`(T08 소유)를 확장한다 — R2 위반 1건. ② `NODE_SEQUENCE`만 읽어 `app/hitl.py`에서 그래프를 조립한다 — R2는 지키지만 그래프 조립이 `app/` 계층으로 새고, 이후 HITL 노드(T19 H1·T22 H4)도 `app/`을 고쳐야 한다.
- 조치 : **사용자 판단으로 ①을 택했다.** 그래프 조립은 `graphs/` 계층에 남는 게 맞고, R2는 세션 간 충돌 방지가 목적인데 T08은 이미 완료돼 동시 편집 위험이 없다. 시그니처는 `build_analysis_graph(checkpointer=None, *, interactive=False)`로 **인자만 추가**했다(제거·변경 없음) — 기본값이 P0 동작이라 T08의 스모크 10건이 한 줄도 안 고치고 그대로 통과한다. `checkpointer`는 만들지 않고 **받는다**(D27과 같은 이유 — import 부작용 방지, 테스트가 인메모리로 대체 가능).
- 부수 : `interactive`를 플래그로 둔 이유는 중단점과 checkpointer가 한 몸이기 때문이다. 중단점이 들어가면 checkpointer 없이는 못 돌고, checkpointer가 붙으면 `thread_id` 없는 `invoke`가 막힌다. 배선을 선택 가능하게 두지 않으면 P0 경로(테스트·비대화형 실행)가 죽는다.
- 후속 : T13(진행 표시)은 `NODE_SEQUENCE`가 아니라 **`node_sequence(interactive=…)`**를 봐야 화면과 실제 배선이 어긋나지 않는다.

### [D30] T12 · 인터뷰 자기 루프는 조건부 간선으로, 라운드 상한은 노드에 위임
- 일자 : 2026-08-06
- 유형 : 선택결정
- 상황 : `verify → delta_interview → aggregate` 직선으로만 배선하면 노드에 한 번밖에 못 들어간다. 그러면 T11이 만든 `MAX_ROUNDS=2`와 상한 초과 처리 코드가 **영원히 실행되지 않는 죽은 코드**가 되고, 1라운드에 못 푼 기준은 `pending_questions`에 남은 채 집계로 흘러가 판정도 질문도 없이 사라진다.
- 조치 : `delta_interview`에 조건부 간선으로 자기 루프를 걸었다 — `pending_questions`가 남아 있으면 자기 자신으로, 비어 있으면 `aggregate`로.
- 핵심 판단 (상한을 어디서 세나) : 라우터에서 `round >= MAX_ROUNDS`를 검사해 끊고 싶은 유혹이 있지만 **그러면 안 된다.** ① 상한이 노드와 라우터 두 군데에 생겨 한쪽만 고치면 조용히 어긋난다. ② 라우터가 먼저 끊으면 잔여 질문이 `UNKNOWN`으로 확정되지 못한 채 사라져 D28 부수결정 3(잔여를 판단보류로 확정)이 무효가 된다. 그래서 라우터는 `pending_questions` 유무만 보고, 상한 초과 시 노드가 스스로 `UNKNOWN` 확정 + `pending_questions=[]` 반환으로 루프를 끊게 뒀다. 노드 진입은 최대 3회(1라운드·2라운드·상한 확정)로 유계다.
- 검증 : `test_round_limit_ends_the_loop` — 무슨 답을 줘도 판정이 안 서는 스텁으로 돌려 중단이 정확히 2회만 발생하고, 못 푼 기준 전부가 `ROUND_LIMIT_RATIONALE`을 단 `UNKNOWN`으로 남는 것을 고정했다.

### [D31] T12 · 완료 조건을 수동 대신 자동 테스트로 증명 + `tests/test_hitl.py` 신설
- 일자 : 2026-08-06
- 유형 : 소유권침범
- 상황 : T12의 검증 방식은 "수동 — 재개 전후 LLM 호출 횟수 로그 첨부"다. 그런데 실 API는 D26의 크레딧 소진(429)으로 여전히 막혀 있어 수동 검증으로는 완료 조건을 증명할 수 없다. 그리고 "재개했더니 처음부터 다시 돌더라"는 **회귀하기 매우 쉬운 결함**이다 — 누가 `resume_or_start()` 밖에서 `invoke`를 한 번 부르면 조용히 되살아나고, 화면상으로는 그냥 느려질 뿐이라 눈에 안 띈다. 수동 검증만 두면 이 회귀를 잡을 그물이 없다.
- 조치 : `tests/test_hitl.py`를 신설해 카드가 요구한 **호출 횟수 비교를 그대로 자동화**했다. LLM 도구 4종을 `Counter`를 물린 픽스처 스텁으로 대체하고(R5 — 반환값은 전부 `fixtures/`의 골든 데이터), 중단 → 재개 전후의 호출 횟수를 비교한다. 실측:

  | 도구 | 재개 전 | 재개 후 | 처음부터 다시 돌았다면 |
  | --- | --- | --- | --- |
  | `extract_competencies` | 1 | **1** | 2 |
  | `decompose_criteria` | 1 | **1** | 2 |
  | `verify_criteria` | 1 | 2 | 3 |
  | `fill_brief_slots` | 0 | 1 | 1 |

  `verify_criteria`만 2인데 이건 재실행이 아니라 **재판정**이다 — `verify` 노드 1회 + 델타 인터뷰가 답변을 받아 다시 판정한 1회. 상류 두 노드가 1로 고정된 것이 완료 조건의 증거다. 아울러 `AppTest`로 `app/main.py`를 실제 구동해 입력 폼 → 중단 폼 → 답변 제출 → 요약 지표·다운로드 버튼까지 관통시켰고, 이 경로에서도 같은 카운트를 확인했다.
- 소유권 : `tests/test_hitl.py`는 어느 카드의 소유 파일도 아니다(D21과 같은 판단). 신규 파일이라 다른 카드의 테스트 거동에 영향이 없고, 이게 없으면 R4("검증 명령 통과가 완료 조건")를 만족시킬 수단이 크레딧 충전 전까지 존재하지 않는다.
- 남은 부채 : **실 API로 UI를 한 번 관통시키는 것** — D26과 같은 부채이며 같은 한 번의 실행으로 함께 해소된다. 새로 짤 코드는 없다.

### [D32] T12 · 그래프를 `st.cache_resource`가 아니라 `st.session_state`에 둔다
- 일자 : 2026-08-06
- 유형 : 선택결정
- 상황 : 컴파일된 그래프(와 그 안의 sqlite 커넥션)를 rerun마다 새로 만들면 커넥션이 계속 쌓인다. 어딘가에 담아 재사용해야 하는데 Streamlit의 관용 수단은 `st.cache_resource`(프로세스 전역)다.
- 조치 : `st.session_state`에 담았다. ① 체크포인터가 여는 sqlite 커넥션의 수명이 세션과 같아진다. ② 프로세스 전역 캐시면 **앞 테스트의 스레드가 다음 테스트로 샌다** — `AppTest`가 세션마다 깨끗한 상태에서 시작해야 재개 루프를 신뢰성 있게 검증할 수 있다. 세션당 그래프 1개 비용은 컴파일 한 번이라 무시할 만하다.
- 부수 (실패 처리) : LLM 호출이 실패하면 그래프가 중간에 서는데, 그 스레드는 `interrupts`가 비어 있어 국면 판별상 **"완료"로 보인다**(D27의 판별 규칙상 불가피). 예외를 삼키면 사용자는 까닭 모를 빈 결과를 보게 되므로, `advance()`가 예외를 잡아 사유를 세션에 남기고 화면 상단에 그대로 띄운다. `graphs/session.py`(T10 소유)는 손대지 않았다.
- 후속 : T13이 `graph.stream()`을 붙일 때도 이 그래프 인스턴스를 재사용해야 한다 — 새로 컴파일하면 체크포인터가 갈라진다.

### [D33] T13 · 스트리밍을 `resume_or_start()` 경계 안으로 (R2 예외, 사용자 승인)
- 일자 : 2026-08-06
- 유형 : 소유권침범
- 상황 : T13의 소유 파일은 `app/progress.py`·`app/main.py`·`presets/node_labels.yaml` 셋인데, 진행 표시를 하려면 `graph.stream()`을 어디선가 불러야 한다. 그런데 `graphs/session.py`(T10 소유)의 유일한 규칙이 "`resume_or_start()` 밖에서 `graph.invoke()`를 직접 부르지 않는다"(D27)이고, DEVPLAN §5가 T13 유의사항으로 **"스트리밍도 이 경계를 지나야 한다"**를 미리 못박아 뒀다. 두 지시가 정면으로 부딪힌다.
- 선택지 : ① `graphs/session.py`에 `on_event` 인자를 추가한다 — R2 위반 1건. ② `app/progress.py`에서 `graph.stream()`을 직접 부른다 — R2는 지키지만 **국면 조회를 건너뛰는 실행 경로가 하나 더 생긴다.**
- 조치 : **사용자 판단으로 ①을 택했다**(D29와 같은 형태의 예외). ②가 위험한 이유는 화면에 안 보이기 때문이다 — 국면을 안 보고 스트림을 열면 완료된 스레드가 rerun마다 처음부터 다시 도는 결함(D27·T12 완료 조건)이 되살아나는데, 증상은 "좀 느리다"뿐이라 눈에 안 띈다. 실행 지점이 하나로 유지되는 편이 그 회귀를 구조적으로 막는다.
- 변경 범위 : `resume_or_start(..., on_event=None)` **인자 추가만**(제거·변경 없음). 실행은 `_run()` 하나로 모았고, `on_event`가 없으면 기존 `invoke` 경로 그대로다 — T10·T12의 테스트가 한 줄도 안 바뀌고 통과한다. **표(어느 국면에서 실행하는가)는 손대지 않았다** — 실행하지 않기로 한 국면에서는 이벤트도 흐르지 않으며 `test_completed_thread_streams_nothing`으로 고정했다.
- 실측 (langgraph 1.2.10) : `stream_mode`에 모드를 **둘 이상** 주면 청크가 `(mode, payload)` **튜플**로 오고, 하나면 payload만 온다. `("updates", "custom")`을 쓴다 — `custom`은 노드 **안에서** `get_stream_writer()`로 흘린 값이고, 실측상 **그 노드의 완료 청크보다 먼저** 도착한다. 수집 ReAct(T18)의 tool call을 "지금 일하는 중"으로 보여줄 통로가 이것이다. 중단은 `('updates', {'__interrupt__': (Interrupt(...),)})`로 온다.
- 후속 : 없음. T14~T19가 노드를 늘려도 이 경계는 그대로다.

### [D34] T13 · 진행 표시의 "문구"와 "규칙"을 프리셋으로 갈라놓았다
- 일자 : 2026-08-06
- 유형 : 선택결정
- 상황 : 카드 불변식이 "노드명은 사용자 언어로 매핑, **내부 함수명 노출 금지**"다. 매핑을 코드에 두면 노드가 하나 늘 때마다 `app/progress.py`를 고치게 되고, 그 파일은 T13 소유라 T14·T18·T19가 전부 R2를 밟는다.
- 조치 : 문구를 전부 `presets/node_labels.yaml`로 뺐다. 아직 배선되지 않은 노드(`discover_jobs`·`collect`·`parse_resume`·`build_profile`)와 수집 도구 5종의 라벨도 **미리** 넣어 뒀다 — 그래야 뒤 태스크가 이 파일을 안 건드린다. 매핑에 없는 노드는 이름이 아니라 `fallback`("처리 중")으로 떨어지므로, 모르는 노드가 와도 내부 이름이 새지 않는다(`test_internal_function_names_never_reach_the_screen`).
- 부수 결정 (`repeats_while`) : 델타 인터뷰는 조건부 간선으로 **자기 루프**를 돈다(D30). 노드가 부분 갱신을 돌려줬다고 곧바로 ✅로 올리면, 1라운드 답변 직후 그 줄이 완료로 뜨고 2라운드에 다시 물을 때 화면이 뒤로 돌아간다. 그래서 `repeats_while: pending_questions` 한 줄로 "이 필드가 남아 있으면 아직 안 끝난 것"을 선언하게 했다. **라운드 상한을 여기서 세지 않는 것이 요점이다** — 상한은 노드가 갖는다(D30)와 같은 판단이며, 진행 표시는 상태만 읽는다. T19 H1·T22 H4는 코드가 아니라 YAML 한 줄로 붙는다.
- 부수 결정 (중단 줄 판별) : `__interrupt__` 청크에는 **어느 노드가 멈췄는지가 안 실린다.** "끝나지 않은 첫 줄"을 ⏸로 세우는 규칙을 썼고, 위의 `repeats_while` 덕에 2라운드째에도 이 규칙이 그대로 맞는다.
- 의존성 : `pyyaml` 6.0.3 추가. 이미 전이 의존으로 설치돼 있었지만 명시하지 않으면 상류가 빠질 때 조용히 깨진다.
- 후속 : T28은 `add_listener()`로 이 스트림에 붙는다. `ProgressEvent`가 화면에 안 쓰는 `seconds`·`payload`까지 들고 있는 게 그 때문이다 — 응답 시간·도구 호출 횟수가 별도 계측 없이 나온다(T28 불변식 "중복 계측 금지").

### [D35] T13 · 수동 검증 대신 자동 + 뮤테이션으로 그물을 확인
- 일자 : 2026-08-06
- 유형 : 소유권침범
- 상황 : 카드의 검증 방식은 "수동 — 화면 녹화 또는 스크린샷"인데 실 API가 크레딧 소진(D26)으로 막혀 있다. 그리고 스크린샷은 **무엇이 보였는지**를 남길 뿐 **무엇이 보여야 하는지**를 고정하지 못한다. 진행 표시는 노드가 하나 늘 때마다 조용히 어긋나는 종류의 화면이라 그물이 필요하다.
- 조치 : `tests/test_progress.py`를 신설했다(D21·D31과 같은 판단 — 신규 파일이라 다른 카드에 영향 없음). 30건이며 층은 넷이다: 프리셋(내부명 누출) / 트래커 상태 전이 / **실제 그래프 중단·재개** / `AppTest` 실앱 관통. LLM 스텁은 T12가 만든 것을 그대로 import해 쓴다 — 같은 스텁을 두 벌 두면 한쪽만 고쳤을 때 두 테스트가 다른 것을 검사하게 된다.
- 그물 확인 (뮤테이션) : 통과만으로는 그물인지 알 수 없어 일부러 깨 봤다. ① `repeats_while` 한 줄 제거 → `test_self_loop_...` **1건만** 실패(델타 인터뷰가 ✅로 조기 승격). ② `line.state = PAUSED`를 no-op으로 → 중단 관련 **3건** 실패. ③ `panel_slot()`이 `st.empty()` 대신 `st`를 반환 → 자리 테스트 1건 실패. 셋 다 의도한 테스트가 정확히 걸렸다.
- 발견한 결함 (단일 패널) : 답변 제출 국면은 "지난 진행을 먼저 그리고, 이어서 실행 패널을 여는" 순서라 패널이 **둘** 그려진다. 실행이 1~2분이라 그동안 묵은 ⏸ 패널과 도는 중인 패널이 나란히 남는다. `st.empty()`로 자리를 하나만 잡아 덮어쓰게 고쳤다. **이 결함은 `AppTest`로 못 잡는다** — `st.rerun()`이 끼어 있어 결과 트리에는 마지막 rerun의 화면만 남고 둘이던 국면은 흔적이 사라진다. 처음에 쓴 `len(at.status) == 1` 테스트가 뮤테이션에도 통과하길래(= 무의미) "어디에 그리는가"를 직접 고정하는 쪽으로 바꿨다.
- 남은 부채 : **실 API로 UI를 한 번 관통시키는 것.** D26·D31과 같은 부채이며 같은 한 번의 실행으로 함께 해소된다. 충전 후 `streamlit run app/main.py`면 되고 새로 짤 코드는 없다.

### [D36] 인프라 · 크레딧 충전 확인 — 차단 해제 + 온라인 회귀 4건 통과
- 일자 : 2026-08-07
- 유형 : 부채완료
- 상황 : D26 이래 `429 insufficient_quota`가 실 API 검증 전체를 막고 있었고, DEVPLAN §5의 차단 요소로 걸려 있었다. T14가 임베딩 API를 타므로 이 확인 없이는 P2를 끝낼 수 없다.
- 확인 방법 : 잔액을 직접 조회하는 API는 없으므로 **최소 비용 호출로 찔러 봤다** — `responses.create(gpt-4.1-mini, max_output_tokens=16)`와 `embeddings.create(text-embedding-3-small)` 각 1회. 채팅만 확인하지 않고 임베딩을 함께 넣은 이유는 **T14가 쓰는 게 임베딩 엔드포인트**라 채팅만 열려 있어도 T14는 여전히 막힐 수 있기 때문이다. 결과는 둘 다 성공(`in=8 out=10`, `dim=1536 tokens=1`)이며 `insufficient_quota`는 재현되지 않았다.
- 회귀 : `uv run pytest -m llm` → **4 passed, 276 deselected in 174.65s**. `test_extract`·`test_verify`·`test_fill_slots`·`test_graph_smoke` 전부 통과. 특히 `test_end_to_end_with_real_llm`이 7노드 파이프라인을 실 API로 관통시켰다 — D26이 "코드 결함이 아니라 계정 잔액 문제"라고 판단한 근거(T08 스모크 통과)가 충전 후에도 그대로 성립함을 재확인한 셈이다.
- 이 항목이 증명하지 **않는** 것 : D26·D31·D35의 남은 부채는 여전히 살아 있다. 통과한 것은 **그래프 계층의 실 API 관통**이고, 부채는 **Streamlit UI를 사람이 실제로 눌러 관통시키는 것**이다. `AppTest`가 UI 전 구간을 이미 덮고 있어 남은 차이는 브라우저 실구동 1회뿐이지만, 그 1회를 하지 않은 채 T09를 ✅로 올리면 R4("됐다"는 자기보고 금지)를 어긴다. **T09는 ⚠️로 둔다.**
- 부수 : 모델 재검토는 하지 않았다. D22의 판단(gpt-5 계열은 temperature 미지원 + reasoning 토큰이 output 과금)은 충전과 무관하게 유효하므로 `gpt-4.1-mini`를 그대로 쓴다.
- 후속 : 차단 요소가 사라졌으므로 T14 착수 가능. 임베딩 모델 선정(§16 S8)은 T14 카드가 요구하는 대로 픽스처 비교 후 이 로그에 근거를 남긴다.

### [D37] T14 · S8(임베딩 모델) 실측 선정 + R6·R2 충돌로 구현 보류
- 일자 : 2026-08-07
- 유형 : 선택결정 / 소유권침범(미해결)
- 상황 : T14 카드가 "임베딩 모델이 미정(§16 S8)이므로 한국어 기술 역량 표현의 유사도 품질을 **픽스처로 비교한 뒤 선택**하고 근거를 남길 것"을 착수 조건으로 요구한다. 후보는 OpenAI `text-embedding-3-small`($0.02/1M)과 `text-embedding-3-large`($0.13/1M)로 잡았다. 로컬 한국어 임베딩(BGE-m3 등)은 후보에서 뺐다 — `torch`·`sentence-transformers`가 딸려 들어와 의존성이 GB 단위로 늘어나는데, 이 프로젝트의 임베딩 물량은 **분석 1회당 34건·786토큰**이라 그 무게를 정당화할 규모가 아니다.

- 실험 1 (픽스처 정답쌍) : `fixtures/competencies_required.json`(요구 26건) × `fixtures/profile_sample.json`(보유 8건)에서 사람이 확인한 명백한 대응쌍 9건을 정답으로 고정하고 순위를 쟀다.

  | 모델 | Recall@1 | Recall@3 | MRR |
  | --- | --- | --- | --- |
  | `3-small` | 9/9 | 9/9 | 1.000 |
  | `3-large` | 9/9 | 9/9 | 1.000 |

  **변별이 안 된다.** 픽스처의 대응쌍이 "Docker 기반 컨테이너 이미지 빌드 및 배포" ↔ 같은 문장처럼 표기가 이미 일치해서, 카드의 완료 조건("명백한 대응쌍이 top-3에 포함")은 어느 모델로도 만족된다. 즉 **완료 조건만으로는 S8을 결정할 수 없다.**

- 실험 2 (별칭 표기, 결정적) : 그래서 카드가 든 예시("쿠버네티스 운영" ↔ "K8s 클러스터 관리")를 실제 변별 조건으로 삼았다. 역량명은 **원문 보존**(T04 불변식)이라 한글 표기·영문·약어가 섞여 들어오는 상황이 반드시 생기며, 문자열 매칭이 불가능한 이유도 그것이다. 요구 역량을 별칭 표기 9건으로 바꿔 보유 8건 전체와 경쟁시켰다.

  | 모델 | Recall@1 | Recall@3 |
  | --- | --- | --- |
  | `3-small` | **8/9 (89%)** | 9/9 |
  | `3-large` @1536 | **9/9 (100%)** | 9/9 |
  | `3-large` @3072 | **9/9 (100%)** | 9/9 |

  small이 놓친 1건은 `포스트그레스 성능 최적화 → pf-04(PostgreSQL 쿼리 튜닝 및 인덱스 설계)`로, 정답을 **3위**로 밀고 그 앞에 `pf-02(pytest 커버리지)`·`pf-08(K8s 운영)`을 올렸다. 의미상 이웃도 아닌 것들이 앞선 것이라 우연한 실패로 보기 어렵다 — "포스트그레스"라는 한글 음차를 제품명으로 못 잡은 것으로 읽힌다.

- 선정 : **`text-embedding-3-large` + `dimensions=1536`.** 근거 셋. ① 별칭 표기에서 유일하게 R@1 100%다. ② `3-large`는 Matryoshka 학습이라 3072 → 1536 절단으로도 품질이 안 떨어졌고(위 표 동일), 그러면 **인덱스 메모리는 small과 완전히 같다.** ③ 단가는 6.5배지만 분모가 분석 1회당 786토큰이라 **회당 약 $0.0001**이다 — 같은 실행에서 도는 `gpt-4.1-mini` 호출 비용에 묻히는 수준이라 비용은 판단 기준이 되지 못한다. `top_k=3`만 보면 두 모델이 같지만, 유사도를 **후보 추림에만 쓰고 판정에 안 쓰는**(카드 불변식) 구조에서는 후보 리스트의 1위가 정확할수록 뒤 단계(T05 기준 검증)가 낭비하는 호출이 줄어든다.

- **구현 보류 (미해결 충돌)** : 코드를 쓰기 직전에 규칙 둘이 정면으로 부딪히는 걸 발견해 멈췄다.
  - **R6** — "LLM 호출은 `llm/` 어댑터를 통해서만". `llm/client.py`는 docstring에서 스스로를 "저장소에서 OpenAI SDK를 직접 import 하는 **유일한** 지점"으로 선언한다. 임베딩도 OpenAI API 호출이다.
  - **R2** — T14의 소유 파일은 `tools/retrieve.py`·`tests/test_retrieve.py` 둘뿐이다. `llm/client.py`는 **T04 소유**라 건드리면 위반이다.
  - 선택지 ① `llm/client.py`에 `embed_texts()`를 **추가만** 한다 → R6 유지, R2 위반 1건. D29·D33과 완전히 같은 형태이며 두 번 다 사용자 판단으로 이 쪽을 택했다. T04는 완료돼 동시 편집 위험이 없고, 기존 호출부는 한 줄도 안 바뀐다.
  - 선택지 ② `tools/retrieve.py`에서 SDK를 직접 import 한다 → R2 유지, R6 위반. SDK import 지점이 둘로 늘어 "모델·SDK를 갈아끼울 때 호출부를 안 건드린다"는 어댑터의 목적이 깨지고, 테스트에서 통째로 대체하는 경로도 두 가지가 된다.
  - **조치 : 사용자 판단을 받기로 하고 구현을 중단했다**(R1의 충돌 시 프로토콜과 같은 취지). 앞선 두 번의 R2 예외가 모두 사용자 승인 위에 성립했으므로, 세 번째를 임의로 밀어붙이면 그 선례의 의미가 없어진다.
- 부수 : `faiss-cpu` 1.15.0이 이 환경(win32·Python 3.11)에 휠로 설치 가능함을 확인했다(`uv pip install --dry-run`). 실제 추가는 구현 착수 시점으로 미뤘다.
- 후속 : 위 선택지 결정 후 T14 착수. **S8은 이 항목으로 해소됐으므로 재실험은 필요 없다** — 위 수치가 근거이며 다시 재면 API 비용만 든다.

### [D38] 설계 · 스택은 "가용 목록"이지 약속이 아니다 — LangChain 모델 계층 미채택, Faiss 유지
- 일자 : 2026-08-07
- 유형 : 선택결정
- 상황 : `AGENTS.md`·`README.md`가 스택에 LangChain을 적어 뒀는데 `pyproject.toml`에 직접 의존성이 없고 `.py` 어디에서도 import하지 않는다(전이 의존으로만 존재). 처음엔 문서불일치로 봤으나, **사용자 확인 결과 그 목록은 "쓸 수 있다"는 가용 스택이지 "무조건 쓴다"는 약속이 아니다.** 따라서 문서는 틀리지 않았고 고칠 것이 없다. 요구는 "적재적소에 쓰고 필요 없으면 과감히 빼되, **장기적으로 뭐가 나은지는 따져라**"였다.
- 정리 (혼동 방지) : LangGraph는 **쓰고 있다** — `graphs/analysis_graph.py:25`의 `StateGraph`, 체크포인터, `interrupt()`, 재개 루프가 전부 그것이다(9개 파일). 안 쓰는 건 **LangChain의 모델 추상화 계층**(`ChatOpenAI` + `with_structured_output`)이다. 둘은 대체재가 아니라 다른 층이므로 "LangGraph 대신 OpenAI SDK"라는 대비는 성립하지 않는다. `langchain-core 1.5.3`은 langgraph의 전이 의존으로 이미 lock에 있어 **쓰려면 공짜였는데 안 쓴 것**이다.
- 유지 판단 (직접 SDK) 근거 넷 :
  ① 교체 지점을 남의 추상화에 맡기지 않는다 — R6이 이미 `llm/` 어댑터를 교체 지점으로 정했고, LangChain을 넣으면 목적이 같은 층이 하나 더 얹힌다.
  ② `responses.parse(text_format=…)`로 Responses API의 네이티브 structured output을 직접 쓴다. 래퍼를 끼면 새 엔드포인트 지원을 기다리는 쪽이 된다.
  ③ **어떤 파라미터가 실제로 API에 가는지 알아야 했다** — D22가 그 실증이다(`temperature` 미지원, reasoning 토큰 output 과금). `test_extract.py:235`의 `assert kw["temperature"] == 0.0`은 SDK에 넘어가는 kwargs를 직접 고정하는데, 래퍼를 끼면 "래퍼가 뭘로 번역했나"에 대한 단언이 되어 의미가 흐려진다.
  ④ 테스트 대체가 주입점 하나(`client=`)로 끝난다 — 오프라인 296건이 네트워크 없이 도는 근거다.
- **포기하는 것 (정직하게)** : 프로바이더 이식성. Claude·Gemini로 옮기면 `llm/` 밑에 구현이 하나 더 필요하다. 다만 어댑터가 함수 하나짜리라 비용이 작고, 애초에 D22가 보여주듯 모델별 파라미터 차이는 **어떤 추상화를 껴도 새어나온다** — 추상화가 감춰주는 건 문법이지 모델 차이가 아니므로, 이 프로젝트가 산 이식성은 장부상보다 작다.
- **재판단 지점은 T18 하나다** : `langgraph.prebuilt.create_react_agent`의 `model` 파라미터를 실측하니 `BaseChatModel`/`langchain_core.Runnable`만 받는다. 즉 T18에서 이걸 쓰면 `langchain-openai`가 딸려 오고 **그 노드만 모델 호출 경로가 달라진다**(한 저장소에 경로 둘 = 이 프로젝트가 계속 경계해 온 "한쪽만 고쳐 조용히 어긋남"). 게다가 T18 카드의 핵심 요구가 "**예산 소진은 실패가 아니라 부분 수집으로 진행**"인데 프리빌트의 `recursion_limit`은 그걸 예외로 표현한다. ReAct 루프 자체는 조건부 간선 + 노드 둘이라 D30에서 이미 해본 패턴이므로 **직접 짜는 쪽을 권한다.** T18 착수 시 이 항목을 근거로 재확인할 것.
- Faiss 유지 : "적재적소" 잣대를 가장 정직하게 대면 이 프로젝트의 인덱스 규모(요구 26 × 보유 8, 세션 내 메모리·영속 캐시 없음)에서 Faiss의 강점(대규모·영속·ANN)은 하나도 걸리지 않는다. numpy 행렬곱으로 대체 가능하며, §14의 "load-bearing"은 이 규모에선 과한 표현이다. **그럼에도 사용자 판단으로 Faiss를 유지한다** — 설계도가 이미 근거를 적어 확정했고 `IndexFlatIP`가 10줄이라 유지 비용이 사실상 없다. 규모가 커지면 그때 이 판단이 자동으로 옳아진다.
- 후속 : 없음. 이 항목이 T18 세션의 재판단 근거다.

### [D39] T14 · R6·R2 충돌을 **새 파일**로 해소 + 뮤테이션에서 그물 구멍 2개 발견
- 일자 : 2026-08-07
- 유형 : 소유권침범 / 부채완료
- 충돌 해소 : D37이 남긴 선택지 ①(`llm/client.py` 수정 → R2 위반)·②(`tools/retrieve.py`에 SDK 직접 → R6 위반) 말고 **③ 새 파일 `llm/embeddings.py`**를 택했다. 근거는 이 저장소가 이미 세 번 쓴 판단이다 — `tests/conftest.py`(D21)·`tests/test_hitl.py`(D31)·`tests/test_progress.py`(D35) 전부 "어느 카드의 소유 파일도 아닌 신규 파일이라 다른 카드의 거동에 영향이 없다"로 정당화됐다. **R6은 파일이 아니라 `llm/` 계층을 가리키므로 새 파일도 R6 안이고, 기존 파일을 한 줄도 안 고치므로 R2도 안 밟는다.** 양쪽 다 어기지 않는 유일한 선택지다.
- 남은 흠 : `llm/client.py`의 docstring이 자신을 "OpenAI SDK를 직접 import 하는 **유일한** 지점"이라 적어 둔 것이 이제 사실이 아니다. 고치려면 T04 소유 파일을 건드려야 해 **그대로 뒀고**, 대신 `llm/embeddings.py` docstring 첫머리에 사정을 적었다. T04 재개 사안이 생기면 같이 정정할 것.
- `_build_client` 재사용 : `embeddings.py`가 `client.py`의 `_build_client`·`_ensure_env`를 import한다. 복제하지 않은 이유는 클라이언트 생성 방식(base_url·타임아웃 등)이 바뀔 때 두 벌이 조용히 갈라지기 때문이다. 같은 `llm/` 패키지 내부 호출이며 **읽기만 하므로 `client.py`는 수정되지 않는다.**
- 구현 : `retrieve_candidates(required, owned, top_k=3, *, embed=None)` — 계약 시그니처에 주입점만 **추가**했다(제거·변경 없음, D27과 같은 형태). 요구·보유를 한 번에 묶어 보내 **API 왕복은 입력 규모와 무관하게 1회**다. 정규화 후 `IndexFlatIP`라 내적이 곧 코사인이다.
- 불변식의 구조적 보장 : **유사도 점수를 반환하지 않는다.** 계약 반환형이 `list[tuple[str, str]]`이라 하류가 판정에 쓸 수단 자체가 없다 — 문서로 금지하는 것보다 강하다(§7-2 가짜 정밀도 방지).
- 검증 : 오프라인 20건 + 온라인 3건. 카드 완료 조건(`명백한 대응쌍이 top-3`)은 `test_llm_obvious_pairs_land_in_top3`이 픽스처 정답쌍 9건으로 고정했고, D37이 모델을 고른 근거였던 **별칭 표기 1순위**도 `test_llm_alias_notation_still_ranks_first`로 회귀 테스트가 됐다. 저장소 전체 **오프라인 296 passed / 온라인 7 passed**.
- **뮤테이션 결과 — 2건이 살아남았다** (D35와 같은 절차로 일부러 깼다) :
  | 뮤테이션 | 결과 |
  | --- | --- |
  | ① `k = min(top_k, len(owned))` → `k = top_k` | **살아남음** |
  | ② `sorted(response.data, key=index)` → 원순서 | 의도한 1건 실패 ✅ |
  | ③ 정규화 제거(`_unit_matrix`) | **살아남음** → 테스트 보강 후 1건 실패 ✅ |
  - ①의 원인 : 캡을 없애도 `pos < 0` 건너뛰기가 faiss의 -1 패딩을 걸러 **결과가 같다.** 두 방어가 서로 중복이라 테스트가 구별하지 못한다. 둘 다 남겼다(faiss 패딩 거동에 기대지 않기 위한 이중 방어) — 대신 테스트 docstring에 "결과만 고정하고 수단은 고정하지 않는다"를 명시해 **다음 사람이 이 테스트를 과신하지 않게** 했다.
  - ③의 원인이 더 중요하다 : **실제 OpenAI 임베딩은 이미 단위벡터로 오기 때문에 정규화가 실 경로에서 no-op이다.** 그래서 온라인 테스트로도 안 잡힌다. 정규화를 빠뜨리면 `IndexFlatIP`가 순수 내적이 되어 **긴 텍스트가 그냥 유리해지는데**, 지금 픽스처로는 증상이 안 보이고 임베더를 바꾸는 순간 조용히 어긋난다. 정규화되지 않은 벡터를 주는 임베더로만 잡히므로 `test_ranking_is_cosine_not_raw_dot_product`를 추가했다 — 방향이 같은 짧은 벡터(cos=1.0, 내적 0.5)와 어긋난 긴 벡터(cos≈0.71, 내적 10.0)를 경쟁시킨다.
- 의존성 : `faiss-cpu` 1.15.0 + `numpy` 추가(D23·D34와 같은 방식). numpy는 이미 전이 의존이었지만 명시했다 — D34가 pyyaml에서 겪은 "상류가 빠지면 조용히 깨진다"와 같은 이유다.
- 후속 : T15(카드 렌더러)와 `aggregate` 노드가 이 후보쌍으로 `my_level`을 채우면 D24가 미뤄 둔 **강점 표시(§7-3)**가 선다. 그건 이 카드 범위가 아니므로 손대지 않았다(R7).

### [D40] 계획 · 남은 계획 점검 — 배선 갭이 T14만이 아니었다 + T14b 신설 + §2-1 규약
- 일자 : 2026-08-07
- 유형 : 선택결정 / 계획수정
- 계기 : "다음 작업이 뭔지, 남은 계획이 맞는지" 점검 요청. 구현이 아니라 계획을 본 세션이다. 시작 시점 실측 **오프라인 296 passed / 7 deselected (4.22초)** — 원장 기록과 일치.

**① 배선 갭은 T14 하나가 아니라 카드 설계의 반복 결함이었다**
D39가 T14를 두고 지적한 "만들었는데 아무도 안 부른다"를 남은 카드 전체에 대고 훑으니 같은 것이 셋 더 나왔다.

| 카드 | 만드는 것 | 부르는 사람 |
| --- | --- | --- |
| T14 | `tools/retrieve.py` | **없음** (D39가 이미 지적) |
| T15 | `render/cards.py` | **없음** — 소유 파일에 `app/main.py`가 없다. 화면은 T09가 자기 파일에 만든 인라인 렌더링을 계속 쓴다 |
| T18 | `nodes/collect.py` | **없음** — `graphs/analysis_graph.py`가 없어 `ingest_pasted_jd`를 교체할 주체가 없다 |
| T25 | `nodes/gates.py` | **없음** — 동일 |
| T26 | `llm/sanitize.py` + `tools/extract.py`(수정) | ✅ 있음 |
| T28 | `obs/metrics.py` + `app/progress.py`(수정) | ✅ 있음 |

- 근본 원인은 **§2의 소유권 원칙("파일 하나는 태스크 하나")이 배선 파일에는 애초에 안 맞는다**는 것이다. 노드를 만드는 카드와 그것을 꽂는 카드가 다르면 죽은 모듈이 남는다. `graphs/analysis_graph.py`는 이미 T08이 만들고 T12(D29)·T13(D33)이 예외 승인으로 확장했다 — **세 번 예외면 그건 규칙이다.**
- 해소 : `DEVPLAN.md`에 **§2-1 배선 전용 공유 파일** 규약 신설(사용자 승인). `graphs/analysis_graph.py`·`app/main.py`·`presets/node_labels.yaml` 셋은 소유자가 없고, 각 카드가 자기 호출부만 추가하면 R2 위반이 아니다. 대신 ① 카드 소유 파일에 **명시**해야 하고 ② **추가한 배선을 검증에 포함**해야 한다(배선은 테스트가 없으면 조용히 빠진다).
- `presets/node_labels.yaml`을 셋에 넣은 이유 : T13이 미배선 노드 라벨을 선제적으로 넣어 뒀지만(주석에 "안 그러면 해당 태스크가 T13 소유 파일을 건드린다"고 적혀 있다) `retrieve`는 예견하지 못했다. 같은 성격의 등록부라 같은 취급이 맞다.
- 후속 : T15 소유 파일에 `app/main.py` 추가 완료. **T18·T25는 착수 시 `graphs/analysis_graph.py`를 추가할 것.**

**② T14 배선 갭의 파장이 D39가 적은 것보다 넓다**
D39·원장은 "LLM 호출 감소가 달성 안 된다"로만 적었는데, `my_level`이 전원 `None`인 결과가 브리프의 **계산 계층**을 갉고 있다.
- `level_gap()`에서 `mine`이 전원 `NO_LEVEL_INDEX`로 상수화 → 격차가 **JD 요구 레벨만** 반영하고 사용자 격차는 반영하지 않는다 → **트랙2 우선순위가 사실상 무의미하다.** `effort_band`도 `level_gap` 파생이라 같이 왜곡된다.
- `exceeds_requirement()`는 `my_level is None`이면 무조건 `False` → 트랙1 강점 정렬이 죽어 있다.
- 즉 **현재 브리프는 "이 JD가 뭘 요구하나"까지는 맞고 "나한테 뭐가 급한가"는 못 낸다.** 제품의 핵심 주장이 여기 걸려 있다.
- 정정 : D39의 "후속" 항목이 "`my_level`을 채우면 강점 표시(§7-3)가 선다"고 적었는데 **절반만 맞다.** `MatchResult.is_strength`는 아무도 세팅하지 않고(기본 `False`) `BriefCard`에는 필드 자체가 없어 화면·문서로 나갈 경로가 없다. 실제로 서는 것은 **트랙1 정렬**뿐이다. 배지까지 세우려면 계약 변경(R1)이라 T14b 범위 밖으로 뒀다.

**③ Phase 2 완료 판정 문구가 달성 불가능한 문장이었다 — 고쳤다**
- 원문 : "후보쌍 필터링으로 LLM 호출이 유의미하게 감소".
- 이 파이프라인의 LLM 호출은 extract 1 + decompose 1 + verify 1(라운드당) + fill_slots 1로 **입력 규모와 무관하게 고정**이다. `verify_criteria`는 기준이 몇 개든 `complete_structured` 1회다 — "LLM 도구는 배치 호출"(AGENTS.md)이 이미 그렇게 만들어 놨다. **후보쌍을 끼워도 호출 수는 그대로다.**
- 줄어드는 건 `build_verification_prompt`의 프로필 블록 토큰뿐인데, 요구 26 × top-3의 합집합이면 보유 8건 거의 전부라 그마저 실질이 없다.
- 새 문구 : "후보쌍이 `my_level`을 채워 **브리프가 '나에게 맞춰' 정렬된다**". D38이 Faiss에 댄 잣대("이 규모에선 load-bearing이 과한 표현")를 완료 판정에도 댄 것이다. **안 고치면 T29 질의응답에서 그대로 역공당한다.**

**④ T14b 카드 신설 — `my_level` 결정 규칙이 이 카드의 유일한 난점**
- 소유 파일 : `nodes/analysis_nodes.py`(수정)·`graphs/analysis_graph.py`(수정)·`presets/node_labels.yaml`(수정)·`tests/test_wiring.py`(신설). T06b(D19)와 같은 형태의 신설이며, §2-1이 방금 규칙이 됐으므로 앞의 셋은 예외 승인이 아니라 규약 적용이다.
- 배선 위치는 `extract`와 `decompose` **사이**. 입력(`required`·`profile`)이 `extract` 직후 갖춰지고 소비는 `aggregate`이므로 그 사이 아무 데나 되지만, **입력이 준비되는 즉시**에 두면 나중에 verify 프롬프트를 후보쌍으로 좁히는 선택지가 열린 채로 남는다.
- **핵심 난점** : 임베딩 top-1은 "가장 가까운 것"이지 "대응하는 것"이 아니다. 보유가 `Docker 사용`뿐인데 요구가 `Kubernetes 운영`이면 top-1은 Docker이고, 그 레벨을 찍으면 **없는 경력을 지어내는 것**이다(§7-3 위반). 그런데 `retrieve_candidates`는 유사도 점수를 **일부러** 반환하지 않으므로(§7-2, D39) 임계값으로 거를 수단이 없다.
- **해결 — 유사도가 아니라 판정을 게이트로 쓴다** : `state == UNMET`이면 `my_level = None`. `MET`/`ADJACENT`일 때만 첫 후보쌍의 보유 레벨을 가져온다. `aggregate_states`가 근거 인용을 거쳐 MET/ADJACENT를 냈다는 것은 그 대응이 실제로 확인됐다는 뜻이므로, **§7-2("유사도는 후보 추림에만")와 §7-3("지어내지 않기")을 동시에 지키는 유일한 조합이다.**
- 순서 의존에 관한 정정 : 처음엔 "리스트 순서를 랭크로 쓰는 건 문서화 안 된 성질에 의존"이라고 봤으나 **틀렸다.** `tools/retrieve.py`의 docstring이 "같은 요구 역량 안에서는 유사도 내림차순"이라고 명문화했고 D39의 뮤테이션 ②가 테스트로 고정했다. 다만 **계약(`contracts/tools.py`)에는 그 문장이 없다** — 계약과 구현 사이에 보장 하나가 어긋나 있다. R1이라 손대지 않았고, 대신 T14b가 이 의존을 자기 테스트로 직접 고정하게 했다. **T01 재개 사안이 생기면 계약 docstring에 순서 보장을 옮겨 적을 것**(D39가 남긴 `llm/client.py` docstring 흠과 같이 처리하면 된다).
- 회귀가 이 카드의 진짜 위험이다 : `aggregate`를 `test_graph_smoke`·`test_hitl`·`test_progress`가 이미 물고 있고 노드가 하나 늘면 `NODE_SEQUENCE` 길이 기반 단언이 깨질 수 있다. 카드에 "깨지면 테스트를 고치기 전에 회귀인지 기대값 변경인지 판단하라"를 명시했다.

**⑤ Phase 3~5에서 발견한 것 (구현 아님, 기록만)**
- **T27이 실제 병목일 가능성이 높다.** 완료 조건이 "사람-LLM 일치율"인데 사람이 20~30건을 매기는 시간이 계획 어디에도 없다. 발표 일정이 있다면 P4 진입 시점에 라벨링을 병렬로 시작해야 한다.
- T24 완료 조건이 "수동 — 업로드 전후 질문 수 비교"인데, T12가 이미 호출 횟수를 자동으로 증명한 전례가 있다(D31). 수동으로 남길 이유가 없다.
- T18은 D38이 재판단 근거를 이미 다 적어 뒀다(직접 짜는 쪽 권장). 추가 검토 없음.
- 남은 부채는 여전히 **`streamlit run app/main.py` 수동 1회**(D26·D31·D35). 크레딧은 D36에서 열렸다. **T14b·T15가 `app/main.py`를 건드리기 전에** 하는 편이 낫다 — 나중에 하면 "언제부터 깨졌나"가 흐려진다.
- 후속 : 없음. 이 항목이 T14b 세션의 근거다.

### [D41] 인프라 · `streamlit` 실행이 500으로 죽음 — `starlette` 상한이 헐거워 생긴 상류 파손
- 일자 : 2026-08-07
- 유형 : 일탈 / 의존성
- 증상 : `uv run streamlit run app/main.py` → 브라우저에 Connection 500. 서버 로그는
  `TypeError: GZipResponder.__init__() missing 1 required keyword-only argument: 'thread_minimum_size'`
  (`streamlit/web/server/starlette/starlette_gzip_middleware.py:125`).
- 원인 : **우리 코드가 아니라 버전 조합이다.**
  - `streamlit 1.61.0`(2026-08-04 릴리스)이 `starlette<2,>=0.46.0`으로 상한을 헐겁게 잡았다.
  - `starlette 1.4.0`이 **그 하루 뒤인 2026-08-05**에 나오면서 `GZipResponder.__init__`에 키워드 전용 **필수** 인자 `thread_minimum_size`를 추가했다(`starlette/middleware/gzip.py:150-157`).
  - streamlit의 서브클래스 `_MediaAwareGZipResponder`는 그걸 안 넘긴다. 즉 **streamlit이 테스트해 본 적 없는 starlette가 상한 안으로 걸어 들어왔다.**
  - `Accept-Encoding`에 gzip이 있는 모든 요청이 responder 생성 단계에서 죽으므로 브라우저 접속이 100% 실패한다.
- 조치 : `uv lock --upgrade-package streamlit --upgrade-package starlette` → **streamlit 1.61.0 → 1.61.1, starlette 1.4.0 → 1.3.1.**
  `starlette`를 손으로 내리지 않았다는 점이 중요하다 — **1.61.1이 상한을 조여서(`<1.4`) 리졸버가 알아서 내렸다.** 즉 업스트림이 이미 고친 문제이고 우리는 그 픽스를 당겼을 뿐이라, 나중에 `uv lock --upgrade`를 해도 되돌아가지 않는다. 우리가 직접 핀을 박았다면 그 핀이 언제 풀려도 되는지 아무도 모르게 됐을 것이다.
- **왜 테스트 296건이 이걸 못 잡았나 (이게 진짜 교훈)** : T09가 쓴 `AppTest`는 스크립트를 **인프로세스로 실행**한다. ASGI 미들웨어·HTTP 응답 경로를 아예 타지 않으므로 `uv run pytest -q`가 초록불이어도 **브라우저에서는 앱이 아예 안 뜬다.** D26·D31·D35가 "실 API로 UI를 한 번 관통시키는 수동 1회"를 부채로 남겨 둔 이유가 여기서 실증됐다 — 그 부채는 "API 키 문제"가 아니라 **HTTP 계층 전체가 미검증**이라는 뜻이었다.
- 검증 : 헤드리스 서버(`--server.port=8599`)를 띄우고 `Accept-Encoding: gzip`으로 요청 → `/` **200**, `/static/js/*` **200**. 이 헤더가 있어야 문제의 `_MediaAwareGZipResponder`가 실제로 생성되므로, 200이 곧 픽스의 증거다. 회귀 **오프라인 296 passed**(변화 없음).
- 남은 부채 : 이 확인은 **HTTP 200까지**다. 폼 제출 → 실 API → `.md` 다운로드의 브라우저 관통(D26)은 여전히 남아 있다. 다만 그 앞을 막고 있던 것이 치워졌다.
- 후속 : 없음. 다만 **스모크의 구멍은 그대로다** — `AppTest`는 HTTP를 안 탄다. T29 데모 리허설 전에 반드시 브라우저로 한 번 열어 볼 것.

### [D42] 계획 · JD가 이미지로만 제공되는 경우 — P0 입력 경로의 구멍 + T15b 신설
- 일자 : 2026-08-07
- 유형 : 선택결정 / 계획수정
- 계기 : **실사용에서 발견.** 채용 페이지의 JD 본문이 이미지로만 되어 있고 **그 이미지조차 다운로드가 막혀 있는** 경우가 있다. 현재 입력 경로는 `text_area` 붙여넣기 하나뿐이라(`app/main.py:166-170`) 사용자가 이미지를 보며 직접 타이핑해야 한다 — 사용성이 성립하지 않는다.
- **P0 완료 판정을 다시 읽어야 한다** : "붙여넣은 JD 1건 → .md 다운로드 관통"은 사실 **"텍스트로 된 JD"에만** 성립했다. 픽스처가 전부 텍스트 JD였기 때문에 이 구멍이 안 보였고, D41이 드러낸 것("테스트가 초록불이어도 실사용 경로가 막혀 있을 수 있다")과 **같은 종류의 눈먼 지점**이다. 다만 이번엔 계층이 다르다 — D41은 HTTP 계층 미검증, 이번은 **입력 데이터 형태의 가정**이다.
- 해결 방향 : 다운로드 차단을 뚫으려 하지 않는다. 그건 네트워크/DOM 계층 방어이고, **화면 캡처는 렌더링된 픽셀을 뜨므로 그 방어와 무관하게 항상 성공한다.** 문제를 "다운로드 뚫기"가 아니라 **"이미지를 입력으로 받기"**로 바꾼다.
- **계약을 안 건드린다는 점이 이 설계의 핵심** : 추출을 Streamlit 레이어에서 끝내고 결과 텍스트를 지금과 똑같이 `raw_jd_input`에 넣으면 `contracts/state.py`도 `ingest_pasted_jd`도 그래프도 **이미지의 존재 자체를 모른다.** R1을 안 밟고, P0 백본의 형태도 안 바뀐다.
- 새 OCR 엔진을 고르지 않는 이유 : T22(이력서)가 엔진을 미확정으로 둔 건 표·구획 같은 **레이아웃** 정확도가 필요해서다(§16 S7). JD는 구조 없는 본문만 뽑으면 되므로 문턱이 낮고, 이미 쓰는 vision 지원 모델로 충분하다. **T22의 엔진 선정과 이 카드는 별개 결정이며 억지로 지금 합치지 않는다.** T22 착수 시 "JD도 같은 엔진으로 통일할지"만 재검토한다.
- **제안 중 정정한 것 (기록해 둔다)** : 처음엔 "`app/hitl.py`의 `ocr_review` 재개 루프를 재사용하라"고 제안했으나 **틀렸다.** 그 재개 루프는 **그래프가 이미 실행 중일 때** `interrupt()`로 멈춘 지점을 다루는 메커니즘이다. JD 이미지 추출은 **그래프가 시작되기도 전** 폼 입력 단계에서 끝나므로 `interrupt()`도 `Command(resume=)`도 필요 없다 — 업로드 위젯 → 추출 → 편집 가능한 `text_area`에 채워 넣기가 전부인 일반 위젯 상호작용이다. `app/hitl.py:38`의 `ocr_review` 제목은 **T22(이력서 OCR 보정, H4)** 용으로 예약된 것이며 그 용도로 남긴다. HITL 메커니즘을 필요 없는 곳에 끌어다 쓰면 "하나의 재개 루프를 재사용한다"는 규약이 오히려 흐려진다.
- 카드 : `tasks/T15b.md` 신설. 소유 파일은 `llm/vision.py`(신설)·`app/main.py`(수정 — §2-1 배선 공유 파일)·`tests/test_vision.py`(신설). `llm/vision.py`는 D39가 `llm/embeddings.py`로 세운 전례를 그대로 따른다 — `_build_client`·`_ensure_env`를 import해 재사용하고(복제 금지), R6은 파일이 아니라 `llm/` **계층**을 가리키므로 새 파일도 R6 안이다.
- 순서 : **사용자 판단으로 MDD(T14b → T15) 뒤에 배치.** 카드 구모가 작고 T14b·T15와 소유 파일이 겹치지 않아(`app/main.py`는 §2-1 공유) 순서를 바꿔도 앞 카드를 다시 열 필요가 없다는 점을 확인한 뒤 내린 결정이다.
- 불변식으로 박은 것 : ① 텍스트 붙여넣기 경로는 **그대로 둔다**(이미지는 대안이지 대체가 아니다) ② 추출 결과를 **자동 신뢰하지 않는다** — 항상 편집 가능한 상태로 사용자에게 보여준다. "근거 없이 지어내지 않는다"를 입력 단계에도 적용하는 것이며, vision 추출이 표·다단 레이아웃에서 완벽하지 않다는 현실과도 맞다 ③ 추출 실패는 전체 실패가 아니다 — 기존 붙여넣기로 폴백(T06b D20·T17과 같은 패턴)
- 후속 : 없음. T15b 세션의 근거가 이 항목이다.

### [D43] T14b · `my_level`의 게이트는 유사도가 아니라 판정 + 오프라인이 실 API를 타고 있었다
- 일자 : 2026-08-08
- 유형 : 선택결정 / 소유권침범 / 부채완료
- 결과 : **오프라인 312 passed(6.72초) / 온라인 1 passed.** D39가 남긴 배선 갭(만들었는데 아무도 안 부름)이 닫혔다.

**① 배선 — `extract` 뒤, `decompose` 앞**
- `NODE_SEQUENCE`에 `("retrieve", retrieve)`를 꽂았다. 소비처(`aggregate`) **바로 앞이 아니라** 입력(`required`·`profile`)이 갖춰지는 지점에 뒀다. 준비 지점에 두면 나중에 `verify` 프롬프트를 후보쌍으로 좁히는 선택지가 열린 채로 남고, 뒤에 두면 그 문이 닫힌다.
- 대화형 경로(`interactive=True`)는 `node_sequence()`가 `INTERVIEW_AFTER` 기준으로 자동 계산하므로 **추가 작업이 없었다.** 그 사실을 테스트로 고정했다(`test_retrieve_is_wired_in_the_interactive_path_too`) — 안 그러면 다음 사람이 대화형 배선을 손으로 또 만진다.
- `presets/node_labels.yaml`에 라벨 추가(`보유 역량과 후보 매칭`). `app/progress.py`는 한 줄도 안 고쳤다(D34가 의도한 대로). `hide_zero: true`를 준 이유 — 프로필이 없으면 후보쌍이 0인데, 0을 띄우면 사용자가 실패로 읽는다.

**② 핵심 결정 — `my_level`의 게이트는 유사도가 아니라 판정이다**
- 문제 : 임베딩 top-1은 **"가장 가까운 것"이지 "대응하는 것"이 아니다.** 보유가 `Docker 사용`뿐인데 요구가 `Kubernetes 운영`이면 top-1은 Docker이고, 그 레벨을 찍으면 없는 경력을 지어내는 것이다(§7-3). 그런데 `retrieve_candidates`는 유사도 점수를 **일부러** 반환하지 않아(§7-2, D39) 임계값으로 거를 수단 자체가 없다.
- 해결 : `aggregate_states`의 판정을 필터로 쓴다.
  ```
  UNMET            → my_level = None   (후보쌍이 있어도 비운다)
  MET / ADJACENT   → 첫 후보쌍의 보유 레벨
  후보 없음 / level 없음 → None
  ```
  MET/ADJACENT가 나왔다는 건 **기준별 근거 인용을 거쳐 대응이 확인됐다**는 뜻이다. 유사도로는 못 하는 판별을 이미 근거가 해준 것이며, **§7-2("유사도는 후보 추림에만")와 §7-3("지어내지 않기")을 동시에 지키는 유일한 조합**이다. 규칙은 `my_level_for()`에 분리해 두어 테스트가 노드 전체를 돌리지 않고도 규칙만 때릴 수 있게 했다.
- "첫 후보 = top-1"은 `tools/retrieve.py` docstring의 순서 보장에 기댄다. **계약(`contracts/tools.py`)에는 그 문장이 없으므로** `test_aggregate_uses_the_first_pair_not_the_last`가 이 의존을 직접 고정한다(뮤테이션 ③). T01 재개 시 계약에 순서 보장을 옮겨 적을 것 — D40이 남긴 숙제 그대로다.
- `aggregate`는 여전히 **LLM을 안 부른다.** 후보쌍은 `retrieve` 노드가 이미 계산해 상태에 넣어 뒀고 여기선 읽기만 한다 — 순수 규칙 함수라는 성질(§8-2)이 안 깨졌음을 `test_aggregate_still_calls_no_llm`이 고정한다.

**③ 발견 — 오프라인 테스트가 실제 임베딩 API를 타고 있었다 (이게 이 세션의 진짜 수확)**
- 증상 : 노드를 꽂자 `uv run pytest -q`가 **4.22초 → 37.34초**. 그래프를 도는 테스트마다 1.5~2.5초씩 붙었다 — 네트워크 왕복 시간이다.
- 원인 : `tests/test_graph_smoke.py`의 `stubbed_tools`와 `tests/test_hitl.py`의 `install_stubs`가 **LLM 도구 4종만** 갈아끼운다. `retrieve_candidates`는 T14 시점에 그래프에 없었으니 목록에 있을 이유가 없었다. 노드가 5번째 API 호출을 데려온 순간 스텁 그물에 구멍이 뚫린 것이다.
- **왜 이게 중요한가** : §4의 "`uv run pytest -q`는 LLM 없이 통과해야 한다"가 조용히 깨졌는데 **테스트는 전부 초록불이었다.** 돈이 나가고 인터넷이 없으면 실패하는데도 아무도 안 알려준다. D41(테스트 초록불인데 브라우저에서 앱이 안 뜸)과 **같은 종류의 눈먼 지점**이며, 이번 세션에서만 세 번째다(D41 HTTP 계층 · D42 입력 데이터 형태 · D43 스텁 그물).
- 조치 : 두 픽스처에 `fake_retrieve`를 각 4줄 추가. **기존 4종을 스텁하는 것과 완전히 같은 패턴**이며 새 기법이 없다. 반환값은 픽스처 역량 id로 만든 쌍이라 R5(자기 구현을 흉내낸 mock 금지)도 안 어긴다 — 여기서 검증하는 건 "무엇이 매칭되는가"가 아니라 "매칭 결과를 어떻게 쓰는가"이기 때문이다.
- **R2 예외 (사용자 승인)** : 두 파일은 T08·T12 소유이며 T14b 카드의 소유 파일이 아니다. §2-1이 규칙으로 올린 배선 공유 파일(`graphs/analysis_graph.py`·`app/main.py`·`presets/node_labels.yaml`)에도 **테스트 파일은 안 들어간다.** 사용자에게 픽스처·스텁의 뜻과 변경 내용을 설명하고 승인받아 진행했다. 스텁을 안 넣으면 §4 정책을 지킬 방법이 없고, 그러면 R4(검증 통과가 완료 조건)로 T14b를 완료로 올릴 수 없다.
- **후속 (다음 사람에게)** : 그래프에 **API를 타는 노드를 새로 꽂을 때마다** 이 두 픽스처를 같이 봐야 한다. T18(수집 ReAct)이 정확히 그 경우다 — `fetch_jd_body`·`fetch_tech_blog`·`get_company_values`가 전부 네트워크를 탄다. 지금은 규약이 아니라 이 로그가 유일한 경고다.

**④ 회귀 — 2건, 둘 다 기대값 변경이지 회귀가 아니다**
| 깨진 테스트 | 판정 |
| --- | --- |
| `test_graph_wires_the_documented_sequence` | 문서화된 노드 순서에 `retrieve` 추가 — **의도된 변경** |
| `test_every_stage_runs_in_order` | 실행 순서에 `retrieve_candidates` 추가 — **의도된 변경** |
- 반면 `test_aggregate_uses_rules_only`의 `assert all(r.my_level is None ...)`은 **안 깨졌다.** 그 테스트는 `retrieve` 노드를 안 거치고 `aggregate`를 직접 부르므로 `candidate_pairs`가 비어 `my_level`이 그대로 `None`이다. D24가 "P0에는 후보쌍이 없어 레벨을 특정할 수 없다"고 적은 전제가 그 테스트 안에서는 여전히 참이라 손대지 않았다.

**⑤ 뮤테이션 — 3종 전부 잡혔다** (D39는 2건이 살아남았다)
| 뮤테이션 | 결과 |
| --- | --- |
| ① `UNMET → None` 게이트 제거 | **2건 실패** ✅ |
| ② `NODE_SEQUENCE`에서 `retrieve` 제거 | **4건 실패** ✅ |
| ③ `setdefault` → `[]=` (첫 후보 대신 마지막) | **1건 실패** ✅ |
- 뮤테이션 후 원본 복원을 확인했다(312 passed).

**⑥ 범위 밖으로 둔 것 (R7)**
- `is_strength`는 채우지 않았다. `BriefCard`에 필드가 없어 화면·문서로 나갈 경로가 없고 필드 추가는 R1이다. 다만 `my_level`이 채워지면서 `exceeds_requirement()`가 살아나 **트랙1 정렬이 강점 우선으로 바뀐다** — §7-3의 실질적 일부는 이것만으로 섰다.
- `verify` 프롬프트를 후보쌍으로 좁히는 것. 지금 규모에선 실익이 없다(D40 ③). 문은 열어 뒀다.
- 후속 : T15가 `BriefCard.my_level`을 그대로 읽으면 된다. **UNMET 카드의 `my_level`이 `None`인 것은 정상이며 버그가 아니다.**

### [D44] T15 · 두 렌더러의 parity를 "문자열 비교"가 아니라 **표시 항목 집합**으로 정의했다
- 일자 : 2026-08-08
- 유형 : 결정
- 배경 : 카드의 완료 조건은 "같은 brief를 T07과 렌더했을 때 **표시 항목 집합이 동일**(순서·형식만 다름)"이다. 그런데 Streamlit 렌더러는 값을 돌려주지 않고 `st.*`를 부를 뿐이라 비교할 문자열 자체가 없다.
- 후보 셋을 놓고 골랐다.
  | 방법 | 문제 |
  | --- | --- |
  | ① `AppTest`로 앱을 돌려 화면 요소를 긁어 `.md`와 대조 | 앱 전체(그래프·LLM 스텁)를 태워야 하고, 느리고, **렌더러가 아니라 앱을 검사**하게 된다 |
  | ② `st.*` 호출을 가로채 출력 문자열을 `.md`와 통째 비교 | 형식이 다르면(굵게·표·아이콘) 무조건 실패한다. 완료 조건이 "순서·형식만 다름"을 허용하는데 그걸 못 지킨다 |
  | ③ **순수 계층 `brief_items()`를 두고 항목·사실만 대조** ✅ | 형식은 자유, 항목 누락만 잡힌다 |
- **결정 ③.** `render/cards.py`를 두 층으로 나눴다. 위층 `brief_items(brief) -> list[Item]`은 Streamlit을 부르지 않고 **"이 브리프가 무엇을 표시하는가"**만 자료구조로 낸다. 아래층 `render_*`가 그걸 그린다.
  - `Item.facts`에는 **값만** 담고 형식(굵게·아이콘·표)은 뺐다. 형식까지 넣으면 "같은 내용을 다르게 꾸몄다"는 이유로 parity가 깨져서, 정작 잡아야 할 "항목이 통째로 빠졌다"를 못 잡는다.
  - 대조는 `fact in render_markdown(brief)` — `.md` 본문 substring이다. `.md`의 서식을 아예 모르는 채로 성립하므로 T07 구현에 결합되지 않는다.
- **두 계층이 갈라지는 것을 따로 막았다.** 순수 계층만 검사하면 "항목은 세는데 화면엔 안 그린다"가 통과한다. `RecordingSt` 대역으로 `render_brief()`가 실제로 그린 문자열을 모아 `brief_items()`의 사실이 전부 들어 있는지 본다.
- **한 방향만 봐도 되는 근거** : 두 렌더러의 입력은 같은 `StrategyBrief` 하나다. "`.md`에만 있고 화면엔 없는 항목"은 곧 `brief_items()`가 브리프 일부를 빠뜨렸다는 뜻이고, 그건 브리프에서 직접 다시 세는 테스트가 잡는다.
- **뮤테이션 7종 전부 잡혔다.**
  | 뮤테이션 | 결과 |
  | --- | --- |
  | ① 트랙3 카드를 항목에서 누락 | **5건 실패** ✅ |
  | ② 요구/보유 레벨을 뒤바꿈 | **2건 실패** ✅ |
  | ③ 근거 인용 누락 | **4건 실패** ✅ |
  | ④ 없는 유사도 점수("매칭 87%")를 지어냄 | **2건 실패** ✅ |
  | ⑤ 트랙2 우선순위 누락 | **1건 실패** ✅ |
  | ⑥ 컬처핏 미수집 표기를 다른 말로 | **2건 실패** ✅ |
  | ⑦ 최소 요건을 무조건 충족으로 | **2건 실패** ✅ |
  - ④가 잡힌다는 게 §7-2(유사도 점수 미반환, D39)를 화면 계층에서도 지킨다는 뜻이다. 점수를 지어내면 `.md`에 없으므로 parity가 깨진다.
  - 뮤테이션 후 원본 복원을 확인했다.
- 후속 : T27 루브릭은 `brief_items()`의 `key`를 평가 단위로 그대로 쓰면 된다 — 카드 하나 = `card:{comp_id}` 하나다.

### [D45] T15 · `BriefCard`에 없는 것을 그리는 방법 — 보강 데이터는 항목을 늘리지 않는다
- 일자 : 2026-08-08
- 유형 : 결정
- 문제 : 카드 규격이 요구하는 다섯 중 **둘이 `BriefCard`에 없다.**
  | 규격 항목 | 어디 있나 |
  | --- | --- |
  | 역량명 · 3-state 배지 · 내 레벨 vs 요구 | `BriefCard` ✅ |
  | **중요도** | `CompetencyRecord.importance` — `GraphState.required` |
  | **기준별 판정** | `MatchResult.verdicts` — `GraphState.match_results` |
- 선택지는 둘이었다. ① `BriefCard`에 필드를 추가한다 → **R1 위반**(계약 수정). ② 렌더러가 `GraphState`에서 보강 데이터를 받는다.
- **결정 ②.** `render_brief(brief, *, matches=None, required=None)`. 계약을 건드리지 않고, 없으면 그 줄만 빠지고 카드는 그대로 나온다.
- **다만 이게 parity를 깨뜨릴 수 있었다** — 화면에만 있고 `.md`엔 없는 내용이 생기기 때문이다. 그래서 경계를 구조로 박았다: **`brief_items()`는 `brief` 하나만 받는다.** 보강 데이터가 항목 계산에 끼어들 통로가 아예 없다. 테스트가 `inspect.signature`로 이 시그니처를 고정한다 — 누가 인자를 늘리면 실패한다.
- 즉 보강 데이터는 **이미 있는 카드 안의 상세**로만 들어가고 새 항목을 만들지 않는다. 접힌 `st.expander` 안에 있어 기본 화면에서는 보이지도 않는다.
- 후속 : `is_strength`는 여전히 아무도 안 채운다(D43). `MatchResult`에는 필드가 있지만 `BriefCard`에 없어 화면까지 못 온다 — 강점은 **트랙1 정렬 순서**로만 드러난다. 표시하려면 R1 변경이 필요하다.

### [D46] T15 · "최소 요건" 배지를 `st.metric`이 아니라 배너로 뺐다 — R2를 지키려는 선택이기도 하다
- 일자 : 2026-08-08
- 유형 : 결정 (사용자 판단)
- 발단 : 카드 규격의 "상단 배지 — 남은 기간 / 최소 요건 충족 여부"를 `st.metric` 다섯째 칸으로 넣었더니 **`tests/test_hitl.py`가 깨졌다.** T12가 결과 화면을 `{m.label for m in at.metric} == {"남은 기간", "소스 충족률", "신뢰등급", "카드"}`로 못 박아 뒀는데, 집합 동등 비교라 지표를 하나라도 더하거나 빼면 실패한다.
- **그 파일은 T15의 소유가 아니다(R2).** 선택지를 사용자에게 올렸다.
  | 안 | 내용 | 대가 |
  | --- | --- | --- |
  | A | 지표 넷은 그대로, 최소 요건은 `st.success`/`st.warning` 배너 | 없음 |
  | B | 지표를 재구성하고 `tests/test_hitl.py` 한 줄 수정 | **R2 예외 승인 필요** |
- **사용자 판단 A.** 규칙 회피가 아니라 실제로 더 낫다고 봤다 — 합불 판정 하나는 숫자 넷 사이에 끼면 묻히고, 색이 있는 배너가 "지원 가능한가"라는 질문에 먼저 답한다. R2를 안 어기는 건 부수 효과다.
- **"최소 요건"의 정의** : 필수(REQUIRED) 역량 중 미보유가 없는 상태. 중요도를 모르면(= `required`를 안 넘기면) **전체 카드로 판정한다** — 기준이 넓어져 통과가 더 어려워질 뿐, 없는 근거로 통과시키지는 않는다. 판정할 카드가 하나도 없으면 "판정하지 못했습니다"로 **충족이 아니다**.
- 지표 네 칸이 T09 때 모습 그대로여야 한다는 제약을 테스트로 못 박아 뒀다(`test_metric_row_stays_as_t09_left_it`) — 근거를 모른 채 나중에 지표를 바꾸면 T12가 깨지므로.
- 후속 : `tests/test_hitl.py`의 저 단언은 화면 구조에 결합돼 있다. 지금은 R2 덕에 오히려 회귀 그물 역할을 하지만, 화면이 더 바뀌면 T12 소유자와 조율이 필요하다.

### [D47] T15 · T09의 인라인 렌더링을 걷어냈다 — 배선을 검증에 포함
- 일자 : 2026-08-08
- 유형 : 결정
- `app/main.py`가 T09 때 자기 파일 안에 임시로 두었던 `render_summary()` · `render_tracks()` · `render_gaps()`를 삭제하고 `render_brief()` 호출 하나로 교체했다. `render_markdown`을 쓰는 다운로드 버튼은 건드리지 않았다.
- **§2-1이 요구하는 두 가지를 지켰다.** ① 카드의 소유 파일에 `app/main.py`가 명시돼 있다. ② 배선을 검증에 포함했다 — T14가 만들어 놓고 아무도 안 부른 전례(D39)가 있어서, 배선 테스트를 네 개 넣었다.
  | 테스트 | 무엇을 막나 |
  | --- | --- |
  | `test_app_calls_the_card_renderer` | 렌더러를 안 부르는 것 (= 죽은 코드) |
  | `test_inline_renderers_are_gone` | **교체가 아니라 추가**로 끝나 화면이 둘로 갈라지는 것 |
  | `test_app_still_offers_the_markdown_download` | 다운로드 경로를 건드린 것 |
  | `test_app_reports_a_missing_brief` | 브리프 없을 때 조용히 빈 화면이 되는 것 |
  - `AppTest`가 아니라 `monkeypatch`로 `main.st`·`main.render_brief`를 갈아끼웠다. 여기서 볼 것은 앱 전체가 아니라 **배선 한 줄**이라 그래프·LLM 스텁을 태울 이유가 없다. 앱 전체 관통은 `tests/test_hitl.py`가 이미 `AppTest`로 하고 있고, 그쪽이 실 Streamlit으로 새 렌더러를 통과시켰다(343 passed).
- **삭제로 없어진 것 하나** : 인터뷰 라운드 표시(`델타 인터뷰 N라운드…`)는 브리프가 아니라 **스레드의 이력**이라 `render/cards.py`로 못 옮긴다. `app/main.py`에 남겼다.
- 후속 : 없음.

### [D48] T15b · 이미지 업로더는 폼 **밖**이어야 한다 — 카드 스니펫을 그대로 쓰면 동작하지 않는다
- 일자 : 2026-08-08
- 유형 : 선택결정
- 상황 : 카드 T15b는 업로더를 `render_input_form()` 안, 즉 `st.form` **안쪽**에 두라고 스니펫까지 적어 놨다. 그런데 `st.form`의 존재 이유가 "제출 전까지 위젯 상호작용이 rerun을 일으키지 않는다"이므로, 업로더를 폼 안에 두면 이미지가 서버에 닿는 시점이 곧 **제출 순간**이다. 그때는 빈 `text_area`가 이미 함께 제출된 뒤라 "추출 결과를 `text_area`에 미리 채워 넣는다"는 카드의 요구를 **채울 자리 자체가 없다.** 사용자는 실행을 눌렀는데 "JD 본문을 입력해야 합니다" 에러를 보게 된다.
- 조치 : 업로더를 폼 **밖**(폼 바로 위)에 두는 `render_jd_image_input()`을 만들고 `start()`에서 폼보다 먼저 부른다. 추출 결과는 `st.session_state[JD_TEXT_KEY]`에 넣고, `text_area`는 같은 키를 `key=`로 받는다 — 위젯 생성 **전에** 세션에 값이 있으면 그 값으로 그려지고 사용자가 그대로 고칠 수 있다.
- **`AppTest`가 이 결함을 못 잡는다는 것이 이 항목의 핵심이다.** 폼 안/밖 두 버전을 만들어 `AppTest`로 돌려 봤더니 **둘 다 통과했다** — `AppTest`는 위젯 상태를 직접 주입하고 다시 돌리므로 브라우저의 폼 배칭을 아예 모사하지 않는다. D41이 "`AppTest`는 ASGI·HTTP 경로를 안 탄다"고 적은 것과 같은 종류의 맹점이고, 이번엔 그게 **위젯 실행 모델**에서 났다. 그래서 배치 근거를 테스트가 아니라 Streamlit의 실행 모델에 두고, 테스트는 동작이 아니라 **배치 자체**를 고정한다 — `test_uploader_sits_outside_the_form`이 JD 업로더의 `form_id`가 빈 문자열임을 보고, 프로필 업로더(폼 안이 맞다)를 대조군으로 같이 본다.
- 후속 : 화면에 위젯을 더 붙이는 카드는 "이 위젯이 폼 안이어야 하는가"를 먼저 판단할 것. 판단 기준은 하나다 — **제출 전에 다른 위젯을 바꿔야 하면 폼 밖이다.**

### [D49] T15b · 오프라인 테스트가 또 실 API를 탔다 — 이번엔 `AppTest`가 스크립트를 새로 실행하기 때문
- 일자 : 2026-08-08
- 유형 : 계약문제
- 상황 : `monkeypatch.setattr(app_main, "extract_text_from_image", fake)`로 추출기를 갈아끼웠는데 **안 걸렸다.** 오프라인 테스트가 실제 vision API를 왕복했고(테스트 1건에 19.7초), 픽스처 스크린샷의 전문이 그대로 추출돼 돌아왔다. 원인은 `AppTest.from_file`이 `app/main.py`를 **별도 스크립트 모듈로 새로 실행**한다는 것이다 — 이미 import된 `app.main` 객체와 다른 네임스페이스라 거기 건 패치가 닿지 않는다.
- 조치 : **원본 모듈**(`llm.vision`)을 갈아끼운다. 스크립트는 매 실행마다 `from llm.vision import …`를 다시 하므로 원본 모듈의 속성을 바꾸면 걸린다. 순수 계층(`ingest_jd_image`)은 import된 `app.main`을 통해 직접 부르므로 **양쪽 다** 갈아끼웠다. `graphs.session.resume_or_start`를 감시하는 테스트도 같은 이유로 원본 모듈 쪽에 걸었다.
- **D43과 같은 결함이 다른 경로로 재발했다.** D43은 "오프라인이 실 임베딩 API를 타는 걸 발견해 스텁 2곳 보강"이었고, 이번엔 `AppTest` 경로였다. `install_stubs`가 처음부터 `analysis_nodes`(원본 모듈)를 갈아끼운 것은 우연이 아니라 이 제약 때문이며, 그 이유가 어디에도 안 적혀 있어서 같은 함정을 다시 밟았다.
- **판별법** : 오프라인 테스트의 소요 시간을 본다. 스텁이 걸렸으면 이 파일은 3~4초, 실 API를 타면 20초를 넘는다. 갑자기 느려진 오프라인 테스트는 성능 문제가 아니라 **스텁이 새고 있다는 신호**다.
- 후속 : `AppTest`를 쓰는 카드는 스텁을 **원본 모듈에** 걸 것. `app.main`에 거는 패치는 `ingest_jd_image` 같은 직접 호출에만 유효하다.

### [D50] T15b · 뮤테이션 3종이 살아남아 테스트를 고쳤다 — "안 불렀다"를 예외로 증명하면 안 된다
- 일자 : 2026-08-08
- 유형 : 선택결정
- 상황 : 뮤테이션 10종 중 3종이 살아남았다.
  | 생존 뮤테이션 | 왜 안 잡혔나 |
  | --- | --- |
  | MIME 검증 제거 | 접근하면 던지는 가짜 클라이언트로 "API 안 탄다"를 증명했는데, 그 예외가 **어댑터의 재시도 루프에 삼켜져** `VisionError`로 둔갑했다. `pytest.raises(VisionError)`가 그대로 통과한다 |
  | 용량 상한 제거 | 위와 같음 |
  | `reset_thread`가 JD 본문을 안 지움 | `JD_TEXT_KEY`는 **위젯 키**라, 완료 화면에서 그 위젯이 안 그려지는 동안 Streamlit이 알아서 폐기한다 — pop이 없어도 비어 보인다 |
- 조치 : ① "안 불렀다"를 예외가 아니라 **호출 기록**(`client.responses.calls == []`)으로 본다. 예외를 삼키는 계층이 사이에 있으면 예외 기반 증명은 성립하지 않는다. ② `reset_thread`가 **혼자 책임지는 것은 위젯 키가 아닌 `IMAGE_DIGEST_KEY`**임을 알아내고, 같은 스크린샷을 새 분석에서 다시 올려 재추출되는지 보는 테스트를 추가했다. 재수정 후 **10종 전부 잡힘.**
- **`JD_TEXT_KEY`의 pop은 남겨 뒀다.** Streamlit의 위젯 상태 폐기에 기대는 것보다 명시적으로 지우는 편이 안전하고, 그 줄이 없어도 통과한다는 사실 자체는 여기 적어 둔다 — 나중에 "테스트가 없으니 지워도 된다"고 판단하지 않도록.
- 후속 : 없음.

### [D51] T09 · 브라우저 실 API 관통 완료 — D26·D31·D35·D41이 한 번에 닫혔다
- 일자 : 2026-08-08
- 유형 : 부채완료
- 상황 : T09(2026-08-05) 이래 **"실 API로 UI를 한 번 관통시키는 것"**이 부채로 남아 있었다. 발생 당시 사유는 크레딧 소진(429, D26)이었으나, D41에서 그게 표면이었음이 드러났다 — `AppTest`는 스크립트를 인프로세스로 돌려 ASGI·HTTP 경로를 아예 타지 않으므로, 이 부채의 실체는 "API 키 문제"가 아니라 **HTTP 계층 전체가 미검증**이라는 것이었다. 실제로 streamlit 1.61.0 + starlette 1.4.0이 gzip 미들웨어에서 죽어 브라우저 접속이 100% 실패하는 동안에도 오프라인 296건은 전부 초록불이었다.
- 조치 : 사용자가 `streamlit run app/main.py`를 브라우저에서 열어 관통을 수행했다. **테스트 픽스처가 아니라 실제 채용공고**(현대오토에버 2026 하반기 신입공채)의 JD **이미지**를 업로드해 추출 → 분석 → 결과 카드까지 도달하는 것을 확인했다. 코드 변경 없음. T09를 ⚠️ → ✅로 올렸다.
- **덤으로 T15b의 실사용 검증까지 됐다.** 이 관통은 붙여넣기 경로가 아니라 **이미지 경로**로 이뤄졌다 — D42가 "실사용에서 발견된 P0 입력 경로의 구멍"으로 T15b를 신설한 그 시나리오 그대로이며, 신설 첫 시도에서 통했다. D48(업로더를 폼 밖으로 뺀 결정)이 브라우저에서 실제로 옳았다는 확인이기도 하다 — 폼 안이었다면 여기서 "JD 본문을 입력해야 합니다"로 막혔을 것이다.
- **교훈은 부채가 닫혀도 유효하다.** `AppTest` 초록불은 브라우저 동작의 증거가 아니다(D41·D48이 서로 다른 층에서 같은 말을 했다). 화면을 바꾸는 카드는 오프라인이 다 통과해도 브라우저를 한 번 열어 볼 것.
- 후속 : 없음. **P0~P2에 ⚠️가 하나도 남지 않았다.**

### [D52] T16 · 한 인자로 ②③을 가르는 법 + 수집 실패를 예외가 아니라 **빈 문서**로
- 일자 : 2026-08-08
- 유형 : 선택결정
- 상황 : 카드 T16의 층 표(①붙여넣기/지정URL ②회사 채용페이지 ③플랫폼 상세페이지)와 계약의 시그니처(`fetch_jd_body(url_or_text: str)`)가 어긋난다. 인자가 하나뿐이라 **"이 URL이 ②인지 ③인지"를 판별할 근거가 함수 안에 없다.** 계약 독스트링은 3층을 "정적 파싱 → 렌더링 → 수동 입력 유도"라는 또 다른 정의로 적어 두어 세 번째 서술까지 있었다.
- 조치 : ① 층은 **URL 호스트로 판별**한다 — 알려진 채용 플랫폼(`PLATFORM_HOSTS`)이면 ③, 그 밖이면 회사 자체 페이지로 보고 ②. 붙여넣기(공백이 섞였거나 http(s)가 아닌 입력)는 ①. ② 설계도가 ①에 "지정 URL"을 넣은 것은 **발견 과정을 안 거쳤다**는 뜻이지 스크래핑 본문이 전문임을 보장한다는 뜻이 아니므로, 가져온 본문에는 상이 아니라 중/하를 준다. 붙여넣기만 상이다. R1을 건드리지 않았다 — 계약의 시그니처·타입은 그대로다.
- **수집 실패를 예외로 표현하지 않는다.** robots 차단·404·본문 부족·주입된 fetcher의 생예외까지 전부 `raw_text=""` + 신뢰도 하 문서로 돌려준다(`is_uncollected()`가 판별자). 카드 불변식이 "②③ 실패는 조용히 스킵, 예외 전파 금지"이고, 실패를 예외로 두면 상류(T18 수집 루프)가 매 호출을 try/except로 감싸야 한다. 계약 반환형이 `SourceDocument`(Optional 아님)라 이 표현이 유일한 선택지이기도 하다.
- **T25 주의** : 하드 게이트는 JD **건수**가 아니라 `is_uncollected()`가 거짓인 문서 수를 세야 한다. 빈 문서를 1건으로 세면 게이트가 헛통과한다.
- **새 의존성 없음.** HTTP는 `urllib`, HTML→텍스트는 `html.parser`로 직접 짰다. beautifulsoup4·trafilatura가 추출 품질은 낫지만, ②③은 설계상 best-effort("제품이 의존하지 않음")라 백본 아닌 경로를 위해 의존성을 늘릴 값이 없다고 봤다. 품질이 문제가 되면 그때 `read_html()` 하나만 갈아끼우면 된다 — 경계는 그렇게 잡아 뒀다.
- 키워드 추출은 **라틴 문자 토큰만** 본다. 한국어는 형태소 분석기 없이 자르면 조사 붙은 쓰레기가 섞이는데, 지금 `keywords`를 소비하는 하류 모듈이 없어(역량 추출은 `raw_text`를 본다) 사전을 들일 값이 없다.
- 후속 : **이 카드가 만든 것을 아직 아무도 부르지 않는다**(§2-1 체크). 소유 파일이 `tools/`·`tests/` 둘뿐이라 배선이 카드 범위 밖이며, 예정된 소비처는 T18(수집 서브에이전트)이다. T18 착수 시 소유 파일에 `graphs/analysis_graph.py`를 추가할 것 — T14가 만들고 아무도 안 부른 채 남았던 그 구멍이다(D39·D43).

### [D53] T16 · 실 HTTP 왕복 **한 번**이 오프라인 75건이 못 잡은 결함을 잡았다 — gzip
- 일자 : 2026-08-08
- 유형 : 계약문제
- 상황 : 오프라인 75건이 전부 초록불인 상태에서 실제 채용 페이지(`python.org/jobs/`)를 한 번 가져와 봤다. 본문 8,287자가 **통째로 깨져** 있었고 제목은 `'  ...'`이었다 — 응답이 gzip이었는데 압축을 안 풀고 utf-8로 디코드한 것이다. `urllib`은 기본이 `Accept-Encoding: identity`지만 CDN이 그냥 gzip으로 준다.
- **왜 못 잡았나** : 오프라인 테스트는 fetcher를 주입점으로 갈아끼우므로 **바이트 계층을 아예 지나가지 않는다.** 주입된 가짜는 처음부터 `str`을 돌려준다. D41(`AppTest`는 ASGI·HTTP를 안 탄다)·D48(폼 배칭을 모사 안 한다)·D49(스크립트를 새로 실행한다)와 **같은 종류의 맹점이 네 번째 층에서 반복**됐다.
- 조치 : `decode_body()`를 분리해 ① `Content-Encoding`이 gzip/deflate면 풀고, ② 헤더가 없어도 **매직 바이트 `1f 8b`**가 보이면 푼다(CDN이 헤더를 흘리는 경우가 있다), ③ 헤더에 문자셋이 없으면 `<meta charset>`을 본다(한국어 채용 페이지는 euc-kr이 흔하다), ④ 없는 인코딩 이름을 부르면 utf-8로 떨어진다. 압축 해제는 `decompressobj`라 `MAX_BYTES`에 잘린 스트림도 있는 만큼 건진다. **`br`(brotli)는 표준 라이브러리로 못 푸니 요청 헤더에서 아예 광고하지 않는다** — 처리 못 할 인코딩을 광고하면 서버가 그걸로 준다.
- 재확인 : 같은 URL이 6,742자 본문 + 제목 `'Python Job Board | Python.org'` + 신뢰도 중(②층)으로 정상 복귀. 대조로 던진 플랫폼 URL(사람인, 없는 `rec_idx`)은 HTTP 404 → **조용히 스킵**되어 미수집 문서로 돌아왔다 — ③층이 죽어도 제품이 안 죽는다는 것의 실물 확인이다.
- 후속 : **네트워크·파일·프로세스 경계를 새로 만드는 카드는 주입점 뒤가 아니라 그 경계를 실제로 한 번 지나가 볼 것.** 이 카드는 UI가 아니라 "브라우저를 열어 보라"가 안 통했고, 대신 실 URL 1건이 그 역할을 했다.

### [D54] T16 · 뮤테이션 24종 — 1종 생존(예외를 삼키는 계층이 앞에 하나 더 있었다)
- 일자 : 2026-08-08
- 유형 : 선택결정
- 상황 : 뮤테이션 24종 중 **M09(`fetch_jd_body`의 `except Exception`을 `except FetchError`로 좁히기)**가 살아남았다. 원인은 테스트가 쓴 `exploding_fetcher`가 **robots.txt부터** 터진다는 것 — `robots_allows()`가 자체 방어로 그 예외를 먹고 False를 돌려주므로, 페이지 요청 경로까지 아예 가지 않았다. 실패 문서는 나오니 테스트는 통과한다.
- 조치 : robots는 정상 응답하고 **페이지에서만** 생예외를 던지는 fetcher를 추가했다(`test_page_fetch_raising_an_unexpected_exception_is_still_silent`). 페이지 요청이 실제로 나갔다는 것(`COMPANY_URL in fetcher.calls`)까지 같이 본다. 재수정 후 **24종 전부 잡힘**(gzip·charset 6종 포함).
- **D50과 같은 교훈의 변주다.** 그때는 "예외를 삼키는 계층이 사이에 있으면 예외 기반 증명이 성립하지 않는다"였고, 이번엔 **방어 계층이 두 겹이라 뒤쪽 계층이 테스트에 노출되지 않았다.** 실패 주입은 "어디서 실패하는가"까지 지정해야 한다.
- 후속 : 없음.

### [D55] T17 · 소프트 요건의 실패는 **빈 리스트**, 그리고 "missing 라벨"을 새로 짓지 않은 이유
- 일자 : 2026-08-09
- 유형 : 선택결정
- 상황 : 카드 불변식이 "수집 실패 시 빈 리스트 반환 + `gate_status["missing"]`에 항목 추가"인데, 계약 시그니처는 `fetch_tech_blog(company) -> list[SourceDocument]`라 **상태를 만질 방법이 함수 안에 없다.** T16이 같은 자리에서 겪은 문제의 변주다(D52).
- 조치 : ① 실패 표현은 T16과 **다르게** 갔다. 거기는 반환형이 `SourceDocument`(Optional 아님)라 빈 본문 문서 + `is_uncollected()` 판별자가 유일한 선택지였지만, 여기는 반환형이 `list`라 **빈 리스트가 곧 미수집**이다. 판별자를 새로 만들지 않았다. ② missing 기록은 순수 함수 `missing_soft_sources(tech_blog, values) -> list[str]`로 뺐다 — 수집 결과에서 라벨을 유도하므로 호출자가 기억할 상태가 없고, 상류(T18)가 그 목록을 `gate_status["missing"]`에 그대로 넣으면 된다.
- **라벨 어휘를 새로 만들지 않았다.** `nodes/analysis_nodes.py`의 `source_coverage()`가 이미 `SourceType`의 값("기술블로그"·"인재상")으로 `BriefMeta.missing_sources`를 채우고 있다. 여기서 "기술블로그 미수집" 같은 문구를 지어내면 화면에 표기가 두 벌 생긴다. 뮤테이션 M22가 정확히 그 회귀다.
- **표지어(`_MARKERS`) 판별을 넣었다** — 주워 온 게 엉뚱한 페이지면 안 가져온 것만 못하다. 회사 사이트가 404 안내나 홈을 200으로 돌려주는 일이 흔한데, 그걸 "인재상"으로 접수하면 하류가 없는 근거로 판단하게 된다. "근거 없이 지어내지 않는다"를 수집 단계에 적용한 것이다.
- **통짜 대형 문서는 청킹이 아니라 절단으로 막았다.** 카드가 "컨텍스트 한계를 넘으면 **그때만** RAG 청킹"이라 했고, 지금 필요한 건 청킹이 아니라 컨텍스트를 통째로 안 날리는 것이다(소프트 요건은 보조 근거라 앞부분으로 족하다). **줄 경계에서 자르고 잘린 자리에 표식을 넣지 않는다** — 원문에 없는 문장이 `raw_text`에 섞이면 evidence.quote 대조가 그걸 인용할 수 있다.
- HTTP·robots·HTML→텍스트·키워드는 **T16 것을 import**했다(`tools.fetch_jd`). 같은 걸 다시 짜면 gzip 결함(D53) 같은 게 한쪽에만 고쳐진다. 다만 robots.txt는 호출당 호스트별로 한 번만 읽도록 얇은 캐시를 씌웠다 — 준수하려고 읽는 파일로 서버를 더 두드리면 앞뒤가 안 맞는다.
- 새 의존성 없음. 최소 본문은 150자로 T16(200자)보다 낮췄다 — 인재상 페이지는 JD보다 짧아서 200자를 그대로 쓰면 다 버려진다.
- 후속 : **이 카드가 만든 것도 아직 아무도 부르지 않는다**(§2-1 체크). T16과 같은 이유(소유 파일이 `tools/`·`tests/` 둘뿐)이고 소비처도 같은 **T18**이다. T18에서 `fetch_jd_body`·`fetch_tech_blog`·`get_company_values`를 한꺼번에 배선할 것.

### [D56] T17 · 실 URL 확인이 또 잡았다 — apex는 죽고 `www.`만 사는 사이트, 그리고 경로 추측의 한계
- 일자 : 2026-08-09
- 유형 : 계약문제
- 상황 : 오프라인 71건이 전부 초록불인 상태에서 실제 회사 몇 곳을 한 번 지나가 봤다(§5 수집 규약 ④). **현대오토에버가 후보 14개 전부 "robots 차단"**으로 나왔다. 파 보니 차단이 아니라 `hyundai-autoever.com`이 **인증서 호스트명 불일치로 TLS에서 죽는** 것이었고 — `robots_allows()`는 상태 코드를 모르는 실패를 보수적으로 금지 처리한다(RFC 9309, D52) — `www.hyundai-autoever.com`만 살아 있었다. 한국 대기업 사이트에 흔한 모양이라 apex만 두드리면 그 회사는 통째로 미수집이 된다.
- 조치 : `live_host(domain, fetch)`를 넣어 `example.com`과 `www.example.com` 중 **응답하는 쪽**을 고른다. **판별은 robots.txt 응답으로 한다** — 어차피 후보마다 읽어야 하는 파일이고 캐시가 재사용하므로 **요청이 한 건도 안 는다.** HTTP 상태가 돌아왔으면(404 포함) 서버는 살아 있는 것이고, 상태 없는 실패(DNS·TLS·타임아웃)만 "이 호스트는 없다"로 읽는다. 서브도메인 후보(`tech.example.com`)는 그 자체로 다른 호스트라 영향받지 않는다.
- **더 큰 것은 경로 추측의 한계였다.** 관례(`tech.{도메인}`·`/careers`·`/culture`…)로 찾아지는 곳은 우아한형제들(`techblog.woowahan.com`) 정도고, 한국 대기업 CMS(`/kor/…do`)나 **다른 등록 도메인에 사는 기술블로그**(`toss.im` → `toss.tech`, `kakaocorp.com` → `tech.kakao.com`)는 영영 못 찾는다. → `CompanySite.tech_blog`/`values`에 절대 URL을 넣을 수 있게 뒀고, **거기 박은 주소는 전부 실제로 한 번 가져와 본문이 나오는 것만 남겼다**(카카오·토스·라인). 추측한 주소는 하나도 안 적었다 — 빗나간 주소는 조용한 실패라 티가 안 나므로 사전이 조용히 썩는다. 레지스트리는 **씨앗이지 회사 사전이 아니며**, 없는 회사는 호출자가 URL·도메인을 그대로 넘기거나 `sites=`로 주입한다.
- **큐레이션한 주소는 신뢰도 중으로 친다.** 관례로 찾은 게 아니라 사람이 확인한 주소라서다. 단 GitHub·Medium 같은 외부 호스팅은 확인했어도 하다(`EXTERNAL_HOSTS`).
- 확인 결과 : 카카오 `tech.kakao.com` 1,708자 / 토스 `toss.tech` 1,772자 / 우아한형제들 `techblog.woowahan.com` 2,000자 — 전부 신뢰도 중. **현대오토에버는 고친 뒤에도 0건**이다. 홈이 JS 렌더링이라 정적 본문이 0자이고 인재상 페이지 경로를 정적으로 못 찾는다. **이게 정상 동작이다** — 소프트 요건이 없으면 `missing`에 표기되고 제품은 그대로 돈다. 억지로 채우려 들면 아무 페이지나 근거로 삼게 된다.
- **T16 품질 한계 하나** (그쪽 소유라 안 고쳤다) : `read_html`이 인라인 `<a>`를 블록으로 안 쳐서 기술블로그 **목록 페이지**가 링크 수프로 나온다("AIAPPAWSBackendCulture…"). 개별 글까지 따라 들어가지 않는 지금 구조에서는 소프트 문서의 본문 품질이 딱 이 정도다. 역량 추출에 쓸 만한지는 T18 배선 뒤에 실물로 볼 것.
- 후속 : D41·D48·D49·D53에 이어 **다섯 번째다** — 주입점 뒤에 갇힌 오프라인 테스트는 그 경계 밖을 아예 안 본다. 이번엔 바이트 계층이 아니라 **DNS·TLS와 남의 사이트 URL 관례**가 그 밖이었다.

### [D57] T17 · 뮤테이션 35종 — 생존 6종이 전부 "테스트가 우연히 통과하던 자리"였다
- 일자 : 2026-08-09
- 유형 : 선택결정
- 상황 : 1차 28종에서 6종이 살아남았다. 셋은 진짜 구멍, 셋은 동치 뮤테이션으로 갈렸다.
- 진짜 구멍 : ① **줄 경계 절단**(`_truncate`가 줄 가운데서 잘라도 통과) — 검증이 `endswith("본문입니")` 같은 우연한 문자열이었다. 마지막 줄이 **원문의 완전한 한 줄인지**를 보도록 고쳤다. ② **조회 순서**(URL·도메인이 사전보다 먼저) — 테스트가 쓴 사전에 도메인 키가 없어 순서를 바꿔도 결과가 같았다. 낡은 도메인이 박힌 사전을 넘겨 순서를 못 박았다. ③ **`resolve_site`의 빈 입력 가드** — 빈 키를 가진 사전을 주면 빈 문자열이 회사로 둔갑한다.
- **가장 배울 게 있던 건 `collected_at`이다.** 주입한 수집일을 무시하고 `date.today()`를 쓰는 회귀가 살아남았는데, 원인은 테스트의 `TODAY`를 **하필 그날 날짜로** 잡아서였다. 오늘 하루는 통과하고 내일부터 잡힌다 — 시간에 의존하는 테스트의 전형적인 함정이라 `TODAY`를 오늘이 아닌 날짜로 바꿨다. `tests/test_fetch_jd.py`에도 같은 모양(`TODAY = date(2026, 8, 8)`)이 남아 있으나 그쪽은 T16 소유라 손대지 않았다.
- 동치 뮤테이션 : `limit <= 0` 가드는 원래 루프 첫 바퀴가 막고 있어 지웠다가, `live_host` 탐색이 들어오면서 **실제로 요청을 막는 의미**가 생겨 되살렸다(그 뒤로는 뮤테이션이 잡힌다).
- 결과 : 보강 후 **35종 전부 잡힘**(www 폴백·큐레이션 신뢰도 등 2차 추가분 5종 포함). 오프라인 **78건**.
- 후속 : 없음.

### [D58] T18 · "ReAct 서브에이전트"에서 **정책만** 규칙으로 갈랐다 — 그리고 재시도 각도를 근거에서 얻는다
- 일자 : 2026-08-10
- 유형 : 선택결정
- 상황 : 카드 T18은 이 노드를 "이 시스템에서 유일하게 자율성이 허용되는 곳"이라 부르고, 근거로 "검색어를 바꿔가며 찾고, 결과가 부실하면 다른 각도로 재시도하고, 언제 충분한지 스스로 판단"을 든다. 그런데 **실제로 배선할 도구를 놓고 보면 LLM이 고를 것이 없다.** 노출된 도구는 셋이고 전부 멱등이며 인자는 회사 식별자 하나다. 유형별 URL 후보 열거와 재시도는 이미 `candidate_urls()`·`MAX_ATTEMPTS` 안에 있다(T17). LLM이 실질적으로 바꿀 수 있는 것은 **인자, 즉 회사 주소**뿐인데 그게 하필 D56이 "추측해서 적지 말 것"이라고 못 박은 바로 그 값이다.
- 조치 : ReAct의 **구조**(관측 → 행동 선택 → 실행 → 관측, 언제 충분한지 스스로 판단)는 그대로 두고 **정책만 규칙**으로 짰다. `create_react_agent`도 쓰지 않았다 — DEVPLAN §5 "스택 판단"이 예고한 재판단 지점이고, 결론도 거기 권장과 같다(`BaseChatModel`을 요구해 `langchain-openai`를 끌고 오며, 카드 요구인 "예산 소진 = 부분 수집(실패 아님)"을 `recursion_limit` 예외로밖에 표현 못 한다).
- **대신 "다른 각도로 재시도"는 진짜로 넣었다 — 단 각도를 근거에서 얻는다.** 회사명으로 못 찾으면 *이미 수집한 JD 문서의 주소에서 뽑은 도메인*으로 한 번 더 두드린다(`collection_angles()`). 지어낸 주소가 아니라 수집 결과에 실려 온 주소라 D56을 어기지 않는다. **실 URL 확인에서 이게 실제로 값을 냈다** — 회사명 "파이썬소프트웨어재단"은 `KNOWN_SITES`에 없어서 사전 조회로는 0건인데, JD URL(`www.python.org/jobs/`)에서 얻은 `python.org`로 기술블로그(`blog.python.org`, 1,748자)와 인재상(`python.org/about`, 4,535자)을 둘 다 찾았다. 이 각도가 없었으면 소스 충족률이 1/3에서 3/3으로 못 올라간다.
- **무한 루프를 막는 것은 예산이 아니라 `tried` 집합이다.** 도구는 실패를 예외가 아니라 빈 결과로 표현하므로(D52·D55), 실패를 근거로 재시도하는 정책이면 같은 호출을 영원히 반복한다. 예산은 그걸 12회에서 끊어 줄 뿐 **끝냈다고 말할 수 없다.** 그래서 `(도구, 인자)` 쌍을 기록해 두 번 부르지 않으며, 행동 후보가 유한하므로 예산이 남아도 루프가 끝난다. 뮤테이션 M10·M23이 이 자리를 지킨다.
- **소진 판정은 "남은 예산 0"이 아니라 "예산이 0인데 할 일이 남았다"다.** 마지막 한 칸을 정확히 쓰고 끝난 것을 소진으로 적으면 게이트가 거짓을 말하고 T25가 멀쩡한 수집을 강등한다(M06).
- 후속 : 자율성이 실제로 필요해지는 지점은 **T19(`discover_jobs`)**다. 거기서는 "어느 공고가 이 직무인가"를 골라야 해서 고를 것이 실제로 있다. 그때 이 노드에 LLM 정책을 얹을지, T19가 자기 노드를 갖는지는 그 카드에서 판단할 것 — 이 결정은 "T18의 도구 셋에는 고를 게 없다"까지만 말한다.

### [D59] T18 · 예산 소진을 적을 칸이 계약에 없다 + JD 문서를 **교체**하기로 한 것의 파장
- 일자 : 2026-08-10
- 유형 : 계약문제
- 상황 : 카드 불변식이 "예산 소진 시 부분 수집으로 진행 + `gate_status`에 기록"인데 **`GateStatus`(contracts/state.py)에 그걸 담을 칸이 없다.** 필드는 `jd_count`·`hard_gate_passed`·`reliability`·`missing` 넷뿐이고 계약은 수정 금지다(R1).
- 조치 : `missing`에 `BUDGET_EXHAUSTED_LABEL`("수집 예산 소진")을 넣고 판별자 `budget_exhausted(gate_status)`를 함께 뒀다. 대안 둘을 버린 이유 — ① `reliability`에 세 번째 값을 넣으면 `BriefMeta.reliability`("정상"|"추정 기반")와 어휘가 갈라진다. ② 계약에 없는 키를 dict에 몰래 추가하는 것은 TypedDict라 런타임에는 통하지만 계약의 조용한 확장이다. **D55가 경계한 "표기 두 벌" 문제는 여기선 안 생긴다** — `gate_status["missing"]`은 화면에 안 그려지고, 브리프의 `missing_sources`는 `build_brief`가 `source_docs`에서 따로 계산한다. 소스 유형 라벨 자체는 `missing_soft_sources()`가 준 것을 그대로 쓴다(어휘는 `SourceType` 값).
- **신뢰등급 규칙은 새로 정하지 않고 `build_brief_meta`(tools/brief.py) 것을 그대로 옮겼다** — JD가 없으면 강등, 소프트 요건 부재는 강등 사유가 아니다(§12-1 하드 요건). 게이트와 브리프 머리말이 서로 다른 등급을 말하면 화면이 자기모순에 빠진다. 뮤테이션 M09가 이 일치를 지키며, 테스트가 두 값을 직접 대조한다.
- **JD 문서는 추가가 아니라 교체다.** P0의 `ingest_pasted_jd`는 입력이 URL이어도 **그 URL 문자열을 본문으로** 삼는데, 그 문서가 살아남으면 `extract`가 URL 한 줄에서 역량을 뽑으려 든다. 그래서 붙여넣기든 URL이든 `fetch_jd_body`를 한 번 통과시키고 그 결과를 정본으로 둔다 — ①층(붙여넣기)은 네트워크를 안 타므로 기존 경로는 비용도 동작도 그대로다(양쪽 다 `.strip()`까지 같다). 이로써 **JD 문서의 정의 지점이 하나**가 된다(`CONFIDENCE_BY_LAYER`, D52).
- **파장 — 배선 하나가 남의 테스트 5건을 깼다.** `NODE_SEQUENCE`에 노드를 한 줄 넣자 `tests/test_graph_smoke.py` 3건(하드코딩된 노드 목록 / `PASTED_DOC_ID`가 살아남는다는 전제 / 노드 함수를 `analysis_nodes`에서 `getattr`로 찾는 루프)과 `tests/test_progress.py` 2건("`ingest_pasted_jd` 다음 줄은 `extract`"라는 전제)이 죽었다. 둘 다 T18 소유가 아니지만 §2-1이 배선 공유 파일에 준 예외의 직접적 결과이고 T14b가 같은 편집을 한 전례가 있어(그때도 `test_graph_smoke.py`의 목록에 "retrieve"를 넣었다) **최소 편집으로 고쳤다.** 고치면서 두 곳은 전제 자체를 없앴다 — 노드 함수는 `node_sequence()`가 들고 있는 것을 그대로 부르고(노드가 전부 한 모듈에 살지 않는다), "다음 줄"은 `WIRED_NODES[1]`로 배선에서 유도한다. 다음에 노드가 하나 더 끼어도 이 둘은 안 깨진다.
- **남긴 것 하나 — `ingest_pasted_jd`는 이제 산출이 전부 버려진다.** `collect`가 JD 문서와 `selected_job_ids`를 다시 만들기 때문이다. 네트워크를 안 타고 마이크로초라 해롭지는 않지만, 부르는데 아무도 안 쓰는 노드는 §2-1이 경계한 구멍의 뒷면이다. **`nodes/analysis_nodes.py` 소유자가 걷어낼 것** — 이 카드의 경계가 아니라 손대지 않았다(진행 표시 줄 하나가 사라지는 변화이기도 하다).
- 후속 : T25는 `budget_exhausted()`로 읽을 것. `missing` 문자열을 직접 비교하지 말 것.

### [D60] T18 · 뮤테이션 26종 — 생존 2종이 각각 "테스트가 순수 함수만 봤다"와 "죽은 가드"였다
- 일자 : 2026-08-10
- 유형 : 선택결정
- 상황 : 1차에서 24/26. 생존 둘의 성격이 정반대였다.
  | 생존 | 정체 |
  | --- | --- |
  | M16 — 노드가 `selected_job_ids`에 빈 문서까지 넣음 | **진짜 구멍.** 순수 함수 `collected_job_ids()`는 직접 검증했는데, **노드가 그 함수를 안 쓰는 회귀**는 아무도 안 봤다 |
  | M13 — 음수 예산 보정 제거 | **동치 뮤테이션.** `while left > 0`이 이미 걸러 내고 소진 경로 반환값이 `0` 고정이라 밖으로 샐 길이 없다 |
- 조치 : M16은 **노드를 통과하는** 테스트를 추가했다(`test_node_reports_no_job_id_when_the_url_could_not_be_fetched`). 실패는 `http_get`을 터뜨려 만들어 T16 본체를 실제로 지나가게 했다 — "실패하면 빈 문서"(D52)가 노드까지 이어지는지가 함께 확인된다. M13은 **가드를 지웠다.** 죽은 가드를 남기면 다음 사람이 그게 뭔가를 막고 있다고 믿는다(D57의 `limit <= 0`과 같은 판단이며, 그때와 달리 되살아날 근거가 안 보인다). 보정은 원래 두 군데(`_budget_of`·`collect_sources`)에 있었는데 둘 다 죽어 있었다.
- **교훈은 M16 쪽이다.** 순수 계층을 분리해 놓으면 그 계층은 잘 검증되는데 **호출부가 그 계층을 계속 쓰는지**는 별도 테스트가 아니면 아무도 안 본다. T15가 `brief_items()` parity로 같은 문제를 다뤘고(D44), 여기서는 "노드를 통과시켜 본다"가 그 역할을 한다.
- 결과 : 재수정 후 **25/25 전부 잡힘**(M13은 대상 코드가 사라져 목록에서 뺐다). 오프라인 **573 passed(9.2초)**, 그중 T18이 39건.
- 후속 : 없음.

### [D61] T18 · 실 URL 확인 — 배선된 노드가 살아 있는 사이트 셋을 지나갔다
- 일자 : 2026-08-10
- 유형 : 부채완료
- 상황 : DEVPLAN §5 수집 규약 ④("네트워크 경계를 새로 만들면 실 URL로 한 번 지나가 볼 것")를 적용했다. 엄밀히 이 카드는 새 경계를 만들지 않았지만(HTTP는 T16·T17 것을 그대로 쓴다) **배선은 새것**이고, 오프라인이 전부 초록불인데 실물이 안 도는 사고가 이 저장소에서 다섯 번 반복됐다(D41·D48·D49·D53·D56).
- 결과 : LLM은 한 번도 안 탔다 — 수집만 도는 경로라 비용이 0이다.
  | 입력 | tool call | 결과 |
  | --- | --- | --- |
  | 회사 "카카오" + 붙여넣은 JD | 3 (예산 9 남음) | JD(상) + 기술블로그 `tech.kakao.com` 1,708자(중). 인재상 `missing`, 신뢰등급 정상 |
  | 회사 "현대오토에버" + 붙여넣은 JD | 3 | JD(상)만. 기술블로그·인재상 `missing` — **D56이 적은 대로가 정상 동작**이다 |
  | **URL만** `www.python.org/jobs/` + 사전에 없는 회사명 | 5 (예산 7 남음) | JD 6,713자(중, ②층) + 기술블로그 1,748자 + 인재상 4,535자. `missing` **비었다** |
- **세 번째 줄이 이 카드의 두 가지를 한꺼번에 증명한다.** ① URL 입력 경로가 실제로 돈다 — 화면은 아직 붙여넣기·이미지만 받지만 `raw_jd_input`에 URL을 넣으면 그대로 통한다(UI 변경 없이 열린 경로다). ② **두 번째 각도가 값을 냈다** — 회사명이 사전에 없어 회사명 각도는 0건인데, JD 주소에서 얻은 `python.org`로 소프트 요건 둘을 다 찾았다(D58).
- 후속 : 없음. 브라우저 관통은 이 카드가 화면을 안 건드려서 대상이 아니다 — 다만 **URL 입력창을 붙이는 카드는 브라우저를 한 번 열어 볼 것**(D41·D51).

### [D62] T19 · H1이 `app/hitl.py`를 고치게 만들었다 — "안 고쳐도 된다"는 낙관이 틀렸다
- 일자 : 2026-08-10
- 유형 : 계약문제
- 상황 : T12가 `app/hitl.py`에 적어 둔 말은 **"이 모듈이 아는 것은 `{텍스트, 선택지 유무}` 뿐이고, T19·T22가 전혀 다른 페이로드를 보내도 렌더링 코드를 고치지 않는다"**였다. 실제로 대부분 맞았다 — `KIND_TITLES`에 `job_selection` 자리가 이미 있었고, 페이로드 정규화·폼 구성·재개 값 조립까지 한 줄도 안 고쳤다. **딱 한 곳이 틀렸다:** 선택지가 있으면 무조건 `st.radio`(단일 선택)로 그리는데, H1은 §9-3이 **multiselect**를 명시하고 카드 완료 조건도 "multiselect 선택 후 재개"다. 라디오로는 표현이 안 된다.
- 조치 : **사용자 판단을 받아 `app/hitl.py`를 확장했다**(R2 예외 — D29·D33·D43과 같은 모양). 고친 것은 셋뿐이다. ① `FormQuestion.multi` 플래그 ② `_render_widget`의 multiselect 분기 ③ `kind`별 제출 문구(`SUBMIT_LABELS` — 이미 있던 `KIND_TITLES`와 같은 구조). 대안이던 "H1 폼을 `app/main.py`에서 따로 그리기"는 버렸다 — HITL 렌더링이 두 벌이 되고, 카드 불변식("새 HITL 메커니즘을 만들지 말 것")·AGENTS.md("하나의 재개 루프를 재사용한다")와 정면으로 부딪친다.
- **반환 타입 `dict[str, str]`은 지켰다.** 다중 선택을 목록으로 돌려주면 `render_pending()`·재개 값·노드 정규화가 전부 `str | list` 분기를 갖게 되고, 그 분기가 H3(텍스트 답변) 경로까지 번진다. 대신 구분자(`MULTI_SEPARATOR = "\n"`)로 이어 붙였고, **라벨에 줄바꿈이 없다는 것을 만드는 쪽(`label_map()`)이 보장한다** — 라벨은 `" ".join(...split())`로 접힌다. 이 전제가 깨지면 선택 하나가 둘로 쪼개지므로 테스트로 못 박았다.
- **라벨의 유일성이 이 노드의 계약이다.** 위젯이 문자열만 돌려주므로 라벨을 키로 공고를 되짚는 수밖에 없는데, 제목만 같고 부서가 다른 공고는 실제로 흔하다. 겹치면 뒤에 일련번호를 붙인다(뮤테이션 M03).
- 후속 : T22(H4 OCR 보정)는 텍스트 편집이라 이 확장이 필요 없다. 다만 **"페이로드만 바꾸면 된다"를 다시 믿지 말 것** — 위젯 종류가 다르면 위젯 선택 코드는 고쳐야 한다. 고쳐야 하는 범위가 *위젯 한 곳*으로 좁게 유지되는 것이 이 모듈 설계의 값이며, 그건 실제로 지켜졌다.

### [D63] T19 · 중단점이 둘이 됐다 — 배선·안내 문구·"발견은 대화형 전용"
- 일자 : 2026-08-10
- 유형 : 선택결정
- 상황 : T11까지 이 그래프의 중단점은 하나(H3 델타 인터뷰)였고, `node_sequence()`가 `INTERVIEW_AFTER` 인덱스 하나로 삽입 위치를 계산했다. H1이 들어오면서 삽입 지점이 둘이 됐다.
- 조치 : `INTERACTIVE_INSERTS = [(앞 노드, [끼울 노드들])]`로 일반화했다(§2-1 배선 파일). H1은 `ingest_pasted_jd` 뒤 — **`collect` 앞**이어야 고른 공고의 본문이 수집·추출로 그대로 흘러간다.
- **발견(`discover_jobs`)도 비대화형 그래프에는 안 넣었다.** checkpointer 때문만이 아니다 — **물어볼 사람이 없으면 발견한 공고를 시스템이 골라야 하는데 그게 §12-2 금지 사항이다**("임의 선택·병합 금지"). 발견만 하고 아무도 안 고르면 §2-1이 경계한 "만들어졌는데 아무도 안 쓰는" 상태가 되기도 한다. 대화형 전용이 유일하게 앞뒤가 맞는 배치다.
- **안내 문구를 중단점별로 갈랐다.** T12가 쓴 `st.info`는 "판정 근거가 부족한 항목이 있어 잠시 멈췄습니다"인데, 공고 선택 화면에 그게 뜨면 사용자는 무슨 말인지 알 수 없다. `resume_notice()`가 `kind`로 고르고 모르는 중단점은 기본 문구로 떨어진다 — 내부 이름이 화면에 안 새는 것은 T13과 같은 원칙이다.
- **`app/main.py`의 `resume()` 본체는 안 고쳤다.** 폼은 `app/hitl.py`가 페이로드를 보고 그리므로, 중단점이 둘이 돼도 이 함수가 아는 것은 여전히 "중단됐다"뿐이다. T12의 일반화가 여기서 값을 했다.
- **JD 본문을 필수에서 뺐다.** 비우면 발견 경로가 열린다. 다만 `text_area`의 **라벨은 "JD 본문" 그대로 뒀다** — "(선택)"을 붙이면 백본(붙여넣기가 가장 정확하다)을 스스로 깎아내리고, 비워도 된다는 사실은 **비었을 때 보이는** placeholder가 말하는 게 맞다. 라벨을 바꿨다가 T15b 소유 테스트 5건이 깨진 것이 재고 계기였는데, 되돌리고 보니 UX 판단으로도 그쪽이 옳았다.
- 후속 : 발견 0건 + JD 없음이면 빈 브리프가 나온다. 정상 동작이지만(하드 게이트가 "JD 없음"으로 보고한다) **화면에서 그 사실을 제대로 알리는 것은 T25(품질 게이트) 소관**이다.

### [D64] T19 · 실 URL이 또 잡았다 — 공고 스크레이핑의 오탐 두 종
- 일자 : 2026-08-10
- 유형 : 계약문제
- 상황 : 오프라인이 전부 초록불인 상태에서 실제 회사 다섯 곳을 지나가 봤다(§5 수집 규약 ④). **당근이 20건을 돌려줬는데 그중 16건이 "김뚜껑부대찌개 장안점 주5일 주방직원"·"용산마포지역 배달 라이더분"이었다.** 회사 채용공고를 찾는 기능이 동네 구인글을 물어 온 것이다.
- 원인 : 후보 목록의 `jobs.{도메인}`이 당근에서는 **당근알바**(동네 구인 서비스)다. 즉 링크를 잘못 고른 게 아니라 **채용 페이지 자체를 잘못 고른** 것이고, 그 페이지 안에서는 전부 정상적인 "공고 링크"라 표지어로는 한 건도 못 거른다. 등록 도메인도 같아서 도메인 비교로도 못 거른다.
- 조치 : 두 겹으로 막았다. ① **범위 규칙**(`in_scope`) — 공고 링크는 그 채용 페이지와 **같은 호스트**이거나 알려진 ATS·채용 플랫폼이어야 한다. 차단 목록이 아니라 **허용 목록**으로 갔다: 회사의 구인 *서비스*와 회사의 *채용*을 가르는 일반 규칙이 없어서 차단 목록은 다음 회사에서 또 뚫리고, 빗나가도 **덜 가져올 뿐**이라 발견이 크리티컬 패스가 아닌 이상 그 방향이 안전하다(§6-2). 플랫폼 목록은 T16의 `PLATFORM_HOSTS`를 그대로 쓴다 — 층 정의를 두 벌로 만들지 않는다(D55와 같은 이유). ② **처음 건진 곳에서 멈춘다** — 여러 채용 페이지 결과를 합치지 않는다. 후보 순서가 이미 "이게 진짜 채용 사이트일 확률" 순이라, 합치는 것은 덜 믿을 만한 출처를 더 믿을 만한 출처와 같은 급으로 올리는 일이다. 결과: 당근 **20건 → 6건, 전부 `careers.daangn.com`**.
- **오프라인이 먼저 잡은 것도 하나 있다.** 링크 표지를 URL **전체**에 대고 있었는데, 채용 서브도메인(`https://careers.x.com/about`)의 `//careers.`가 경로 표지 `/career`를 만족시켜 **아무 페이지나 공고로 둔갑했다.** 채용 서브도메인은 후보 관례라 이 오탐은 예외가 아니라 **기본값**이었다. 표지는 경로에만 댄다.
- **남은 한계는 고치지 않고 적는다.** ① 당근의 6건은 개별 공고가 아니라 분류 목록 링크다("Tech 24"·"Design 4"). ② **카카오·토스·네이버·쿠팡은 0건이다** — 채용 사이트가 JS 렌더링이라 정적 본문에 링크가 없다(D56의 현대오토에버와 같은 벽). 즉 **한국 대기업에서는 발견이 대체로 0건이고, 그게 정상 동작이다.** 사용자는 JD를 붙여넣으면 되고 폼이 그렇게 안내한다. §6-2가 발견을 크리티컬 패스에서 뺀 판단이 실물로 확인된 셈이다.
- 후속 : 발견 품질을 올리려면 정적 스크레이핑이 아니라 공식 API(T20)나 렌더링 엔진이 필요하다. **T20을 "있으면 좋은 것"으로 둔 §6-2 판단은 이 결과 뒤에도 유효하다** — 없어도 제품은 붙여넣기로 완주한다.

### [D65] T19 · 뮤테이션 29종 — 생존 4종이 전부 "테스트가 그 자리를 안 지나갔다"
- 일자 : 2026-08-10
- 유형 : 선택결정
- 상황 : 1차 25/29. 생존 넷의 성격이 같았다 — 로직이 동치라서가 아니라 **테스트가 그 경로를 밟지 않아서** 살았다.
  | 생존 | 왜 안 잡혔나 |
  | --- | --- |
  | M08 표지를 URL 전체에 댄다 | 오탐을 드러내던 `/about`이 제외 경로에도 걸려서, 어느 쪽이든 결과가 같았다 |
  | M21 다중 선택에서 첫 항목만 보낸다 | `AppTest`가 완주만 확인했다 — 2건 중 1건만 넘어가도 흐름은 끝까지 간다 |
  | M23 자기 링크를 안 거른다 | 픽스처에 `<a href="#">`이 없었다 |
  | M26 제출 문구를 kind와 무관하게 고정 | 버튼 **라벨**을 아무도 안 봤다 |
- 조치 : 픽스처에 `/team`(호스트에만 표지가 있는 링크)과 `<a href="#">`을 넣고, `AppTest`가 **고른 건수**(`len(seen["fetch"]) == 2`)와 **버튼 라벨**을 보게 했다. 재수정 후 **29종 전부 잡힘.**
- **교훈은 M21이다.** UI 관통 테스트가 "끝까지 갔다"만 보면 **양이 틀린 회귀를 통째로 놓친다.** 2건을 골랐는데 1건만 분석해도 다운로드 버튼은 똑같이 뜬다. 관통 테스트에는 반드시 **개수나 내용**을 하나 걸어야 한다(T18의 D60 M16과 같은 모양 — 순수 함수는 봤는데 호출부는 안 봤다).
- 결과 : 오프라인 **644 passed(9.6초)**, 그중 T19가 67건.
- 후속 : 없음.

### [D66] T19 · 오프라인 스텁 누수 여섯 번째 — 429가 날 때까지 초록불이었다
- 일자 : 2026-08-10
- 유형 : 계약문제
- 상황 : T19 작업 중 전체 테스트가 갑자기 `EmbeddingError: RateLimitError`로 깨졌다. 범인은 **T19와 무관한** `tests/test_graph_smoke.py::test_pending_questions_do_not_stop_the_flow`였다 — LLM 도구 넷은 갈아끼우면서 **`retrieve_candidates`만 빼먹어** 실제 임베딩 API를 왕복하고 있었다. T14b가 `retrieve` 노드를 그래프에 넣은 뒤로 계속 그랬고(D43이 "스텁 2곳 보강"할 때 이 한 곳을 놓쳤다), **API가 성공하는 동안은 계속 초록불이었다.**
- 조치 : 그 테스트에 `retrieve_candidates` 스텁 한 줄을 넣었다. §4가 "`uv run pytest -q`는 LLM 없이 통과해야 한다"고 못 박은 정책 위반이라 T19 소유가 아니어도 고쳤다.
- **DEVLOG가 이미 판별법을 적어 뒀는데 그걸 놓쳤다.** D49가 남긴 말은 "오프라인 테스트가 갑자기 느려지면 스텁이 새는 것이다 — 성능 문제가 아니다"였고, 실제로 T18 세션에서 `--durations`를 찍었을 때 이 테스트가 **2.36초로 1등**이었다. 그 숫자를 보고도 넘어갔다. 고친 뒤 최장 테스트는 1.08초다.
- **비용이 조용히 나가고 있었다.** 전체 테스트를 한 번 돌릴 때마다 임베딩을 한 번씩 태웠다.
- 후속 : **그래프를 통째로 돌리는 테스트는 "LLM 도구 4종"이 아니라 `retrieve_candidates`까지 5종을 갈아끼워야 한다.** 노드가 늘면 이 목록도 는다 — `install_stubs`(tests/test_hitl.py)를 쓰면 자동으로 맞지만, 직접 monkeypatch하는 테스트는 매번 빠뜨린다. 새 노드가 네트워크를 타면 **직접 스텁하는 테스트 전부**를 훑을 것.

### [D67] T20 · 착수하지 않는다 — 카드가 스스로 적은 탈출구를 쓴다
- 일자 : 2026-08-10
- 유형 : 선택결정
- 상황 : P3의 마지막 칸이 T20(공식 채용 API 연동)이다. 그런데 카드 본문이 이미 "**미완이어도 제품은 완주해야 한다**", "승인이 지연되면 이 태스크를 `⛔`로 두고 다음 Phase로 진행할 것"이라고 적고 있다. §6-2가 이 카드를 크리티컬 패스에서 뺀 이유는 **키 발급·승인이 통제 불가 요소**이기 때문이다 — 사람인·잡코리아는 실무 승인이 필요하고, 원티드는 3자용 공개 검색 API가 사실상 없다.
- 조치 : 사용자 판단으로 **착수하지 않고 `⛔` 표기.** 다만 카드의 **완료 조건만 떼어내 실행했다** — "API 없이 실행해도 T19가 정상 동작함을 재확인(회귀 방지)"은 승인과 무관하게 지금 할 수 있고 한 줄이면 끝난다. `uv run pytest tests/test_fetch_jd.py -q` → **75건 통과.** ⛔ 표기의 근거를 자기보고가 아니라 검증 명령으로 남긴 것이다(R4의 취지).
- **`⬜`로 두지 않은 이유.** R3가 "완료 표기는 §3 원장 한 곳에서만"이라 못 박았는데, `⬜`로 두면 원장에서 **"아직 안 한 것"과 "안 하기로 한 것"이 구분되지 않는다.** 다음 세션이 P3를 미완으로 읽고 되돌아온다. `⛔`는 §1-4에서 "차단됨"이고, 여기서 차단자는 외부 승인 절차다.
- **D64가 이 판단의 대가를 이미 실측해 뒀다.** T20을 건너뛰면 발견 품질은 낮은 채로 남는다 — 카카오·토스·네이버·쿠팡 0건은 정적 스크레이핑의 벽이라 API나 렌더링 엔진 없이는 안 풀린다. 그래도 제품이 도는 근거는 **붙여넣기가 백본**이고 입력 경로가 넷(붙여넣기·이미지·URL·발견 후 선택)이라는 것이다. 즉 이 `⛔`는 기능 포기가 아니라 **증강 포기**다.
- 후속 : 재개하려면 원장의 T20 행을 `⬜`로 되돌리면 된다. 착수 시점은 여전히 자유롭다 — 의존이 T19뿐이고 T19는 완료다. 다음은 **T21(P4)**.

### [D68] T21 · OCR 이음매를 미리 뚫었다 + "한글 포함"을 "한글 또는 라틴"으로 완화
- 일자 : 2026-08-10
- 유형 : 선택결정
- 상황 : P4 첫 카드. 의존이 T01 하나뿐이라 착수는 자유로웠지만, **§2-1의 "누가 부르는가" 체크에 걸리는 카드**였다 — `tools/parse_resume.py`를 만들어도 호출자는 T22·T24에 있고 둘 다 미착수다.
- **T22가 이 파일을 고칠 수 없다는 게 핵심이었다.** T22의 소유 파일은 `tools/ocr.py`·`app/hitl.py` 둘뿐이다. 그런데 T21 카드의 계층도는 OCR 폴백을 **`parse_resume` 안**에 그려 놨다. 즉 T22가 규칙을 지키면 배선이 영영 안 붙고, 붙이려면 R2를 어겨야 한다 — **D39·D43·D59에서 세 번 반복된 그 구멍이 카드 설계 단계에 이미 박혀 있었다.**
- 조치 : **호출부를 T21이 미리 뚫었다.** `_try_ocr()`이 `from tools.ocr import ocr_document`를 지연 import하고 `ImportError`를 삼킨다. T22는 그 시그니처의 함수를 만들기만 하면 되고 이 파일로 돌아올 필요가 없다. 모듈 문서에 계약을 적어 뒀다. 효과는 양방향이다 — T21은 T22 없이 완결이고(테스트가 그걸 건다), T22는 들어오는 순간 죽은 모듈이 되지 않는다.
- **품질 검사의 ③을 카드 문구대로 안 만들었다.** 카드는 "한글 포함 여부"라고 적었는데 그대로 구현하면 **영문 이력서가 통째로 스캔본 취급**을 받아 멀쩡한 텍스트 레이어를 버리고 OCR을 탄다. 이 검사가 가려내야 하는 것은 *언어*가 아니라 **깨진 인코딩**이라, "한글 **또는** 라틴 문자가 N자 이상"으로 갔다. 모지바케는 한글도 라틴도 아니라 이 조건에 그대로 걸린다(테스트로 양쪽 다 건다). 카드 문구에서 벗어난 것이라 여기 적는다.
- **신뢰도는 층에서 나온다** — `CONFIDENCE_BY_LAYER`가 유일한 정의 지점이고 텍스트레이어 `상`/OCR `중`/실패 `하`다. T16의 규약(D52)을 어휘까지 그대로 가져왔다. 층마다 신뢰도가 다르다는 것 자체를 테스트가 건다(`len(set(values)) == 3`).
- **실패해도 건진 텍스트를 돌려준다.** 카드의 4)가 "추출 텍스트를 **보여주고** 직접 보정"이라 빈 문자열을 주면 사용자가 고칠 거리가 없다. T16이 "실패는 빈 본문 문서"였던 것과 결론이 갈리는데, 거기는 소비자가 코드고 여기는 사람이라 그렇다.
- 검증 : 오프라인 **38건**, 전체 **682 passed(16.1초)**. **뮤테이션 28종 전부 잡힘.** 1차는 24/28이었고 생존 넷의 성격이 갈렸다.
  | 생존 | 왜 안 잡혔나 | 조치 |
  | --- | --- | --- |
  | M11 판독 가능 경계 부등호 | 경계값 테스트가 없었다 | `MIN_READABLE_CHARS` 정확히/직전 두 건 추가 |
  | M22 DOCX 빈 문단 필터 제거 | 빈 문단이 있는 픽스처가 없었다 | 문단 사이에 빈 줄 100개 — 실제 이력서 모양 |
  | M23 PDF 첫 쪽만 읽음 | **픽스처가 한 쪽짜리였다** | 픽스처를 두 쪽으로 다시 굽고 2쪽 전용 낱말을 건다 |
  | M27 이미지도 텍스트레이어 시도 | 결과가 동치다(이미지를 PDF 추출기에 넣어도 빈 문자열) | 결과 말고 **호출 자체**를 본다 |
- **M23이 제일 값졌다.** 첫 쪽만 읽어도 분량은 여전히 임계를 넘어 품질 검사가 초록불을 준다 — **조용한 데이터 손실**이고, 이력서 뒤쪽에 몰리는 학력·수상·기타가 통째로 사라진다. 한 쪽짜리 픽스처로는 영원히 못 잡는다. **픽스처를 만들 때 "가장 단순한 형태"로 만들면 그 단순함이 그대로 사각지대가 된다.**
- **픽스처는 R5를 지켜 진짜로 만들었다.** `resume_scan.pdf`는 `resume_text.pdf`를 150dpi 흑백으로 구워 만든 것이라 텍스트 레이어가 **실제로 0자**다(테스트가 그걸 직접 확인한다). 스캔본을 흉내만 낸 파일이었다면 이 카드의 검증 전체가 가짜가 된다. 한글은 malgun.ttf를 서브셋 임베딩해 왕복을 보장했다(폰트 전체를 박으면 13MB).
- 의존성 : `pymupdf` 1.28.2(PDF 텍스트 레이어 + 픽스처 생성), `python-docx` 1.2.0(DOCX). `lxml` 6.1.1은 전이 의존.
- **경계 하나를 안 건드리고 남긴다** — `fixtures/README.md`가 픽스처 전체 목록표인데 T02 소유라 새 PDF 두 건을 못 적었다(R2). **T02 소유자나 다음에 `fixtures/`를 건드리는 카드가 두 줄 추가할 것.**
- 후속 : ① **T22는 `tools/ocr.py`에 `ocr_document(file_path: str) -> str`만 만들면 된다** — 실패는 예외가 아니라 빈 문자열, 배선은 이미 있다. ② H4를 띄울지는 `needs_manual_correction(text, confidence)`가 답한다(T16 `is_uncollected()`와 같은 역할). ③ **T24가 `parse_resume`을 실제로 부르는지 P4 완료 판정에서 확인할 것** — 이 카드의 소유 파일에는 노드도 배선 파일도 없다.

### [D69] T22 · 엔진은 기존 비전 어댑터, 다만 프롬프트는 갈랐다 — 그리고 카드가 쪼개졌다
- 일자 : 2026-08-11
- 유형 : 선택결정
- **엔진 선정 (카드 불변식)** — 후보 넷을 놓고 **기존 `llm/vision.py` 재사용**으로 갔다(사용자 판단).
  | 후보 | 강점 | 왜 안 골랐나 |
  | --- | --- | --- |
  | **OpenAI vision (채택)** | 키가 이미 있고 실제 채용공고 이미지로 관통 확인됨(D51). 회당 1콜이라 비용이 얕다(D49). `llm/` 어댑터 경계가 이미 서 있어 R6를 새로 뚫을 필요가 없다 | — |
  | Upstage Document Parse | 한국어 문서 특화, 표·레이아웃 보존이 가장 강함 | **새 API 키 발급이 필요**하다. T20을 건너뛴 이유(키 발급이 통제 불가 요소, D67)를 P4에서 다시 안는 셈 |
  | CLOVA OCR | 한국어 인식 품질, 국내 문서 양식에 강함 | 네이버 클라우드 가입 + 도메인 생성 + 키 발급까지 착수 지연 |
  | Tesseract | 무료·오프라인, 테스트가 네트워크를 안 탐 | **카드가 명시적으로 말리는 선택지** — 한국어 이력서 레이아웃에서 품질 편차가 크다(§16 S7) |
- **다만 `vision.py`를 그대로 부르면 안 됐다.** `EXTRACTION_PROMPT`가 JD 전용으로 하드코딩돼 있고("이 이미지는 채용 공고(JD) 화면을 캡처한 것이다 … 공고 본문이 아닌 것은 제외한다") 프롬프트 주입 매개변수가 없다. 이력서를 넣으면 모델이 문서 종류를 오인하고 **"공고가 아니다"라고 판단한 줄을 버리는데, 이력서에서 그렇게 잘리는 것은 날짜·직함·수상 같은 실제 내용이다.**
- 조치 : **`llm/ocr_vision.py` 신설.** `vision.py`는 T15b 소유라 매개변수를 뚫으면 R2에 걸린다 — `llm/embeddings.py`(D39)·`llm/vision.py`(D42)가 같은 이유로 갈라져 나온 그 판단의 세 번째 적용이다. 대신 **겹치는 것은 최대한 import**했다(MIME 정규화·지원 목록·용량 상한·예외 타입). 새로 쓴 것은 프롬프트와 그것을 싣는 입력뿐이고, 재시도 루프만 부득이하게 겹친다.
- **카드가 쪼개졌다 — H4는 얹힐 자리가 없었다.** T22 카드는 "엔진 + H4 보정"을 한 장에 담았는데, 착수해 보니 **보정 화면이 설 자리를 T24가 만든다**: ① `app/main.py`의 업로더는 `ProfileJSON`(.json)을 받지 이력서 파일을 안 받는다 ② `parse_resume`을 부르는 것은 T24의 `profile_graph`(`START → parse_resume → extract → level_survey → build_profile → END`)다. T22의 소유 파일(`tools/ocr.py`·`app/hitl.py`)로는 둘 다 만들 수 없다. → **T22는 엔진까지, H4는 신설 `T22b`로**(사용자 판단). 전례는 T06b·T14b·T15b. **이건 카드에 안 적힌 숨은 의존이었다** — 의존란은 "T21, T12"인데 실제로는 T24가 필요했다.
- **T21의 이음매가 값을 했다.** `tools/ocr.py`를 만들자 `parse_resume`이 배선 없이 그 경로를 탔고, 테스트가 그것을 직접 건다(`test_parse_resume이_이제_OCR_경로를_실제로_탄다`). D68이 미리 뚫어 둔 대로다.
- **그 대신 남의 테스트를 깼다(D59의 재현).** `tools/ocr.py`가 생기자 `tests/test_parse_resume.py`의 `no_stale_ocr` 픽스처가 무력해졌다 — `sys.modules`에서 지우기만 해서 **이제는 진짜 모듈이 import돼 실 vision API를 왕복**한다(§4 위반 + 조용한 과금). `None`을 꽂아 import 자체를 `ImportError`로 만들어 고쳤다. T22 소유가 아니지만 §4 정책 위반이라 고쳤다(D66과 같은 판단).
- 검증 : 오프라인 **33건**(T22) + 전체 **713 passed(13.5초)**. **뮤테이션 22종 전부 잡힘.** 1차는 18/24였고 생존 여섯의 성격이 셋으로 갈렸다.
  | 생존 | 성격 | 조치 |
  | --- | --- | --- |
  | M08 빈 응답인 쪽도 담음 | **죽은 가드** — 어댑터가 빈 응답을 이미 `VisionError`로 바꿔 도달 불가 | 가드 삭제 |
  | M11 빈 목록 조기 반환 제거 | **죽은 가드** — `join([])`이 이미 `""` | 가드 삭제 |
  | M04 렌더 해상도를 뭉갬 | 오프라인이 주입점 뒤라 **픽셀을 못 본다** | PNG IHDR을 직접 뜯어 너비로 dpi 검증 |
  | M05 회색조 대신 컬러 | 같음 | IHDR color type 0 검증 |
  | M12 빈 파일도 콜을 태움 | **방어가 두 겹**이라 어댑터가 먼저 먹었다(D54) | 어댑터를 스파이로 바꿔 **어느 계층에서 걸렀는지**를 본다 |
  | M22 MIME 정규화 생략 | 별칭(`image/JPG`)을 쓰는 테스트가 없었다 | 별칭 테스트 추가 |
- **M04·M05가 이 카드의 교훈이다.** 해상도와 색공간은 **OCR 품질과 비용을 직접 정하는 두 손잡이**인데, 주입점 뒤에 있는 오프라인 테스트로는 영원히 안 보인다. 실 API로 가면 비용이 들고 판정도 흐리다. 답은 중간이었다 — **모델에 보내기 직전의 바이트를 뜯어보는 것.** PNG 헤더 여섯 바이트면 둘 다 고정된다.
- 후속 : ① **온라인 검증이 남았다 — D70 참조(크레딧 소진).** ② H4는 T22b. ③ `MAX_PAGES = 10`은 비용 상한이며, 이력서가 그보다 길면 뒤가 잘린다(조용히). T22b의 보정 화면이 그것을 사용자에게 보여 주는 자리이기도 하다.

### [D70] T22 · 온라인 검증이 크레딧 소진으로 막혔다 — 다만 429는 정보다
- 일자 : 2026-08-11
- 유형 : 차단
- 상황 : `uv run pytest -m llm tests/test_ocr.py -q` 2건이 "실 API가 아무것도 못 읽었다"로 실패했다. `ocr_document`가 실패를 삼키는 설계라 화면에는 빈 문자열만 보였다.
- 원인 : **두 겹이었다.** ① 내가 넣은 `dummy_key` autouse 픽스처가 **온라인 테스트에까지 가짜 키를 덮어씌우고 있었다**(6초 만에 끝난 것이 신호였다 — 실 호출이면 그렇게 빠를 리 없다). `llm` 마커를 보고 비켜서게 고쳤다. ② 고치고 다시 돌리자 174초가 걸렸고 — 실제로 호출은 나갔다 — `429 insufficient_quota / credit_balance_exhausted`였다. **OpenAI 크레딧이 소진됐다.**
- **429가 400이 아니라는 것이 정보다.** 200dpi 회색조 PNG 187KB로 구운 페이로드가 OpenAI에 **도달해서 형식은 수용됐다**는 뜻이다. 즉 렌더링·인코딩·요청 구성까지 배관은 뚫렸고, 남은 미확인은 **모델이 한국어 스캔본을 실제로 읽는가** 하나다. 이건 D53(gzip)·D56(apex/www)처럼 실 네트워크만이 잡아 주는 층을 **부분적으로는 통과했다**는 뜻이기도 하다.
- **삼키는 설계의 대가를 봤다.** `ocr_document`는 "실패는 예외가 아니라 빈 문자열"이 계약이라 원인을 못 보여준다. 진단은 어댑터를 직접 부르는 스크립트로만 났다. 계약을 바꿀 일은 아니지만(호출부가 화면이라 그게 맞다) **이 카드를 다시 만지는 사람은 원인을 보려면 `llm/ocr_vision.py`를 직접 부를 것.**
- 조치 : 원장에서 T22를 **⚠️**로 뒀다. 오프라인은 전부 초록불이고 뮤테이션도 다 잡혔지만, **엔진 선정의 핵심 근거(인식 품질)가 미검증**이라 ✅는 자기보고가 된다(R4).
- 후속 : **크레딧 충전 후 `uv run pytest -m llm tests/test_ocr.py -q` 2건을 돌리고 원장을 ✅로 고칠 것.** 읽히지 않으면 그때 D69 표의 2순위(Upstage Document Parse)로 간다 — `llm/ocr_vision.py`가 이미 갈라져 있어 교체 지점은 그 파일 하나다. **§5 "차단 요소"의 2026-08-08 기록("크레딧 상태는 추정하지 말고 `-m llm` 1건으로 확인할 것")이 이번에도 맞았다.**

### [D71] T23 · 주입점을 기본 인자로 박으면 주입이 안 된다 (누수 일곱 번째) + 좌표 키 정리
- 일자 : 2026-08-11
- 유형 : 계약문제
- **누수 일곱 번째, 그리고 처음으로 "주입점이 있는데도" 샜다.** `bind_axes(survey, owned, *, matcher=retrieve_candidates)`로 썼다. 주입점이 버젓이 있으니 안전하다고 여겼는데, **파이썬 기본 인자는 함수 정의 시점에 묶인다** — `monkeypatch.setattr(module, "retrieve_candidates", fake)`가 이미 묶인 기본값을 바꾸지 못해 테스트가 실 임베딩 API를 왕복했다.
- **크레딧이 없어서 드러났다(D70).** 잔액이 있었으면 초록불인 채로 조용히 과금됐을 것이고, D49가 남긴 "느려지면 스텁이 새는 것" 신호도 이번엔 안 통했다 — 이 테스트 하나는 원래 느리지 않다. **차단이 오히려 그물 역할을 했다.**
- 조치 : 기본값을 `None`으로 두고 **호출 시점에** `matcher = matcher or retrieve_candidates`로 모듈 전역을 본다. 뮤테이션 M22가 이 회귀를 지킨다(기본 인자로 되돌리면 죽는다).
- **규약으로 올린다 — `def f(..., dep=some_function)` 형태를 쓰지 말 것.** 이 저장소의 주입점은 전부 `dep=None` + 호출 시점 해석이어야 한다. `tools/retrieve.py`의 `embed=None`·`llm/vision.py`의 `client=None`이 이미 그 형태이며, 이번 것만 어긋나 있었다.
- **`level_coordinates`의 키를 역량 id로 확정했다.** 질문세트 §6은 "역량명"이라 적었지만 `fixtures/profile_sample.json`이 `pf-01`을 쓰고 **D06이 그 전제 위에서 `my_level=None` 결정을 내려 뒀다.** 골든 픽스처와 기록된 결정이 산문 명세를 이긴다(§1-1 "계약은 산문이 아니라 실행 가능한 코드다"). 규칙: 축이 추출 역량과 묶이면 **그 역량의 comp_id**, 안 묶이면 **축 id**(`d4-02`)를 쓴다 — 후자는 설문이 새로 발견한 역량이라 id가 없다.
- **상태 계약에 설문 산출 칸이 없다.** `GraphState`에 `level_coordinates`도 `coverage`도 자리가 없고 R1이라 못 늘린다. 카드의 완료 조건이 "**ProfileJSON**의 …가 채워짐"이므로 `profile` 필드로 돌려줬다 — 계약 안에서 유일하게 맞는 자리이며, T18이 `GateStatus`에 칸이 없어 `missing` 라벨을 재사용한 것과 같은 판단이다(D59).
- **보유 역량을 `required`에서 읽는다.** 이름이 어색하지만 `GraphState`에 역량 목록 칸이 그것뿐이고, 프로필 모드에서는 `extract`가 그 칸을 채운다. 엉뚱한 칸을 보면 **사전 채움만 통째로 죽고 폼은 멀쩡히 뜬다**(조용한 회귀) — 뮤테이션 M21이 그 자리를 지킨다.
- **`app/hitl.py`에 `default`를 뚫은 것은 D28의 원칙과 충돌하지 않는다.** 그 파일은 "기본 선택을 두면 손대지 않은 항목이 답한 것으로 넘어간다"며 `index=None`을 못 박아 뒀는데, 레벨 측정의 사전 채움은 **없는 근거로 고르는 게 아니라 이력서에서 추출된 근거로 고른 것을 확인시키는 것**이라 그 우려가 성립하지 않는다. 레벨을 모르는 역량(`level=None`)은 여전히 아무것도 안 고른 채로 뜬다 — 뮤테이션 M09가 지킨다.
- **`C_인재상컬처핏`을 프리셋에서 뺐다.** 4단계 사다리는 "무엇을 해봤나"의 깊이 좌표인데 인재상은 그런 축이 아니다. 사용자에게 인재상을 4단계로 물으면 그 자체가 자기평가 척도가 된다(§1 첫 원칙 위반). 질문세트 §3의 산출 정의도 `coverage: {D1~D5 → …}`로 다섯 개만 적는다.
- **위젯 렌더를 가짜 `st`로 직접 걸었다.** 페이로드가 맞아도 그리는 쪽이 `default`를 무시하면 화면에서 조용히 사라지는데, `normalize_prompt`만 보는 테스트로는 안 잡힌다(H01이 그 자리다). `AppTest`는 T24가 화면을 만든 뒤에 쓸 것.
- 검증 : 오프라인 **48건**(T23) + 전체 **761 passed(10.4초)**. **뮤테이션 30종 전부 잡힘**(1차부터 생존 0 — 프리셋 3종·hitl 5종 포함).
- 후속 : ① **이 노드를 아직 아무도 안 부른다** — `graphs/profile_graph.py`는 T24가 만든다(§2-1 "누가 부르는가"). T21과 같은 상태이며, T24가 `parse_resume → extract → level_survey → build_profile`을 세우면 닫힌다. ② **`build_profile`이라는 이름이 T24의 노드와 겹친다** — 이쪽은 `nodes/level_survey.py`의 순수 함수이고 저쪽은 그래프 노드다. T24는 조립·영속을 맡고 이 함수를 부르면 된다. ③ 폼 안에서 사용자가 **올린** 선택에는 근거란이 안 붙는다(배치 폼이라 제출 전엔 알 수 없다) — 화면의 한계이고, 값이 큰 자리는 "시스템이 이미 높게 본 축"이라 대부분 커버된다.

### [D72] T24 · 계약에 이력서 칸이 없다 — `raw_jd_input`을 프로필 모드의 경로 칸으로 쓴다
- 일자 : 2026-08-11
- 유형 : 계약문제
- 상황 : `profile_graph`의 첫 노드는 **파일 경로**를 받아야 한다(`parse_resume(file_path)`가 T21의 계약이다). `GraphState`에는 그 경로를 담을 칸이 없고 R1이라 못 늘린다.
- 조치 : **`raw_jd_input`("사용자가 준 원본 입력")을 프로필 모드에서 이력서 경로로 쓴다.** 이 모드에는 JD가 없어 칸이 통째로 비어 있고, 같은 모양의 판단이 이미 둘 있다 — T18이 `GateStatus`에 칸이 없어 `missing` 라벨을 재사용했고(D59), T23이 설문 산출을 `profile`로 돌려줬다(D71). 대신 **문자열을 호출부에 새지 않게** `RESUME_PATH_KEY`와 `initial_profile_state()`를 유일한 통로로 뒀다.
- **`SourceType`에 RESUME이 없어 `기타`로 둔다.** 이건 아쉬운 타협이 아니라 오히려 맞는 자리다 — `source_coverage()`가 세는 세 유형(JD·기술블로그·인재상)은 **회사 정보**의 충족률이고, 이력서가 거기 끼면 회사 소스를 하나도 못 모았는데 충족률이 오른다.
- **역량 id에 `req-` 접두가 붙는다(표기상의 흠).** 보유 역량인데도 `req-resume-001-01`이 된다 — `extract`(T04)의 id 발급 규칙을 그대로 쓰기 때문이다. **무해하다**: 요구 역량 id는 JD 문서 id에서, 보유 역량 id는 `resume-001`에서 나와 네임스페이스가 겹치지 않고(테스트가 그 비충돌을 직접 건다), 고치려면 T04 소유 파일을 건드리거나 조립 단계에서 id를 다시 매겨야 하는데 후자는 `level_coordinates`의 키까지 함께 갈아야 해서 버그 자리만 는다(R7 — 기록만 한다).
- **`extract`는 분석 그래프 것을 그대로 재사용했다.** T23이 이미 그 전제로 쓰여 있었다(`nodes/level_survey.py`: "프로필 모드에서는 `extract`가 그 칸을 채운다"). 다만 그 프롬프트는 "이 직무가 **요구하는** 역량을 추출해라"라 이력서에 대면 말이 어긋난다 — 결과는 "문서에 적힌 역량"이라 실질이 같지만, **이력서 전용 프롬프트를 갖는 편이 정확하다.** T04 소유 파일이라 이 카드에서는 안 건드렸다(R7).
- 후속 : `parse_resume` 노드와 `extract` 사이가 **H4(T22b)가 들어갈 자리**다. 판별자는 `needs_manual_correction(text, confidence)`이고, 보정 결과는 같은 `source_docs`의 `raw_text`를 갈아끼우면 된다 — 배선을 위해 이 파일로 돌아올 필요가 없게 노드 docstring에 적어 뒀다(T21이 D68에서 한 것과 같은 이음매).

### [D73] T24 · 화면에 그래프가 둘이 됐다 — 접두사가 아니라 네임스페이스로 가른다
- 일자 : 2026-08-11
- 유형 : 설계결정
- 상황 : `app/main.py`는 그래프 하나(UC-2)를 전제로 쓰여 있었다. `GRAPH_KEY`·`TRACKER_KEY`·`THREAD_KEY`·`ERROR_KEY`가 전부 `st.session_state` 최상위에 산다. UC-1을 같은 화면에 얹으면 네 키가 전부 충돌한다.
- 조치 : **UC-1의 세션 상태를 `st.session_state[PROFILE_NS]` 안쪽 dict 하나에 통째로 넣었다.** `uc1_graph`처럼 접두사로 가르지 않은 이유는, 키를 더할 때마다 접두사를 잊을 자리가 생기고 그 순간 두 모드가 같은 스레드를 공유하기 때문이다. `get_or_create_thread()`가 `MutableMapping`이면 무엇이든 받게 돼 있어서(T10) **세션 계층은 한 줄도 안 고쳤다.**
- **`advance()`는 갈라 놓지 않고 인자 둘(`tracker`·`store`)만 뚫었다.** 실행 경로를 두 벌로 만들면 D27의 규칙("실행 전에 반드시 상태를 조회한다")을 지키는 자리가 둘이 되고, 그중 하나만 고친 순간 완료된 스레드가 rerun마다 다시 돈다. 기존 호출부는 인자를 안 주므로 그대로다.
- **진행 표시는 시트를 통째로 갈랐다 — `presets/profile_labels.yaml` 신설.** 노드 라벨만이 아니라 **제목**이 달라야 하는데(프로필을 만드는 내내 "회사 분석 진행 중"이 뜬다), 제목은 시트당 하나뿐인 값이라 한 파일에 둘을 못 담고 담으려면 스키마를 고쳐야 하는데 §2-1이 그걸 막는다. `load_labels(path)`가 처음부터 경로를 받게 돼 있어(T13) 코드는 안 고쳤다. **`node_labels.yaml`에 있던 `parse_resume`·`build_profile` 자리표시는 지웠다** — 배선하는 카드가 라벨을 갖는다는 §2-1 규약대로이며(D59가 같은 이동을 했다), 남겨 두면 같은 노드 라벨이 두 시트에 살아 한쪽만 고쳤을 때 조용히 어긋난다.
- **`st.radio(format_func=)`는 쓰지 말 것 — `AppTest`가 못 다룬다.** 내부 키(`"profile"`)를 선택지로 두고 `format_func`으로 라벨을 씌웠더니, `AppTest`는 옵션을 **꾸며진 문자열**로 들고 있으면서 `set_value`에는 꾸미기를 안 해서 `ValueError: 'profile' is not in list`로 죽는다. 화면 테스트가 아예 불가능해진다. **라벨을 그대로 선택지로 두고 뒤에서 되짚는다**(`MODE_BY_LABEL`).
- **`at.radio[0]`으로 위젯을 잡지 말 것.** 사이드바 위젯이 본문보다 **뒤**에 온다 — 설문이 떠 있는 동안 0번은 첫 문항이다. 이 파일의 화면 테스트는 전부 `key`로 찾는다.

### [D74] T24 · `build_profile` 노드가 무위(no-op)가 될 뻔했다 — 그래서 읽는 쪽을 함께 만들었다
- 일자 : 2026-08-11
- 유형 : 설계결정
- 상황 : 카드의 그래프는 `… → level_survey → build_profile → END`인데, **조립은 T23의 순수 함수 `build_profile()`이 이미 끝내고** `level_survey` 노드가 완성된 `ProfileJSON`을 상태에 넣어 준다(이름이 겹쳐 헷갈린다 — DEVPLAN이 미리 경고해 둔 자리다). 그대로면 노드가 상태를 그대로 되돌려주는 무위가 되고, 그건 §2-1이 경계하는 "아무도 안 부르는 모듈"의 거울상이다.
- 조치 : 이 노드에 **영속**을 맡겼다(DEVPLAN이 "조립·영속"이라 적은 대로). `.jobprep/profiles/`에 `ProfileJSON`을 떨구고, 파일명에 **내용 해시**를 붙여 같은 날 여러 번 만들어도 앞엣것이 안 덮인다.
- **읽는 쪽을 같이 만들지 않으면 저장은 그 자체로 죽은 코드다.** 그래서 UC-2 입력 폼에 `[직전에 만든 프로필 사용]`을 뒀다 — 다운로드·업로드 없이 UC-1 → UC-2로 넘어가는 지름길이다. **기본은 꺼짐이고, 그게 중요하다**: 켜 두면 예전에 만든 프로필이 사용자도 모르게 판정 근거가 되고 화면에는 아무 표시도 안 난다. 위젯은 저장본이 없어도 **늘 그린다**(비활성) — 있을 때만 나타나면 화면이 파일 시스템 상태에 따라 달라져 테스트가 흔들린다.
- **정본 export는 여전히 다운로드다**(설계 §10-3 "디스크에 떨어뜨리고 업로드로 받는다"). 그래서 저장 실패는 `OSError`를 삼키고 흐름을 세우지 않는다 — 디스크가 막혔다고 방금 만든 프로필을 잃게 하는 쪽이 손해가 크다. 대신 **저장과 다운로드가 같은 바이트를 쓰도록 직렬화 지점을 하나로 뒀다**(`profile_bytes`). 갈리면 "내려받은 건 업로드가 되는데 저장된 건 안 되는" 어긋남이 난다(뮤테이션 M20이 그 자리다).

### [D75] T24 · 완료 조건을 자동화했다 — 비교 대상은 "프로필 없음"이 아니라 "빈 프로필"이다
- 일자 : 2026-08-11
- 유형 : 설계결정
- 상황 : 카드의 검증란은 "수동 — 업로드 전후 질문 수 비교"다. T12가 같은 종류의 조건을 호출 횟수로 자동 증명한 전례가 있어(D31) 그대로 자동화했다. 수동 기록은 다음에 누가 회귀를 넣어도 아무 소리를 내지 않는다.
- **첫 시도가 뒤집혔다 — `verify` 노드는 프로필이 `None`이면 판정도 질문도 안 만든다**(빈 리스트 둘). 그래서 "프로필 없음 → 질문 0건", "프로필 있음 → 질문 N건"이 되어 **업로드하면 질문이 늘어난다.** 줄어들 것이 있으려면 판정이 돌아야 하므로 비교 대상은 **역량이 하나도 없는 빈 `ProfileJSON`**이다. 이건 테스트 기교가 아니라 제품의 사실이다 — 프로필 없이 쓰면 애초에 되묻지 않고 전부 공백 고지로 나간다(§11-2).
- **T12의 `install_stubs`를 쓸 수 없었다.** 그쪽 `fake_verify`는 프로필을 안 보고 정해진 기준만 질문으로 승격하므로, 프로필이 좋아져도 질문 수가 한 톨도 안 준다. 이 파일의 것은 `verify_criteria`의 **계약을 규칙으로 옮긴 것**이다 — 근거가 있으면 판정하고 없으면 Question으로 승격하며, 근거는 "프로필에 같은 이름의 역량이 있는가"로 본다. 이름은 양쪽 다 골든 픽스처에서 온다(R5).
- 결과 : **빈 프로필 12건 → UC-1 프로필 4건.** 프로필은 손으로 조립하지 않고 **UC-1 그래프를 실제로 돌려** 만들었고, `profile_bytes` → `model_validate_json` 왕복(= 다운로드 → 업로드)을 실제로 거친 뒤 UC-2에 넣었다. **개수를 정확히 건다**(`before - after == 이력서가 덮은 역량의 기준 수`) — "줄기만 하면 통과"면 한 건만 줄어도 초록불이다(D65 M21과 같은 이유).
- **스텁 누수 여덟 번째 — `nodes.interview`가 `verify_criteria`를 따로 import 해 간다.** 분석 쪽 스텁에서 그 한 줄을 빠뜨렸더니 델타 인터뷰의 재판정이 실 API로 나갔다(테스트가 20초를 넘겨 드러났다 — D43·D49가 남긴 신호 그대로다). T12의 `install_stubs`가 같은 두 곳을 거는 이유가 이것이다.
- 검증 : 오프라인 **38건**(T24) + 전체 **799 passed(14.2초)**. **뮤테이션 20종 전부 잡힘.** 1차는 18/20이었고 **생존 둘이 같은 성격**이었다 — `AppTest`가 checkpointer를 인메모리로 갈아끼우는 탓에 화면 테스트로는 안 보이는 자리다.
  | 생존 | 왜 안 잡혔나 | 조치 |
  | --- | --- | --- |
  | M14 UC-1이 UC-2의 스레드를 씀 | 모드마다 **다른 checkpointer 객체**를 쥐여 줘서 id가 겹쳐도 아무 일이 안 난다 (실제로는 sqlite 파일 하나를 공유한다) | 결과가 아니라 **키가 어디에 사는지**를 직접 건다 + 두 모드의 스레드 id가 다른지 |
  | M15 UC-1이 UC-2의 진행 표시 시트를 씀 | 라벨이 전부 fallback으로 떨어져도 흐름은 똑같이 끝까지 간다 | 패널 제목이 UC-1 시트의 것인지 + 줄에 fallback 문구가 없는지 |
- **두 그래프가 sqlite 파일 하나를 공유하는 경로는 화면 테스트가 한 번도 안 탄다**(위와 같은 이유). 실행에서만 나는 자리라 별도 테스트로 못 박았다 — 같은 db에 커넥션 둘을 열고, 한쪽 그래프의 스레드가 다른 쪽에 **미시작으로 보이는지**까지 건다.
- 후속 : ① **브라우저 관통은 부분 확인이다** — `streamlit run`으로 서버가 실제로 뜨는 것(200 / `_stcore/health` ok)까지 봤고, 화면 조작은 `AppTest`가 두 모드를 다 걷는다. 다만 사람 눈으로 본 스크린샷은 없다(D51에 해당하는 확인은 사용자 몫으로 남는다). ② 이력서 있는 UC-1 경로는 `extract` 1콜 + 임베딩 2왕복(중단 전후, T23이 감수한 값)이 든다. **이력서 없는 경로는 LLM 비용이 0이다** — `extract`가 문서 없이 부르면 API를 안 타고 사전 채움도 안 돈다. 데모에서 크레딧이 없을 때 쓸 수 있는 경로다.
