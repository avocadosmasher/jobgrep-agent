# T29 · 발표 자료

| | |
| --- | --- |
| Phase | P5 |
| 의존 | T27, T28 |
| 소유 파일 | `docs/presentation.md` |
| 읽어도 됨 | `docs/설계_v2.md`, `DEVLOG.md`, `eval/` 결과 |
| 설계 참조 | v2 §14, §11-1, §2-1 |

## 포함할 것

**1. 아키텍처 — 왜 이렇게 갈랐는가**
LangGraph 골격 + ReAct 수집 서브에이전트.
자율성이 필요한 곳은 수집 한 곳뿐이며, 나머지에 자율성을 주면 멱등성과 감사 가능성이 무너진다.

**2. 멱등성의 정확한 범위 (중요)**
> "구조와 계산은 멱등이고, 자연어 표현은 아니다. 그래서 평가를 자연어가 아니라 구조와 계산에 건다."

과한 주장("같은 입력이면 같은 결과")은 질의응답에서 역공당한다. 범위를 정직하게 좁힐 것.

**3. 비사용 기술의 근거**
- 캐시 — 회사당 콘텐츠 규모가 작아 이득 없음, TTL 관리 비용만 발생
- MCP — v1은 함수로 충분, 확장 시점에 서버화
- **Faiss는 억지가 아님** — 매칭 1단계 후보쌍 검색에 원래 필요했던 부품. "RAG를 위한 RAG를 넣지 않았다"

**4. 지표** — T27 일치율 + T28 과정 지표

**5. 라이브 데모** — JD 붙여넣기 → 진행 표시 → HITL 개입 → `.md` 다운로드

## 완료 조건
발표 자료 작성 + 데모 리허설 1회.
