# T28 · 과정 지표 로깅

| | |
| --- | --- |
| Phase | P5 |
| 의존 | T13 |
| 소유 파일 | `obs/metrics.py`, `app/progress.py` (수정), `app/main.py` (배선 1줄 · §2-1), `tests/test_metrics.py` |
| 읽어도 됨 | `app/progress.py`, `graphs/**` 시그니처 |
| 설계 참조 | v2 §13 |

## 맥락
루브릭이 결과 품질을 본다면 이건 **과정 품질**을 본다. 발표에서 "됩니다"가 아니라 숫자로 말할 수 있게 하는 부분.

## 지표
| 지표 | 목표 |
| --- | --- |
| 수집 성공률 | ≥ 90% |
| 근거 첨부율 | 100% |
| 최소 요건 충족률 | — |
| 응답 시간 | — |
| 도구 호출 횟수 중앙값 | — |

## 불변식
> **T13 스트림 이벤트와 동일 소스에서 수집한다. 중복 계측 금지.**
> T13에서 훅을 잘 만들어뒀다면 이 태스크는 거의 공짜다. 별도 계측을 새로 심으면 두 숫자가 어긋난다.

## 완료 조건
실행 1회당 지표 레코드가 파일로 남는다.

## 검증
```
pytest tests/test_metrics.py -q
```
