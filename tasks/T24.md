# T24 · profile_graph + ProfileJSON 저장/로드

| | |
| --- | --- |
| Phase | P4 |
| 의존 | T21, T23 |
| 소유 파일 | `graphs/profile_graph.py`, `app/main.py` (수정) |
| 읽어도 됨 | `contracts/**`, `nodes/**` 시그니처 |
| 설계 참조 | v2 §10-3 |

## 맥락
UC-1을 **별도 그래프**로 조립한다. 산출 `ProfileJSON`을 파일로 내보내고 UC-2가 업로드로 받는다.

**분리 이유**: ① 프로필은 회사 무관이라 두 번째 회사 분석 시 UC-1을 다시 태울 이유가 없다 ② 그래프가 작아져 디버깅·데모가 쉬워진다 ③ "프로필 = 영속, 다운로드/업로드" 상태 모델과 일치한다

## 그래프
```
START → parse_resume → extract → level_survey(HITL) → build_profile → END
```

## 불변식
- **사용자 프로필과 수집 결과를 혼합하지 않는다** — 혼합하면 "내 역량"에 타사 JD 문장이 유입된다
- UC-2 화면에 프로필 업로드 위젯 추가

## 완료 조건
프로필 다운로드 → UC-2에서 업로드 → **델타 인터뷰 질문 수가 유의미하게 감소**.

## 검증
수동 — 업로드 전후 질문 수 비교 기록
