# T22 · OCR fallback (+ H4 보정 → **T22b로 분리됨**)

> **이 카드는 엔진까지만 한다 (2026-08-11, D69).** 아래 "할 일" 2번(H4 보정)은
> **`tasks/T22b.md`로 빠졌다** — 보정 화면이 얹힐 그래프(`graphs/profile_graph.py`)와
> 이력서 업로드 화면을 **T24가 만들기 때문**이고, 이 카드의 소유 파일로는 둘 다 만들
> 수 없다. 소유 파일도 `tools/ocr.py` 하나로 줄었다(`app/hitl.py`는 T22b가 가져갔다).
> 완료 조건 역시 "스캔 이력서에서 텍스트가 추출된다"까지다.

| | |
| --- | --- |
| Phase | P4 |
| 의존 | T21, T12 |
| 소유 파일 | `tools/ocr.py`, `app/hitl.py` (수정) |
| 읽어도 됨 | `tools/parse_resume.py` 시그니처, `contracts/**` |
| 설계 참조 | v2 §8-5, §9-3 H4, §16 S7 |

## 맥락
한국어 문서 파싱 엔진이 **미확정 상태**(설계도 §16 S7). Tesseract는 한국어 이력서 레이아웃에서 품질 편차가 크므로 문서 파싱 API(Upstage Document Parse, CLOVA, Vision API 등)를 권장한다.

## 할 일
1. 엔진 선정 + 연동
2. **H4 보정** — 추출 텍스트를 사용자에게 보여주고 직접 수정하게 함
   → **T12의 재개 루프를 재사용.** 새 HITL 메커니즘 만들지 말 것

## 불변식
- OCR 실패도 **전체 실패로 만들지 않는다** — 사용자 직접 입력 경로로 폴백
- **엔진 선정 근거를 `DEVLOG.md`에 기록** (비교한 후보, 선택 이유, 비용)

## 완료 조건
스캔 이력서에서 텍스트 추출 후 사용자 보정이 반영됨.

## 검증
수동
