# T17 · fetch_tech_blog / get_company_values

| | |
| --- | --- |
| Phase | P3 |
| 의존 | T16 |
| 소유 파일 | `tools/fetch_soft.py`, `tests/test_fetch_soft.py` |
| 읽어도 됨 | `contracts/**` |
| 설계 참조 | v2 §6, §12-1 |

## 맥락
**소프트 요건.** 없어도 실패하지 않고 "미수집"으로 표기만 된다.
관통 규칙: **부분 실패를 전체 실패로 만들지 않는다.**

## 할 일
- `fetch_tech_blog(company) -> list[SourceDocument]` — 기술블로그·컨퍼런스 발표·GitHub org
- `get_company_values(company) -> list[SourceDocument]` — 인재상 페이지·채용 브랜딩

## 불변식
- 수집 실패 시 **빈 리스트 반환** + `gate_status["missing"]`에 항목 추가. 예외 전파 금지
- robots.txt 준수
- 지속가능경영보고서처럼 **통짜 대형 문서**가 걸려 컨텍스트 한계를 넘으면 그때만 RAG 청킹 적용 (평소엔 불필요)

## 완료 조건
네트워크 실패 시나리오에서 예외 없이 빈 결과 + missing 기록.

## 검증
```
pytest tests/test_fetch_soft.py -q
```
